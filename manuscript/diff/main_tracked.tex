\documentclass[pdflatex,sn-mathphys-num]{sn-jnl}
%DIF LATEXDIFF DIFFERENCE FILE
%DIF DEL /tmp/vouch_v1f/main.tex   Fri Sep  4 06:40:06 2026
%DIF ADD /tmp/vouch_v2f/main.tex   Fri Sep  4 06:40:06 2026


\theoremstyle{thmstyleone}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{thmstyletwo}
\newtheorem{remark}{Remark}
\theoremstyle{thmstylethree}
\newtheorem{definition}{Definition}

\usepackage{graphicx}
\usepackage{multirow}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{mathtools}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

% sn-mathphys-num.bst emits \burl for URLs and falls back to \textsf (which
% breaks on underscores) when \burl is undefined; route it through hyperref.
\providecommand{\burl}[1]{\url{#1}}

\graphicspath{{figures/}}

\newcommand{\vI}{\textcolor[rgb]{0.11,0.60,0.36}{\textbf{I}}}
\newcommand{\vR}{\textcolor[rgb]{0.80,0.16,0.16}{\textbf{R}}}
\newcommand{\vU}{\textcolor[rgb]{0.85,0.55,0.00}{\textbf{U}}}
\newcommand{\F}{\mathcal{F}}
%DIF 34a34
\newcommand{\G}{\mathcal{G}} %DIF > 
%DIF -------
\newcommand{\Ucal}{\mathcal{U}}
\newcommand{\KL}{\mathrm{KL}}
\newcommand{\Bern}{\mathrm{Bern}}
\newcommand{\eps}{\varepsilon}
\newcommand{\Efrak}{\mathbb{E}}

\raggedbottom
%DIF PREAMBLE EXTENSION ADDED BY LATEXDIFF
%DIF UNDERLINE PREAMBLE %DIF PREAMBLE
\RequirePackage[normalem]{ulem} %DIF PREAMBLE
\RequirePackage{color}\definecolor{RED}{rgb}{1,0,0}\definecolor{BLUE}{rgb}{0,0,1} %DIF PREAMBLE
\providecommand{\DIFadd}[1]{{\protect\color{blue}\uwave{#1}}} %DIF PREAMBLE
\providecommand{\DIFdel}[1]{{\protect\color{red}\sout{#1}}}                      %DIF PREAMBLE
%DIF SAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddbegin}{} %DIF PREAMBLE
\providecommand{\DIFaddend}{} %DIF PREAMBLE
\providecommand{\DIFdelbegin}{} %DIF PREAMBLE
\providecommand{\DIFdelend}{} %DIF PREAMBLE
\providecommand{\DIFmodbegin}{} %DIF PREAMBLE
\providecommand{\DIFmodend}{} %DIF PREAMBLE
%DIF FLOATSAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddFL}[1]{\DIFadd{#1}} %DIF PREAMBLE
\providecommand{\DIFdelFL}[1]{\DIFdel{#1}} %DIF PREAMBLE
\providecommand{\DIFaddbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFaddendFL}{} %DIF PREAMBLE
\providecommand{\DIFdelbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFdelendFL}{} %DIF PREAMBLE
\newcommand{\DIFscaledelfig}{0.5}
%DIF HIGHLIGHTGRAPHICS PREAMBLE %DIF PREAMBLE
\RequirePackage{settobox} %DIF PREAMBLE
\RequirePackage{letltxmacro} %DIF PREAMBLE
\newsavebox{\DIFdelgraphicsbox} %DIF PREAMBLE
\newlength{\DIFdelgraphicswidth} %DIF PREAMBLE
\newlength{\DIFdelgraphicsheight} %DIF PREAMBLE
% store original definition of \includegraphics %DIF PREAMBLE
\LetLtxMacro{\DIFOincludegraphics}{\includegraphics} %DIF PREAMBLE
\newcommand{\DIFaddincludegraphics}[2][]{{\color{blue}\fbox{\DIFOincludegraphics[#1]{#2}}}} %DIF PREAMBLE
\newcommand{\DIFdelincludegraphics}[2][]{% %DIF PREAMBLE
\sbox{\DIFdelgraphicsbox}{\DIFOincludegraphics[#1]{#2}}% %DIF PREAMBLE
\settoboxwidth{\DIFdelgraphicswidth}{\DIFdelgraphicsbox} %DIF PREAMBLE
\settoboxtotalheight{\DIFdelgraphicsheight}{\DIFdelgraphicsbox} %DIF PREAMBLE
\scalebox{\DIFscaledelfig}{% %DIF PREAMBLE
\parbox[b]{\DIFdelgraphicswidth}{\usebox{\DIFdelgraphicsbox}\\[-\baselineskip] \rule{\DIFdelgraphicswidth}{0em}}\llap{\resizebox{\DIFdelgraphicswidth}{\DIFdelgraphicsheight}{% %DIF PREAMBLE
\setlength{\unitlength}{\DIFdelgraphicswidth}% %DIF PREAMBLE
\begin{picture}(1,1)% %DIF PREAMBLE
\thicklines\linethickness{2pt} %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\framebox(1,1){}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\line( 1,1){1}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,1){\line(1,-1){1}}}% %DIF PREAMBLE
\end{picture}% %DIF PREAMBLE
}\hspace*{3pt}}} %DIF PREAMBLE
} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbegin}{\DIFaddbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddend}{\DIFaddend} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbegin}{\DIFdelbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelend}{\DIFdelend} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbegin}{\DIFOaddbegin \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbegin}{\DIFOdelbegin \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbeginFL}{\DIFaddbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddendFL}{\DIFaddendFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbeginFL}{\DIFdelbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelendFL}{\DIFdelendFL} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbeginFL}{\DIFOaddbeginFL \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbeginFL}{\DIFOdelbeginFL \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
%DIF COLORLISTINGS PREAMBLE %DIF PREAMBLE
\RequirePackage{listings} %DIF PREAMBLE
\RequirePackage{color} %DIF PREAMBLE
\lstdefinelanguage{DIFcode}{ %DIF PREAMBLE
%DIF DIFCODE_UNDERLINE %DIF PREAMBLE
  moredelim=[il][\color{red}\sout]{\%DIF\ <\ }, %DIF PREAMBLE
  moredelim=[il][\color{blue}\uwave]{\%DIF\ >\ } %DIF PREAMBLE
} %DIF PREAMBLE
\lstdefinestyle{DIFverbatimstyle}{ %DIF PREAMBLE
	language=DIFcode, %DIF PREAMBLE
	basicstyle=\ttfamily, %DIF PREAMBLE
	columns=fullflexible, %DIF PREAMBLE
	keepspaces=true %DIF PREAMBLE
} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim}{\lstset{style=DIFverbatimstyle}}{} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim*}{\lstset{style=DIFverbatimstyle,showspaces=true}}{} %DIF PREAMBLE
%DIF END PREAMBLE EXTENSION ADDED BY LATEXDIFF

\begin{document}

\title[Anytime-Valid Certificates for Machine Unlearning]{VOUCH: Anytime-Valid Certificates for Machine Unlearning via Paired-Canary Hypothesis-Betting}

% Double-blind toggle: \anontrue for the review copy (author identity moves to
% the cover letter); \anonfalse for the camera-ready version.
\newif\ifanon
\anontrue

\ifanon
\author*[1]{\sur{Anonymous Author}}
\affil*[1]{\orgname{Blinded for double-blind review}}
\else
\author*[1]{\fnm{Quang-Vinh} \sur{Dang}}\email{vinh.dq4@buv.edu.vn}
\affil*[1]{\orgname{British University Vietnam}, \orgaddress{\city{Hung Yen}, \country{Vietnam}}}
\fi

\DIFdelbegin %DIFDELCMD < \abstract{Machine unlearning methods for large language models are deployed to honour
%DIFDELCMD < deletion requests, yet they ship with no statistical certificate that forgetting
%DIFDELCMD < actually occurred. The evaluations in common use are fixed-sample tests: they are
%DIFDELCMD < unsound as membership evidence unless what enters training is randomised, their error
%DIFDELCMD < guarantees collapse the moment an auditor looks at the evidence as it accumulates, and
%DIFDELCMD < they lean on a retrained reference model that is unaffordable at scale and, for
%DIFDELCMD < multi-stage pipelines, not even well defined. We introduce \emph{VOUCH}, an
%DIFDELCMD < anytime-valid statistical certification framework that removes all three obstacles at
%DIFDELCMD < once. At fine-tuning time the provider plants \emph{paired ghost canaries}:
%DIFDELCMD < exchangeable twin records of which a fair coin admits one into training (and later into
%DIFDELCMD < the forget set, so that it traverses the entire unlearning pipeline) while the other is
%DIFDELCMD < never shown to any model. Because exact unlearning leaves the two twins statistically
%DIFDELCMD < interchangeable, the within-pair score difference is symmetric \emph{by construction};
%DIFDELCMD < this hands the verifier an exact, distribution-free, finite-sample null that needs
%DIFDELCMD < neither shadow models nor a retrained reference nor any assumption about the model.
%DIFDELCMD < Over this null VOUCH runs betting e-processes that remain valid at every stopping time
%DIFDELCMD < simultaneously, and from them derives three coupled objects: an anytime-valid confidence
%DIFDELCMD < sequence on the residual membership advantage, an $(\eps,\alpha)$-forgetting certificate
%DIFDELCMD < obtained by sequential two-sided equivalence testing, and a revocation alarm. We prove
%DIFDELCMD < that valid composition runs in opposite directions for the two claims---certificates
%DIFDELCMD < compose by consensus, alarms by product---and show empirically that the intuitive
%DIFDELCMD < alternatives are invalid, falsely certifying up to $99.6\%$ of the time. Across a
%DIFDELCMD < two-thousand-seed calibration study under adversarial optional stopping and end-to-end
%DIFDELCMD < pipelines on the TOFU and MUSE-News benchmarks over several architectures, VOUCH
%DIFDELCMD < certifies genuine unlearning, revokes un-unlearned models within a handful of canary
%DIFDELCMD < pairs, and---by pairing every certificate with a utility reading---exposes
%DIFDELCMD < gradient-ascent unlearning that forgets by destroying the model. Verification costs on
%DIFDELCMD < the order of $10^4$ forward passes, roughly two orders of magnitude below shadow-model
%DIFDELCMD < evaluation.}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \abstract{Unlearning methods for large language models honour deletion requests with no
statistical certificate that forgetting occurred. Existing evaluations are
fixed-sample tests: unsound without randomised inclusion, void under optional
stopping, and reliant on retraining. We introduce \emph{VOUCH}, an anytime-valid
certification framework. At fine-tuning time the provider plants \emph{paired ghost
canaries}, exchangeable twins: a fair coin admits one into training and later into the
forget set; the other is never trained on. Under exact unlearning the twins are
interchangeable, so their score difference is symmetric \emph{by construction}: an
exact, distribution-free, finite-sample null. Betting e-processes yield a confidence
sequence on the residual advantage, a two-sided $(\eps,\alpha)$-forgetting certificate
by sequential equivalence testing, and a revocation alarm. All canaries share one
trained model, so their signs are dependent; we certify the \emph{realised advantage
on the committed cohort} against a without-replacement boundary, exact under arbitrary
dependence, where a fixed boundary breaches its level eightfold. Certificates compose
by consensus, alarms by product, and the adaptive-mixture alternative of our own
earlier design is anytime-invalid, falsely certifying $99.6\%$ of the time under an
adversarially constructed composite null. Against four valid sequential comparators
and on TOFU and MUSE-News on three architectures, VOUCH certifies exact unlearning
down to $\eps = 0.05$ on the cohort target, revokes un-unlearned models within tens of
pairs, and exposes gradient ascent that destroys the model, at $2Qm$ forward passes.
The certificate concerns a declared score class on a committed cohort under an
honest-but-verifiable provider, and we measure what each qualification costs.}
\DIFaddend 

\keywords{machine unlearning, anytime-valid inference, e-values, membership inference,
certification, large language models}

\maketitle

\section{Introduction}\label{sec:intro}

When a user invokes the right to erasure, a model provider must do more than run an
unlearning routine: it must be able to \emph{show}, to a regulator or an internal audit
function, that the routine worked. This obligation is \DIFdelbegin \DIFdel{now concrete}\DIFdelend \DIFaddbegin \DIFadd{concrete, though it is worth
locating it precisely. Article~17 of the GDPR creates the right to erasure itself, but
the duty to }\emph{\DIFadd{demonstrate}} \DIFadd{rather than merely assert compliance lives in
Articles~5(2) and 24(1), which require controllers to be able to show that processing
conforms to the Regulation, and in Article~58(1)(b), which empowers supervisory
authorities to conduct data-protection audits \mbox{%DIFAUXCMD
\citep{gdpr2016}}\hskip0pt%DIFAUXCMD
}\DIFaddend . The EU AI Act \DIFdelbegin \DIFdel{'s
audit provisions, Article~17 of the GDPR, }\DIFdelend \DIFaddbegin \DIFadd{adds
obligations of a different shape: automatic event logging over a system's lifetime
(Article~12), technical documentation and its ten-year retention (Articles~11 }\DIFaddend and \DIFdelbegin \DIFdel{a growing set of national data-protection
regimes all presume that erasure can be demonstrated, not merely asserted.
}\DIFdelend \DIFaddbegin \DIFadd{18),
production of that documentation and those logs to authorities on reasoned request
(Article~21), conformity assessment (Article~43), and the AI Office's power to evaluate
general-purpose models (Article~92) \mbox{%DIFAUXCMD
\citep{aiact2024}}\hskip0pt%DIFAUXCMD
. Neither instrument prescribes a
statistical test for erasure. Both presuppose that a provider can produce evidence, and
that is the gap this paper addresses.
}

\DIFaddend The algorithmic side of machine unlearning has risen to meet the demand: gradient ascent
and its stabilised successors, negative preference optimisation
\citep{zhang2024npo,fan2025simnpo}, representation misdirection \citep{li2024wmdp},
and task-vector negation now remove targeted knowledge from large models at modest
cost. The \emph{verification} side has not kept pace, and \DIFdelbegin \DIFdel{it is verification}\DIFdelend \DIFaddbegin \DIFadd{a substantial recent
literature documents why: forget-quality metrics track surface behaviour rather than
retained knowledge and collapse under attack \mbox{%DIFAUXCMD
\citep{wang2025towards}}\hskip0pt%DIFAUXCMD
, output-matching
scores hide degenerate models \mbox{%DIFAUXCMD
\citep{yuan2025closer}}\hskip0pt%DIFAUXCMD
, benign relearning on unrelated
public text restores supposedly erased content \mbox{%DIFAUXCMD
\citep{hu2025jogging}}\hskip0pt%DIFAUXCMD
, and
gradient-ascent objectives redistribute probability mass onto paraphrases so that
standard metrics report success while the information remains
\mbox{%DIFAUXCMD
\citep{li2026beliefs,feng2025existing}}\hskip0pt%DIFAUXCMD
. Evaluation}\DIFaddend , not optimisation, \DIFdelbegin \DIFdel{that a regulator cares about}\DIFdelend \DIFaddbegin \DIFadd{is the bottleneck}\DIFaddend .

\DIFdelbegin \DIFdel{Three }\DIFdelend \DIFaddbegin \paragraph{\DIFadd{Three obstacles.}} \DIFadd{Three specific }\DIFaddend obstacles stand between current practice
and a defensible certificate\DIFdelbegin \DIFdel{, and it is
worth stating them plainly because VOUCH is organised around dismantling each one}\DIFdelend . \emph{First}, a high membership-inference score on a
supposedly forgotten example is not evidence that the example was ever trained on.
Distribution shift and the model's own priors confound the signal, and
\citet{zhang2024mia} have shown formally that membership cannot be established without
controlling the randomness of what enters training. \emph{Second}, deletion requests
arrive as a stream and auditors inspect the evidence as it accrues, stopping when they
are satisfied; every fixed-sample test in current use---the forget-quality metric of
\citet{maini2024tofu}, the leakage metric of \citet{shi2024muse}, the shadow-model
panels of \citet{hayes2024inexact}---has its error guarantee voided by exactly this
optional stopping, and we measure the damage in Section~\ref{sec:exp-validity}.
\emph{Third}, the comparators these tests rely on---a model retrained from scratch
without the deleted data---are prohibitively expensive at the scale of modern language
models and, as \citet{yu2025impossibility} show, are not even well defined for the
multi-stage pipelines used in practice, because path-dependence means no unique
retrained model exists to compare against.

\DIFdelbegin \DIFdel{VOUCH dissolves all three obstacles }\DIFdelend \DIFaddbegin \paragraph{\DIFadd{The design.}} \DIFadd{VOUCH addresses all three }\DIFaddend with a single design decision and a
single inferential engine. The design decision is the \emph{paired ghost canary}. At
fine-tuning time we generate exchangeable twin records, and for each pair we flip a
fair coin to decide which twin is admitted into training; the admitted twin is later
appended to the forget set, so that it passes through the very same unlearning pipeline
as organic data, while its twin is withheld from every model. This construction is what
makes the audit sound: under exact unlearning the model is independent of the coin, so
the two twins are exchangeable and the difference between their scores is symmetric
about zero \emph{regardless of the model, the scoring function, or the data
distribution}. We therefore obtain an exact, distribution-free, finite-sample null that
we generated ourselves, rather than one we must assume. The engine is the betting
e-process from game-theoretic statistics, which turns this null into a test whose
validity holds not at one predetermined sample size but at every stopping time at once,
by Ville's inequality. Streaming deletions and continuous public monitoring, fatal to a
fixed-sample test, are precisely what an e-process is built to survive.

\DIFaddbegin \paragraph{\DIFadd{What the certificate claims, and what it does not.}} \DIFadd{We state the scope here
because it governs how every later number should be read, and because a certificate
whose scope is overclaimed is worse than none. A VOUCH certificate asserts that the
residual advantage extractable by a }\emph{\DIFadd{declared class $\F$ of scores}}\DIFadd{, on the
}\emph{\DIFadd{committed canary cohort}}\DIFadd{, against the }\emph{\DIFadd{deployed model}}\DIFadd{, is below a declared
tolerance $\eps$, under an }\emph{\DIFadd{honest-but-verifiable}} \DIFadd{provider who genuinely executes
the unlearning routine on the forget set as declared. Four qualifications follow, and
each is quantified rather than merely disclosed. The guarantee is per-cohort, not
per-deletion-request, since per-request certificates would need per-user canaries. It
concerns $\F$, and that is a definitional limit: two weak attacks from outside $\F$
(Section~\ref{sec:exp-outclass}) do }\emph{\DIFadd{not}} \DIFadd{beat the noise floor of subsampling and
so settle nothing either way, while a LiRA-style shadow-model attack
(Section~\ref{sec:exp-lira})---demonstrably potent, recovering an advantage of $0.82$
on an un-unlearned model---finds none on a certified one, which bounds the gap on that
tier without closing it in general. The transfer from the realised
cohort to the mechanism-level advantage costs an explicit inflation of
$\kappa\sqrt{2\log(2/\alpha')/m}$ (Proposition~\ref{prop:transfer}), which is why our
tight-tolerance results are cohort statements. And the honest-but-verifiable assumption is load-bearing:
Section~\ref{sec:exp-detect} shows a zero-knowledge perplexity filter recovers the
entire planted cohort, so a provider who wished to special-case it could find it, and
only cryptographic attestation \mbox{%DIFAUXCMD
\citep{eisenhofer2025verifiable} }\hskip0pt%DIFAUXCMD
closes that gap. What
detection does not do is defeat the audit, because both twins in a pair are equally
conspicuous and locating the cohort reveals nothing about which twin the coin admitted.
}

\paragraph{\DIFadd{Corrections carried by this version.}} \DIFaddend Building the framework and running it
end-to-end forced \DIFdelbegin \DIFdel{four corrections and
refinements }\DIFdelend \DIFaddbegin \DIFadd{five corrections }\DIFaddend on the initial design, and because each turned out
to matter \DIFdelbegin \DIFdel{in practice }\DIFdelend we foreground them rather than bury them. \DIFdelbegin \DIFdel{We found that the }\DIFdelend \DIFaddbegin \emph{\DIFadd{(i)}} \DIFadd{The certificate's null
must be stated conditionally, not marginally. Signs from all canaries pass through one
jointly trained model, so they are dependent; a fixed-boundary process that assumes
otherwise is breached eightfold under model-level over-dispersion
(Table~\ref{tab:dependence}). We retarget the certificate at the realised cohort
advantage and bet against a without-replacement boundary, which is exact under
arbitrary dependence (Theorem~\ref{thm:cert}). }\emph{\DIFadd{(ii)}} \DIFadd{The }\DIFaddend natural way to combine
several attack scores into one certificate---bet on an adaptively weighted mixture of
their signs---is \emph{anytime-invalid}, and in simulation \DIFdelbegin \DIFdel{it }\DIFdelend \DIFaddbegin \DIFadd{our own earlier design
}\DIFaddend falsely certified a leaking model $99.6\%$ of the time \DIFaddbegin \DIFadd{under an adversarially
constructed composite null}\DIFaddend ; the sound alternative requires every score's own process
to concur. \DIFdelbegin \DIFdel{We found that certificates }\DIFdelend \DIFaddbegin \emph{\DIFadd{(iii)}} \DIFadd{Certificates }\DIFaddend and alarms must compose
in \emph{opposite} directions across deletion waves, and \DIFdelbegin \DIFdel{that }\DIFdelend multiplying certificate
e-values, the instinctive move, certifies histories that contain a genuinely bad wave.
\DIFdelbegin \DIFdel{We found }\DIFdelend \DIFaddbegin \emph{\DIFadd{(iv)}} \DIFadd{Gradient-ascent unlearning frequently }\emph{\DIFadd{over-forgets}} \DIFaddend on real language
models\DIFdelbegin \DIFdel{that gradient-ascent unlearning frequently }\emph{\DIFdel{over-forgets}}%DIFAUXCMD
\DIFdelend , driving the forgotten twins to scores conspicuously \emph{below} their
never-seen partners---membership leakage that a one-sided test happily certifies and
that a two-sided test must catch. \DIFdelbegin \DIFdel{And we found, when we corrected the sample-size analysis
against the information-theoretic lower bound, that an earlier back-of-envelope estimate
understated the required cohort by threefold. Each of these is now a theorem or a measured result rather than a footnote.
}\DIFdelend \DIFaddbegin \emph{\DIFadd{(v)}} \DIFadd{The small-$\eps$ expansion of the
Kullback--Leibler drift is $\eps^2/2$, not $\eps^2/8$, so the cohort-size rule is
$2\log(1/\alpha)/\eps^2$; the numerical columns were always computed from the exact
divergence and are unaffected, but the stated rule was wrong by a factor of four.
}\DIFaddend 

\DIFdelbegin \DIFdel{The contributions of this paper are as follows.
}\DIFdelend \DIFaddbegin \paragraph{\DIFadd{Contributions.}} \DIFaddend We formulate unlearning certification as sequential
two-sided equivalence testing against a declared attack class, with an exact
distribution-free null supplied by paired ghost canaries
(Section~\ref{sec:framework}). We give the supporting theory: \DIFdelbegin \DIFdel{exact validity at any
stopping time}\DIFdelend \DIFaddbegin \DIFadd{an exact conditional null
requiring no independence across pairs, a certificate that is exact under arbitrary
dependence via without-replacement betting, a priced bridge to the super-population
advantage}\DIFaddend , sound composition across scores and across deletion waves in both
directions, power that matches the Kullback--Leibler information limit, \DIFdelbegin \DIFdel{a
magnitude-aware revocation arm that is exact
under the symmetry null}\DIFdelend \DIFaddbegin \DIFadd{an exact
finite-cohort certification-time bound that holds for every betting strategy and every
reveal order}\DIFaddend , and a \DIFaddbegin \DIFadd{one-directional consistency }\DIFaddend bridge from certified
$(\eps_u,\delta_u)$-unlearning (Section~\ref{sec:theory}). \DIFdelbegin \DIFdel{Finally we }\DIFdelend \DIFaddbegin \DIFadd{We }\DIFaddend report a comprehensive empirical study
(Section~\ref{sec:exp}): calibration under adversarial optional stopping over \DIFdelbegin \DIFdel{two thousand seeds; }\DIFdelend \DIFaddbegin \DIFadd{2}{\DIFadd{,}}\DIFadd{000
seeds; head-to-head comparison against four }\emph{\DIFadd{valid}} \DIFadd{sequential procedures;
}\DIFaddend end-to-end certification of gradient ascent, GradDiff, \DIFdelbegin \DIFdel{negative preference optimisation, and retraining-from-scratch on the }\DIFdelend \DIFaddbegin \DIFadd{NPO and retraining on }\DIFaddend TOFU and
MUSE-News \DIFdelbegin \DIFdel{benchmarks across three model }\DIFdelend \DIFaddbegin \DIFadd{across three }\DIFaddend architectures, with \DIFdelbegin \DIFdel{utility guardrails }\DIFdelend \DIFaddbegin \DIFadd{SimNPO and representation misdirection
added on TOFU/GPT-2 and the latter confirmed on a second architecture and a second
benchmark, a tolerance sweep down to $\eps = 0.05$, utility guardrails, }\DIFaddend and
dose--response calibration; \DIFaddbegin \DIFadd{a head-to-head
against the descriptive metrics VOUCH is argued to replace; attacks from outside the
declared score class, including a LiRA-style shadow-model attack with matched shadows
and a positive control; a paraphrase-aware score added to the declared class and
priced; a measurement of how detectable the planted cohort is, with an
entropy-dilution design rule that follows from it; }\DIFaddend recoverability probes that relearn,
quantise, and jailbreak the unlearned model; a model axis spanning 2019 to 2026; and
cost measurements \DIFdelbegin \DIFdel{showing
that a certificate takes seventy-six seconds on a single mid-range GPU}\DIFdelend \DIFaddbegin \DIFadd{with their hardware and workload assumptions stated}\DIFaddend . All code, all
results, and the fault-tolerant harness that produced them on free ephemeral compute
are released.

\section{Related work}\label{sec:related}
Our work sits at the meeting point of \DIFdelbegin \DIFdel{three }\DIFdelend \DIFaddbegin \DIFadd{four }\DIFaddend literatures---unlearning algorithms, the
\DIFaddbegin \DIFadd{measurement of memorisation and forgetting, the }\DIFaddend verification of unlearning, and
game-theoretic statistics---and it is the gap between the \DIFdelbegin \DIFdel{first }\DIFdelend \DIFaddbegin \DIFadd{middle }\DIFaddend two that
game-theoretic statistics turns out to close. We survey each in turn and then state
precisely how VOUCH differs from its nearest neighbours.

\subsection{Unlearning algorithms and their benchmarks}

Exact unlearning by architectural design, exemplified by sharded training
\citep{bourtoule2021machine}, retrains only the shards touched by a deletion; it is
sound but cannot be applied to a model that was not trained for it, which is the
situation every deployed language model is in. The practical response has therefore
been \emph{approximate} unlearning, and for large language models a compact toolbox
has emerged: gradient ascent on the forget set and its retain-regularised variant
GradDiff; negative preference optimisation \citep{zhang2024npo}, which replaces the
unbounded ascent objective with a bounded preference loss and which its authors
introduced precisely to avoid the catastrophic collapse that ascent invites; the
simplified reappraisal SimNPO \citep{fan2025simnpo}\DIFaddbegin \DIFadd{, which discards NPO's frozen
reference model in favour of a length-normalised margin objective and which we
include as a subject in Section~\ref{sec:exp-bench}}\DIFaddend ; representation misdirection
\citep{li2024wmdp}\DIFaddbegin \DIFadd{, which acts on hidden activations rather than on the token loss and
which we add as a subject in Section~\ref{sec:exp-rmu}}\DIFaddend ; and task-vector negation. These methods are evaluated on
purpose-built benchmarks---TOFU's fictitious-author question answering
\citep{maini2024tofu}, MUSE's six-way news-and-books evaluation \citep{shi2024muse},
and the hazardous-knowledge multiple-choice of WMDP \citep{li2024wmdp}---and we treat
all of them, methods and benchmarks alike, as \emph{subjects} that VOUCH certifies
rather than as competitors.

\DIFdelbegin \DIFdel{What this }\DIFdelend \DIFaddbegin \subsection{\DIFadd{Measuring memorisation and forgetting}}

\DIFadd{The canary methodology VOUCH depends on descends directly from the secret-sharer line
of work. \mbox{%DIFAUXCMD
\citet{carlini2019secret} }\hskip0pt%DIFAUXCMD
insert random secret sequences into training data
and define }\emph{\DIFadd{exposure}}\DIFadd{, the canary's rank among all possible instantiations
ordered by model log-perplexity, converting memorisation into a calibrated number;
they show that memorisation is commonplace, that it is not explained by overfitting,
and that it is eliminated only by differentially private training. VOUCH borrows the
planted-canary device and changes one thing that turns out to matter for inference:
where the secret sharer plants a canary and }\emph{\DIFadd{measures}} \DIFadd{its exposure, we plant a
}\emph{\DIFadd{pair}} \DIFadd{and randomise which twin enters training, so that the measurement acquires
a null. Exposure answers ``how memorised is this string''; the ghost twin answers
``could this have happened by chance,'' which is the question a certificate must
answer and the one \mbox{%DIFAUXCMD
\citet{zhang2024mia} }\hskip0pt%DIFAUXCMD
show cannot be answered without controlled
randomisation.
}

\DIFadd{The strongest membership attacks in this lineage calibrate per example rather than
thresholding a raw score. \mbox{%DIFAUXCMD
\citet{carlini2022lira} }\hskip0pt%DIFAUXCMD
show that comparing a target model's
confidence on an example against the confidences that shadow models trained with and
without it produce---a likelihood-ratio test they call LiRA---dominates score-threshold
attacks, particularly in the low false-positive regime that matters for a privacy
claim. LiRA is the natural adversary against which to stress-test a declared score
class, since it uses information no member of ours has: $N$ additionally trained
models. It is for the same reason not a candidate for inclusion in $\F$, because a
verifier held to $2Qm$ forward passes and no training cannot run it. We use it in
Section~\ref{sec:exp-lira} as an attack from outside the class, with a positive
control that establishes it is working before its null on a certified model is read as
meaning anything.
}

\DIFadd{The complementary literature measures forgetting rather than memorisation.
\mbox{%DIFAUXCMD
\citet{jagielski2023measuring} }\hskip0pt%DIFAUXCMD
quantify how the success of privacy attacks on a
training example decays as training continues past it, and find that standard models
do forget over time---but also prove that non-convex models can retain memorised data
indefinitely, and show that forgetting disappears entirely under deterministic
training. Their conclusion is the premise of ours: incidental forgetting is real but
uncontrolled, and therefore cannot substitute for a certificate.
\mbox{%DIFAUXCMD
\citet{barbulescu2024each} }\hskip0pt%DIFAUXCMD
document, in the same spirit, how per-sequence memorisation
strength governs how hard a datum is to remove, which is the phenomenon our
repetition strata are designed to traverse.
}

\subsection{\DIFadd{Evaluating unlearning: a literature of negative results}}

\DIFadd{What the unlearning }\DIFaddend literature has increasingly documented \DIFdelbegin \DIFdel{, however, }\DIFdelend is that the metrics used
to declare success are unreliable\DIFdelbegin \DIFdel{. A growing body of adversarial work }\DIFdelend \DIFaddbegin \DIFadd{, and this body of work is the direct motivation for
VOUCH. A first strand }\DIFaddend shows that apparent forgetting is often mere suppression\DIFdelbegin \DIFdel{:
}\DIFdelend \DIFaddbegin \DIFadd{.
}\DIFaddend \citet{lucki2024adversarial} recover ostensibly unlearned capabilities through
lightweight attacks; \citet{zhang2025quantization} show that \DIFdelbegin \DIFdel{simply }\DIFdelend quantising an unlearned
model to \DIFdelbegin \DIFdel{four bits resurrects the }\DIFdelend \DIFaddbegin \DIFadd{$4$ bits resurrects }\DIFaddend forgotten knowledge; \citet{deeb2024remove} recover most
\DIFdelbegin \DIFdel{of the }\DIFdelend pre-unlearning accuracy by fine-tuning on adjacent facts; and \citet{fan2025relearning}
formalise relearning attacks and connect robustness to sharpness-aware minimisation.
\DIFdelbegin \DIFdel{The evaluations themselveshave come under equally
sharp scrutiny}\DIFdelend \DIFaddbegin \DIFadd{Closest to our P1 probe is \mbox{%DIFAUXCMD
\citet{hu2025jogging}}\hskip0pt%DIFAUXCMD
, who show that fine-tuning an
unlearned model on a small, }\emph{\DIFadd{benign}}\DIFadd{, publicly available corpus that never
contains the forget targets is enough to jog the model's memory and restore both
hazardous knowledge and verbatim memorised passages. Their framing---obfuscation
versus removal---is precisely the distinction our relearn probe operationalises, and
their result is why we treat a post-relearning verdict as part of the certificate
rather than as an optional extra: on our benchmark tier at $\eps = 0.2$ the same probe
revokes NPO's certificate on one MUSE/GPT-2 seed and one MUSE/Phi-1.5 seed, and leaves
it intact on every TOFU cell (Section~\ref{sec:exp-bench}).
}

\DIFadd{A second strand indicts the evaluation protocols themselves}\DIFaddend .
\citet{feng2025existing} demonstrate that current LLM-unlearning evaluations can be
made to both overstate and understate success\DIFdelbegin \DIFdel{;
}\DIFdelend \DIFaddbegin \DIFadd{. \mbox{%DIFAUXCMD
\citet{wang2025towards} }\hskip0pt%DIFAUXCMD
show that
forget-quality metrics track surface output behaviour rather than retained knowledge
and collapse under red-teaming, and---more damagingly for cross-paper
comparison---that the reported unlearning/retention trade-off does not lie on a Pareto
frontier, so method rankings are artifacts of hyperparameter choice; they propose
recalibrating retained performance before comparing. \mbox{%DIFAUXCMD
\citet{yuan2025closer} }\hskip0pt%DIFAUXCMD
make the
complementary point that ROUGE-style output matching hides degenerate behaviour, and
add metrics for token diversity, sentence semantics, and factual correctness. Both
diagnoses apply to VOUCH's subjects and shaped how we report them: we do not rank
unlearning methods against one another, we certify each one against a declared
tolerance and pair every verdict with a utility reading, precisely because a ranking
built on a non-Pareto trade-off would not survive a change of hyperparameters. Our own
worked example in Figure~\ref{fig:example} is an instance of the degeneracy
\mbox{%DIFAUXCMD
\citet{yuan2025closer} }\hskip0pt%DIFAUXCMD
warn about---an NPO-unlearned model emitting
}\texttt{\DIFadd{LLLLLL\ldots}} \DIFadd{on canary prompts while its held-out loss is untouched---and it
is caught here not by a fluency metric but by the two-sided certificate, which treats
scoring conspicuously }\emph{\DIFadd{below}} \DIFadd{the ghost twin as leakage.
}

\DIFadd{A third strand is mechanistic and, for our purposes, the most pointed.
}\DIFaddend \citet{wei2025forget} show that facts presumed forgotten persist through correlated
knowledge\DIFdelbegin \DIFdel{; }\DIFdelend \DIFaddbegin \DIFadd{, and }\DIFaddend \citet{xu2026reversibility} distinguish genuine erasure from
suppression at the representation level\DIFdelbegin \DIFdel{and find that behaviour is }\DIFdelend \DIFaddbegin \DIFadd{, finding behaviour }\DIFaddend easily restored by minimal
fine-tuning\DIFdelbegin \DIFdel{; and
\mbox{%DIFAUXCMD
\citet{barbulescu2024each} }\hskip0pt%DIFAUXCMD
document how per-sequence memorisation
strength governs how hard a datum isto remove.
}\DIFdelend \DIFaddbegin \DIFadd{. \mbox{%DIFAUXCMD
\citet{li2026beliefs} }\hskip0pt%DIFAUXCMD
identify the mechanism they call the
}\emph{\DIFadd{squeezing effect}}\DIFadd{: gradient-ascent-style objectives push probability mass off
the target response but redistribute it onto nearby high-likelihood regions, typically
semantically equivalent rephrasings, so the content resurfaces under paraphrase and
commonly used metrics report success while semantically related information remains.
Their remedy couples the unlearning objective to the model's own high-confidence
generations. This result bears directly on the scope of what VOUCH certifies and we
want to be exact about the relationship. VOUCH is a }\emph{\DIFadd{verification}} \DIFadd{framework and
inherits the limits of its declared score class: our scores read the likelihood of the
literal secret span under a fixed set of query wrappers, so probability mass that has
migrated to a paraphrase is, by construction, mass our default class does not see. The
framework's response is structural rather than rhetorical---the class $\F$ is declared,
the certificate is explicitly a statement about $\F$, and any score a sceptic can name
can be added to $\F$ at the cost of a slower certificate and no loss of validity, since
Theorem~\ref{thm:cert} needs no multiplicity correction. Sections~\ref{sec:exp-outclass} and
\ref{sec:exp-lira} run attacks from }\emph{\DIFadd{outside}} \DIFadd{the declared class against
already-certified models and report where they exceed the tolerance. We also
implement the score their result calls for: Section~\ref{sec:exp-para} adds a
paraphrase-aware member to $\F$, which costs one to two per cent in pairs and changes
no verdict---though we are explicit there that our random-string secrets have no
semantic neighbourhood for mass to migrate into, so this tests the score's cost rather
than the squeezing effect itself, which would need canaries whose secret is
paraphrasable.
}

\DIFaddend The recurring message across all of this \DIFdelbegin \DIFdel{work }\DIFdelend is that \emph{evaluation}, not optimisation,
is the bottleneck---which is exactly the problem VOUCH addresses, and which motivates
our recoverability probes (R-VOUCH) and our insistence on pairing every certificate
with a utility reading.

\subsection{Verifying and certifying unlearning}

The earliest guarantees came from \emph{certified removal}
\citep{guo2020certified,sekhari2021remember,neel2021descent,ginart2019making}, which
bounds the distance between the unlearned and retrained models in a differential-
privacy-style $(\eps_u,\delta_u)$ \DIFdelbegin \DIFdel{sense}\DIFdelend \DIFaddbegin \DIFadd{sense---the same framing \mbox{%DIFAUXCMD
\citet{dang2021rtbf}
}\hskip0pt%DIFAUXCMD
argues is the more defensible reading of ``forgotten'' than the erasure language of
the GDPR itself, since it requires that no test can tell whether the deleted record
was ever present, exactly the guarantee a ghost-canary pair is built to supply on the
model VOUCH certifies}\DIFaddend . These results are elegant but confined to
convex or near-convex regimes, and their bounds become vacuous at the scale of modern
language models; we use them not as a baseline but as a \DIFdelbegin \DIFdel{semantic anchor}\DIFdelend \DIFaddbegin \DIFadd{consistency check}\DIFaddend , showing in
Proposition~\ref{prop:bridge} that any $(\eps_u,\delta_u)$-certified algorithm passes
\DIFdelbegin \DIFdel{VOUCH}\DIFdelend \DIFaddbegin \DIFadd{VOUCH---a one-directional implication, as Remark~\ref{rem:onedirection} states}\DIFaddend .
The verification of \emph{arbitrary} unlearning has proven far harder.
\citet{thudi2022necessity} argue that approximate unlearning cannot be audited from
weights alone and that auditable definitions are a prerequisite for any guarantee;
\citet{zhang2024verification} show that a dishonest provider can defeat both existing
families of verification strategies while passing them; and, most consequentially for
our design, \citet{zhang2024mia} establish that membership-inference scores
\emph{cannot} prove that a model was trained on given data unless the inclusion of
that data was randomised---the soundness principle that our fair-coin canary design
is built to satisfy. Two further impossibility results bound what any behavioural
certificate can claim: \citet{yu2025impossibility} show that path-dependence makes
retrain equivalence unattainable for multi-stage pipelines, which is why VOUCH's null
is internally generated rather than referenced to a retrained model, and the
reversibility findings above imply that no behavioural method can certify
parameter-space erasure---a limit we state explicitly rather than paper over.

A handful of recent efforts move toward statistical certification, and these are our
closest neighbours. \citet{pandey2025gaussian} give a hypothesis-testing notion of
certified unlearning, but it is fixed-sample and tied to a Gaussian high-dimensional
regression regime rather than to LLM-scale behavioural auditing.
\citet{pawelczyk2025machine} use data-poisoning as a lens on unlearning failure but
provide no error-controlled certificate. Cryptographic proof-of-unlearning
\citep{eisenhofer2025verifiable} attests that the correct computation \emph{ran},
which is orthogonal to whether forgetting \emph{holds} statistically and with which
VOUCH \DIFdelbegin \DIFdel{composes}\DIFdelend \DIFaddbegin \DIFadd{composes---and which is the right tool against the special-casing provider our
threat model excludes}\DIFaddend . The recent survey of \citet{xue2026survey} organises this
fast-moving area and confirms that no existing method delivers what VOUCH does: an
anytime-valid, distribution-free certificate of bounded residual influence. The
broader programme is surveyed by \citet{liu2025rethinking}.

\subsection{Game-theoretic statistics}

The inferential engine we borrow is that of safe, anytime-valid inference: e-values,
test martingales, and betting confidence sequences
\citep{ramdas2023game,grunwald2024safe,shafer2021testing,waudbysmith2024,howard2021time}.
The defining property, traceable to Ville's inequality \citep{ville1939}, is that a
nonnegative martingale started at one exceeds $1/\alpha$ with probability at most
$\alpha$ \emph{at every stopping time simultaneously}; this is precisely what a
streaming, continuously-monitored deletion audit requires, and precisely what a
fixed-sample test cannot offer. The betting perspective
\citep{waudbysmith2024,orabona2016coin} additionally supplies confidence sequences
whose bets adapt online to near-optimal growth, \DIFdelbegin \DIFdel{which we use both for the certificate and for its quantitative confidence-sequence face}\DIFdelend \DIFaddbegin \DIFadd{and---the ingredient our corrected
certificate turns on---the without-replacement construction for finite-population
means, in which the null boundary is recentred at each step on the mean the unsampled
remainder would need to carry. That device is what lets Theorem~\ref{thm:cert} dispense
with independence across pairs entirely}\DIFaddend . The one adjacent application of these tools
is to privacy auditing: \citet{steinke2023privacy} audit differential
privacy in a single training run, and \citet{gonzalez2025sequential} and
\citet{csillag2026dpevalues} bring sequential e-value machinery to DP auditing. The
distinction is fundamental to the hypothesis structure. Privacy auditing
\emph{lower}-bounds the privacy loss of a \emph{training} algorithm by mounting a
successful attack; VOUCH \emph{upper}-bounds the residual influence \emph{after
unlearning} by showing that no attack in a declared class succeeds---an equivalence
test, not a significance test, run over ghost twins that have passed through the
unlearning pipeline. To our knowledge, VOUCH is the first application of
game-theoretic statistics to unlearning certification.

\subsection{Positioning}

The contrast with our nearest neighbours can be summarised sharply. Fixed-sample
paired or two-sample tests---including the KS-based forget-quality metric of
\citet{maini2024tofu} and the min-$k\%$ leakage metric of \citet{shi2024muse}---are
invalidated by optional stopping and typically require a retrained reference;
\DIFdelbegin \DIFdel{the
}\DIFdelend \DIFaddbegin \DIFadd{Section~\ref{sec:exp-descriptive} runs both on our own models and seeds and reports
where they and VOUCH disagree. The }\DIFaddend sound-membership results of \citet{zhang2024mia}
tell us \emph{why} controlled randomness is indispensable but stop at a fixed-$n$
membership proof rather than a sequential forgetting certificate; \DIFaddbegin \DIFadd{the secret-sharer
and forgetting-measurement lines \mbox{%DIFAUXCMD
\citep{carlini2019secret,jagielski2023measuring}
}\hskip0pt%DIFAUXCMD
supply the canary device and the decay measurements but no null and no error control;
}\DIFaddend shadow-model evaluators \citep{hayes2024inexact} are descriptive and cost dozens of
fine-tunes; certified removal \citep{guo2020certified,sekhari2021remember} does not
scale; and DP auditing \citep{steinke2023privacy,gonzalez2025sequential} solves the
opposite, lower-bounding, problem. VOUCH is, to our knowledge, the first framework to
formulate unlearning certification as sequential equivalence testing and to deliver
exact, finite-sample, anytime-valid certificates of bounded residual influence at LLM
scale, with soundness inherited from a randomised paired-canary design rather than
from distributional assumptions or shadow models.


\section{Setting and the VOUCH protocol}\label{sec:framework}
\DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend This section develops the protocol in full: the actors and threat model, the canary
construction that manufactures the null, the \DIFaddbegin \DIFadd{filtration that makes the martingale
argument literally correct, the }\DIFaddend certified quantity, the betting engine with its
explicit wealth dynamics, and finally the assembled protocol as pseudocode
(Algorithm~\ref{alg:vouch}) and as a flowchart (Figure~\ref{fig:flowchart}).

\subsection{Actors, pipeline, and threat model}\label{sec:setting}

The provider fine-tunes a base model on
$D_{\text{keep}} \cup D_{\text{forget}} \cup C$, where $C$ is the canary set described
below. When deletion requests arrive, an unlearning algorithm $\Ucal$---negative
preference optimisation, GradDiff, and so on---produces an unlearned model $M_u$ from
the fine-tuned model, using the forget set augmented with the relevant canaries. A
verifier, holding only black-box query access to $M_u$ together with a canary manifest
whose \DIFdelbegin \DIFdel{hash }\DIFdelend \DIFaddbegin \DIFadd{commitment }\DIFaddend was published before unlearning began, runs VOUCH and either issues,
withholds, or revokes a certificate.
\DIFaddbegin 

\DIFaddend Throughout we assume the \DIFdelbegin \DIFdel{honest-but-verifiable
}\DIFdelend \DIFaddbegin \emph{\DIFadd{honest-but-verifiable}} \DIFaddend setting: the provider genuinely
executes $\Ucal$ \DIFaddbegin \DIFadd{on the forget set as declared, }\DIFaddend and the open question is whether it
worked and how well, under quantified error control. \DIFaddbegin \DIFadd{This assumption is doing real
work and we state it wherever the certification claim is summarised, because the
commitment prevents the manifest from being changed after the fact but does not
prevent a provider who has }\emph{\DIFadd{located}} \DIFadd{the cohort from treating it differently
from organic data. Section~\ref{sec:exp-detect} measures how hard locating the cohort
actually is---the answer is that it is cheap---and explains why
locating it is nonetheless not the same as defeating the audit: both twins in a pair
are equally conspicuous, so a provider who finds the cohort still learns nothing
about which twin the coin admitted. }\DIFaddend A provider willing to special-case the committed
canaries is outside our scope and, as prior work establishes, can only be addressed
by cryptographic attestation \DIFaddbegin \DIFadd{\mbox{%DIFAUXCMD
\citep{eisenhofer2025verifiable}}\hskip0pt%DIFAUXCMD
}\DIFaddend , with which VOUCH
composes.

\subsection{Paired ghost canaries}\label{sec:canaries}

A templated generator emits exchangeable twin pairs
$(c_i^0, c_i^1)$ for $i = 1,\dots,m$. The two twins in a pair are independent draws from
the same conditional template distribution---the same surface form populated with
independently sampled fictitious entities carrying secrets of at least \DIFdelbegin \DIFdel{forty }\DIFdelend \DIFaddbegin \DIFadd{$40$ }\DIFaddend bits of
entropy---so that within a pair they are exchangeable by construction. We match the
template to the host corpus, using question--answer pairs for TOFU and record-style
sentences for news text\DIFdelbegin \DIFdel{, so that the canaries do not stand out}\DIFdelend . For each pair an
independent fair coin $b_i \sim \Bern(\tfrac12)$ selects the \emph{in-twin}
$c_i^{b_i}$, which we insert into
the fine-tuning corpus with a repetition factor $r_i \in \{1,2,4,8\}$ (these strata give
us a dose--response curve) and later append to the forget set, so that it traverses the
unlearning pipeline exactly as organic forget data does. The \emph{ghost twin}
$c_i^{1-b_i}$ is never shown to any model.
\DIFaddbegin 

\subsection{\DIFadd{Commitment and filtration}}\label{sec:filtration}

\DIFaddend Before unlearning begins the provider publishes \DIFdelbegin \DIFdel{the commitment
}\DIFdelend \DIFaddbegin \DIFadd{a commitment to the manifest. The
commitment is a Merkle tree whose $i$-th leaf is
}\DIFaddend \begin{equation}\label{eq:commit}
\DIFdelbegin \DIFdel{\mathrm{commit} }\DIFdelend \DIFaddbegin \DIFadd{\ell_i }\DIFaddend \;=\; H\bigl(\DIFdelbegin \DIFdel{\{(}\DIFdelend c_i^0,\, c_i^1,\, b_i,\, r_i\DIFdelbegin \DIFdel{)\}_{i=1}^{m}}\DIFdelend \DIFaddbegin \DIFadd{,\, \rho_i}\DIFaddend \bigr),
\qquad H = \text{SHA-256},
\end{equation}
\DIFdelbegin \DIFdel{which }\DIFdelend \DIFaddbegin \DIFadd{with $\rho_i$ a per-leaf blinding nonce derived from the manifest seed, and the
published value is the tree root. This }\DIFaddend binds the pairs and the coins and prevents the
audit from being gamed after the fact\DIFdelbegin \DIFdel{: at verification time the manifest is revealed and
checked against \eqref{eq:commit} before a single query is
made}\DIFdelend \DIFaddbegin \DIFadd{, exactly as a flat hash of the whole manifest
would; the tree structure buys one further thing that a flat hash does not, and the
theory needs it. At step $t$ the provider hands over leaf $\pi(t)$ together with its
authentication path, which the verifier checks against the published root; the path is
$\lceil\log_2 m\rceil$ hashes long, so per-pair opening costs $9$ hashes at
$m = 384$. The blinding nonce matters because the template library is public: without
it, an unopened leaf's hash could be attacked by enumeration.
}

\DIFadd{The verifier draws a uniformly random reveal order $\pi$, committed in advance, and
opens leaf $\pi(t)$ at step $t$, checking its Merkle path against the published root.
Where $\pi$'s randomness comes from is a protocol requirement rather than an
implementation choice, because Theorem~\ref{thm:cert} spends it: it must be the
verifier's own, drawn after the commitment and unpredictable to the provider
(Remark~\ref{rem:order}).
The filtration is
}\begin{equation}\DIFadd{\label{eq:filtration}
\F_t \;=\; \sigma\Bigl(\text{query access to } M_u,\;
\bigl\{(c_{\pi(u)}^0, c_{\pi(u)}^1, b_{\pi(u)}, r_{\pi(u)})\bigr\}_{u \le t},\;
\text{tie coins } 1..t \Bigr).
}\end{equation}
\DIFadd{Opening leaf by leaf rather than all at once is what makes $b_{\pi(t)}$ genuinely
unrevealed at time $t-1$, and hence what makes ``the coin is still fair given the
past'' a true statement about $\F_{t-1}$ rather than an informal one. Every
supermartingale argument in Section~\ref{sec:theory} is with respect to
\eqref{eq:filtration}. Nothing else about the protocol changes, and the verifier's
computational cost is unaffected}\DIFaddend .

\subsection{Scores and the certified quantity}\label{sec:scores}

We fix a declared class $\F$ of scalar score \DIFdelbegin \DIFdel{functions---the }\DIFdelend \DIFaddbegin \DIFadd{functions, each aggregated over $Q$ fixed
query prompts, and each oriented so that a }\emph{\DIFadd{higher}} \DIFadd{score means }\emph{\DIFadd{more
memorised}}\DIFadd{:
}\begin{itemize}
\item \DIFadd{$s_{\mathrm{loss}}$, the }\DIFaddend token-normalised \DIFdelbegin \DIFdel{negative log-likelihood }\DIFdelend \DIFaddbegin \emph{\DIFadd{log-likelihood}} \DIFaddend of the secret
      \DIFdelbegin \DIFdel{span, the min-}\DIFdelend \DIFaddbegin \DIFadd{span---that is, the negative of its token-normalised negative log-likelihood;
}\item \DIFadd{$s_{\mathrm{mink}}$, the mean of the lowest }\DIFaddend $k\%$ token \DIFdelbegin \DIFdel{log-probability}\DIFdelend \DIFaddbegin \DIFadd{log-probabilities of the
      span ($k = 20$);
}\item \DIFadd{$s_{\mathrm{ratio}}$}\DIFaddend , the base-model-calibrated \DIFdelbegin \DIFdel{loss ratio, and
optionally }\DIFdelend \DIFaddbegin \DIFadd{difference
      $s_{\mathrm{loss}}(M_u,\cdot) - s_{\mathrm{loss}}(M_0,\cdot)$, available when the
      verifier may query the pristine base model;
}\item \DIFadd{optionally $s_{\mathrm{probe}}$, }\DIFaddend a linear probe on the hidden \DIFdelbegin \DIFdel{state---each aggregated over $Q$ fixed query
prompts}\DIFdelend \DIFaddbegin \DIFadd{state, calibrated
      on a disjoint cohort.
}\end{itemize}
\DIFadd{The sign convention matters and we fix it once here, since a log-likelihood and a
negative log-likelihood differ by exactly the sign of every reported gap}\DIFaddend . For pair $i$ and
score $s$ we form the within-pair difference and its sign,
\begin{equation}\label{eq:sign}
D_i^{(s)} = s(M_u, c_i^{\text{in}}) - s(M_u, c_i^{\text{ghost}}), \qquad
Z_i^{(s)} = \mathbf{1}\{D_i^{(s)} > 0\},
\end{equation}
breaking ties by a committed fair coin. \DIFdelbegin \DIFdel{Writing $p^{(s)} = \Pr[Z^{(s)} = 1]$ and $\Delta^{(s)} = 2p^{(s)} - 1$, the }\DIFdelend \DIFaddbegin \DIFadd{With scores oriented as above, a
}\emph{\DIFadd{positive}} \DIFadd{$D$ means the trained twin is the better-remembered one, which is the
direction of leakage; the worked example of Figure~\ref{fig:example} reports
$D = +8.0$ for a fine-tuned model whose in-secret costs $0.99$ nats per token against
the ghost's $9.03$, and this is now consistent with \eqref{eq:sign}, with the figure,
with the tables, and with the code. Exact ties are rare but not always negligible: we
measure a rate of $0$ in $124{,}416$ scored pairs for $s_{\mathrm{loss}}$ across every
architecture, and $2.7\%$ for $s_{\mathrm{mink}}$ on Gemma-4-E2B, where the min-$k\%$
statistic over a short span takes few distinct values
(Section~\ref{sec:exp-bench}). The committed tie-break coin is what keeps the null
exact in that cell.
}

\DIFadd{The }\DIFaddend quantity we certify is $\sup_{s\in\F}|\Delta^{(s)}|$ \DIFdelbegin \DIFdel{,
the
largest residual advantage any score in the class can extract}\DIFdelend \DIFaddbegin \DIFadd{with $\Delta^{(s)}$ the
realised cohort advantage of Definition~\ref{def:delta}}\DIFaddend . We are deliberate that this
is influence extractable \emph{through $\F$} on the \DIFdelbegin \DIFdel{canary population}\DIFdelend \DIFaddbegin \emph{\DIFadd{committed canary cohort}}\DIFaddend : it
is the strongest target compatible with the impossibility results for behavioural
audits, and we make no claim about erasure in parameter space\DIFdelbegin \DIFdel{. The certificate itself is worth
pinning down precisely, since everything that follows is engineered to earn it.
}%DIFDELCMD < 

%DIFDELCMD < \begin{definition}[$(\eps,\alpha)$-forgetting certificate]\label{def:cert}
%DIFDELCMD < %%%
\DIFdel{Fix a tolerance $\eps \in (0,1)$, an error budget $\alpha \in (0,1)$, and the boundary
$p_0 = \tfrac12 + \tfrac\eps2$. An $(\eps,\alpha)$-forgetting certificate for $M_u$
over the class }\DIFdelend \DIFaddbegin \DIFadd{, about scores outside
}\DIFaddend $\F$\DIFdelbegin \DIFdel{is a data-dependent stopping decision that asserts
$\sup_{s\in\F}|\Delta^{(s)}| < \eps$, issued by a procedure with the guarantee that,
whenever in truth $\sup_{s\in\F}|\Delta^{(s)}| \ge \eps$, the probability that a
certificate is }\emph{\DIFdel{ever}} %DIFAUXCMD
\DIFdel{issued---at any stopping time, under any monitoring rule---is
at most $\alpha$.
}%DIFDELCMD < \end{definition}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{The two-sidedness in Definition~\ref{def:cert} is deliberate: $\Delta^{(s)} \le -\eps$,
the over-forgetting regime in which the in-twin scores conspicuously }\emph{\DIFdel{below}} %DIFAUXCMD
\DIFdel{its
never-seen partner, is membership leakage exactly as much as $\Delta^{(s)} \ge \eps$
is, and Section~\ref{sec:exp} shows that gradient-ascent unlearning lands there in
practice}\DIFdelend \DIFaddbegin \DIFadd{, or---without the explicit inflation of Proposition~\ref{prop:transfer}---about
the super-population advantage}\DIFaddend .

\subsection{The betting engine}\label{sec:engine}

All three of VOUCH's outputs are built from one primitive, the \emph{betting
e-process}, and it is worth writing its dynamics out explicitly because the validity
proofs in Section~\ref{sec:theory} reduce to inspecting them. A gambler starts with
unit wealth and, before \DIFdelbegin \DIFdel{pair $i$ }\DIFdelend \DIFaddbegin \DIFadd{the pair opened at step $t$ }\DIFaddend is revealed, stakes a predictable
fraction \DIFdelbegin \DIFdel{$\lambda_i$ }\DIFdelend \DIFaddbegin \DIFadd{$\lambda_t$ }\DIFaddend (chosen from the information \DIFdelbegin \DIFdel{$\F_{i-1}$ }\DIFdelend \DIFaddbegin \DIFadd{$\F_{t-1}$ }\DIFaddend revealed so far) on the
sign \DIFdelbegin \DIFdel{$Z_i$ }\DIFdelend \DIFaddbegin \DIFadd{$Z_{\pi(t)}$ }\DIFaddend deviating from a hypothesised mean.
\DIFdelbegin \DIFdel{Against the null $H_0^{+}\colon
\Efrak[Z] \ge p_0$ (``the in-twins still score high'') }\DIFdelend \DIFaddbegin 

\DIFadd{For the }\emph{\DIFadd{certificate}} \DIFadd{arm the hypothesised mean is not a constant but the
predictable without-replacement boundary $p_0(t)$ of Eq.~\eqref{eq:worboundary}: the
mean the unrevealed remainder of the cohort would have to carry for the null
$\Delta^{(s)} \ge \eps$ still to hold. Against that boundary }\DIFaddend and its mirror \DIFdelbegin \DIFdel{$H_0^{-}\colon \Efrak[Z] \le 1-p_0$, }\DIFdelend the wealth
processes for score $s$ are
\begin{equation}\DIFdelbegin %DIFDELCMD < \label{eq:wealthwor}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \label{eq:wealthwor}
\DIFaddend E_t^{s,+} \DIFdelbegin \DIFdel{\;}\DIFdelend = \DIFdelbegin \DIFdel{\; }\DIFdelend \prod\DIFdelbegin \DIFdel{_{i \le t}}\DIFdelend \DIFaddbegin \DIFadd{_{u \le t}}\DIFaddend \bigl(1 + \lambda\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_u}\DIFaddend ^{s,+}\,(p_0\DIFaddbegin \DIFadd{(u) }\DIFaddend - Z\DIFdelbegin \DIFdel{_i}\DIFdelend ^{(s)}\DIFaddbegin \DIFadd{_{\pi(u)}}\DIFaddend )\bigr),
\qquad
E_t^{s,-} \DIFdelbegin \DIFdel{\;}\DIFdelend = \DIFdelbegin \DIFdel{\; }\DIFdelend \prod\DIFdelbegin \DIFdel{_{i \le t}}\DIFdelend \DIFaddbegin \DIFadd{_{u \le t}}\DIFaddend \bigl(1 + \lambda\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_u}\DIFaddend ^{s,-}\,(Z\DIFdelbegin \DIFdel{_i}\DIFdelend ^{(s)}\DIFaddbegin \DIFadd{_{\pi(u)} }\DIFaddend - \DIFdelbegin \DIFdel{(1 - }\DIFdelend \DIFaddbegin \bar \DIFaddend p_0\DIFaddbegin \DIFadd{(u}\DIFaddend ))\bigr),
\end{equation}
with \DIFaddbegin \DIFadd{$\bar p_0(t)$ the analogous boundary for the null $\Delta^{(s)} \le -\eps$ and
}\DIFaddend stakes confined to \DIFdelbegin \DIFdel{$\lambda_i^{s,\pm} \in [0,\, 1/(1-p_0))$ }\DIFdelend \DIFaddbegin \DIFadd{the admissible range }\DIFaddend so that wealth can never go negative. \DIFdelbegin \DIFdel{Under any distribution in the respective }\DIFdelend \DIFaddbegin \DIFadd{Because
sequential reveal of a uniform permutation is simple random sampling without
replacement, the conditional mean of the next sign is exactly the mean of the
remainder, so under the }\DIFaddend null each factor has conditional mean at most one\DIFdelbegin \DIFdel{, so }\DIFdelend \DIFaddbegin \DIFadd{: }\DIFaddend each
process is a nonnegative supermartingale \DIFdelbegin \DIFdel{with
initial value }\DIFdelend \DIFaddbegin \DIFadd{started at }\DIFaddend one, and Ville's inequality
converts wealth into evidence\DIFdelbegin \DIFdel{: the chance
that a supermartingale ever exceeds $1/\alpha$ is at most $\alpha$}\DIFdelend . Wealth above $1/\alpha$ is \DIFdelbegin \DIFdel{therefore }\DIFdelend a licence to reject \DIFdelbegin \DIFdel{that null }\DIFdelend at
whatever time we happen to be \DIFdelbegin \DIFdel{looking---the anytime validity on which the whole framework rests}\DIFdelend \DIFaddbegin \DIFadd{looking. Setting $p_0(t) \equiv p_0$ instead recovers
the fixed-boundary process of the earlier draft, which targets the super-population
advantage and requires the conditional-mean null; we retain it as a reported variant
and compare the two throughout Section~\ref{sec:exp}.
}

\DIFadd{For the }\emph{\DIFadd{revocation}} \DIFadd{arm the hypothesised mean is the constant $\tfrac12$, which
Theorem~\ref{thm:null} makes exact conditionally, so no boundary recentring is needed}\DIFaddend .

How the stakes are chosen determines power, not validity, which frees us to be
aggressive. VOUCH plays a uniform mixture of \DIFdelbegin \DIFdel{eight }\DIFdelend \DIFaddbegin \DIFadd{$8$ }\DIFaddend experts and lets their wealth
arbitrate: \DIFdelbegin \DIFdel{six }\DIFdelend \DIFaddbegin \DIFadd{$6$ }\DIFaddend constant-fraction bettors $\lambda = c \cdot \lambda_{\max}$ with
$c \in \{0.02, 0.05, 0.1, 0.2, 0.4, 0.7\}$, an online-Newton-step (ONS) learner, and a
truncated Krichevsky--Trofimov (KT) plug-in. The ONS expert follows the coin-betting
recursion
\begin{equation}\label{eq:ons}
g\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t }\DIFaddend = \DIFdelbegin \DIFdel{\frac{p_0 - Z_i}{1 + \lambda_i (p_0 - Z_i)}}\DIFdelend \DIFaddbegin \DIFadd{\frac{p_0(t) - Z_t}{1 + \lambda_t (p_0(t) - Z_t)}}\DIFaddend , \qquad
A\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t }\DIFaddend = A\DIFdelbegin \DIFdel{_{i-1} }\DIFdelend \DIFaddbegin \DIFadd{_{t-1} }\DIFaddend + g\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_t}\DIFaddend ^2, \qquad
\lambda\DIFdelbegin \DIFdel{_{i+1} }\DIFdelend \DIFaddbegin \DIFadd{_{t+1} }\DIFaddend = \Pi_{[0,\lambda_{\max}]}\!\Bigl(\lambda\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t }\DIFaddend + \DIFdelbegin %DIFDELCMD < \tfrac{\eta\, g_i}{A_i}%%%
\DIFdelend \DIFaddbegin \tfrac{\eta\, g_t}{A_t}\DIFaddend \Bigr),
\end{equation}
with $A_0 = 1$, $\eta = \tfrac12$, and $\Pi$ the clip onto the admissible range; its
log-wealth regret against the best constant bet in hindsight is $O(\log t)$. The KT
expert bypasses stake selection altogether: it estimates the mean by
\DIFdelbegin \DIFdel{$q_i = (\sum_{j<i} Z_j + \tfrac12)/i$, truncates $q_i$ }\DIFdelend \DIFaddbegin \DIFadd{$q_t = (\sum_{u<t} Z_u + \tfrac12)/t$, truncates $q_t$ }\DIFaddend to the alternative side of
\DIFdelbegin \DIFdel{$p_0$}\DIFdelend \DIFaddbegin \DIFadd{$p_0(t)$}\DIFaddend , and multiplies wealth by the plug-in likelihood ratio
\begin{equation}\label{eq:kt}
e\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t }\DIFaddend \;=\; Z\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_t}\DIFaddend \,\DIFdelbegin \DIFdel{\frac{q_i}{p_0} }\DIFdelend \DIFaddbegin \DIFadd{\frac{q_t}{p_0(t)} }\DIFaddend \;+\; (1 - Z\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_t}\DIFaddend )\,\DIFdelbegin \DIFdel{\frac{1 - q_i}{1 - p_0}}\DIFdelend \DIFaddbegin \DIFadd{\frac{1 - q_t}{1 - p_0(t)}}\DIFaddend ,
\end{equation}
which has conditional mean at most one under the null \DIFdelbegin \DIFdel{(it is linear in the mean, equals
one at $p_0$, and decreases into the null) }\DIFdelend and achieves the optimal growth
rate up to a $\tfrac1{2t}\log t$ redundancy. Because a convex combination of
e-processes with a fixed prior is again an e-process, the mixture inherits exact
validity while paying only a $\log 8$ additive cost in log-wealth relative to its best
expert---power adapts online with nothing to tune.

The same wealth idea, run over a grid rather than at a single boundary, yields the
quantitative face of the certificate. For every candidate mean \DIFdelbegin \DIFdel{$m$ }\DIFdelend \DIFaddbegin \DIFadd{$m'$ }\DIFaddend on a grid of
$1001$ points in $[0,1]$ we maintain two wealth processes betting that the truth is
above and below \DIFdelbegin \DIFdel{$m$}\DIFdelend \DIFaddbegin \DIFadd{$m'$, each recentred by the same without-replacement rule}\DIFaddend , with
empirical-Bernstein (aGRAPA) stakes
\begin{equation}\label{eq:agrapa}
\lambda\DIFdelbegin \DIFdel{_i}\DIFdelend \DIFaddbegin \DIFadd{_t}\DIFaddend (m\DIFaddbegin \DIFadd{'}\DIFaddend ) \;=\; \Pi\DIFdelbegin \DIFdel{_{[0,\,\lambda_{\max}(m)]}
}\DIFdelend \DIFaddbegin \DIFadd{_{[0,\,\lambda_{\max}]}
}\DIFaddend \Bigl(\DIFdelbegin %DIFDELCMD < \tfrac{\hat\mu_{i-1} - m}{\hat\sigma_{i-1}^2 + (\hat\mu_{i-1} - m)^2}%%%
\DIFdelend \DIFaddbegin \tfrac{\hat\mu_{t-1} - m'}{\hat\sigma_{t-1}^2 + (\hat\mu_{t-1} - m')^2}\DIFaddend \Bigr),
\end{equation}
where \DIFdelbegin \DIFdel{$\hat\mu_{i-1}$ and $\hat\sigma^2_{i-1}$ }\DIFdelend \DIFaddbegin \DIFadd{$\hat\mu_{t-1}$ and $\hat\sigma^2_{t-1}$ }\DIFaddend are the running mean and variance of the
signs. The set of grid values whose wealth on both sides stays below $2/\alpha$ \DIFaddbegin \DIFadd{and
whose recentred boundary remains in $[0,1]$}\DIFaddend , intersected over time,
\begin{equation}\label{eq:cs}
C_t \;=\; \bigcap_{u \le t}\,\bigl\{\, m\DIFaddbegin \DIFadd{' }\DIFaddend :\ \max\bigl(W_u^{\uparrow}(m\DIFaddbegin \DIFadd{'}\DIFaddend ),\,
W_u^{\downarrow}(m\DIFaddbegin \DIFadd{'}\DIFaddend )\bigr) < 2/\alpha \,\bigr\},
\end{equation}
is a $(1-\alpha)$ confidence sequence for \DIFdelbegin \DIFdel{$p^{(s)}$}\DIFdelend \DIFaddbegin \DIFadd{the cohort sign rate}\DIFaddend : it covers the truth at
every time simultaneously, so the auditor may glance at it whenever they please\DIFaddbegin \DIFadd{, and
it collapses onto the exact cohort value once the cohort is exhausted}\DIFaddend . Mapped through
$\Delta = 2p - 1$ it reports the residual advantage with its anytime-valid
uncertainty\DIFaddbegin \DIFadd{; Remark~\ref{rem:cs} states precisely which coverage claims are per-score
and which are simultaneous over $\F$}\DIFaddend .

The revocation arm inverts the role of the null. Under \emph{exact} unlearning,
Theorem~\ref{thm:null} makes every \DIFdelbegin \DIFdel{$Z_i^{(s)}$ an exact }\DIFdelend \DIFaddbegin \DIFadd{$Z_{\pi(t)}^{(s)}$ an exact conditional }\DIFaddend fair coin,
so here---and only here---adaptive score weighting is sound. We run, per score and per
direction, e-processes against $\Efrak[Z] = \tfrac12$ and combine them with predictable
wealth-proportional weights: with \DIFdelbegin \DIFdel{$w_{s,i} \propto \exp(\log E^{s}_{i-1})$ computed
before pair $i$ is revealed,
}\DIFdelend \DIFaddbegin \DIFadd{$w_{s,t} \propto \exp(\log E^{s}_{t-1})$ computed
before the pair at step $t$ is opened,
}\DIFaddend \begin{equation}\label{eq:revmix}
R_t \;=\; \prod\DIFdelbegin \DIFdel{_{i\le t}}\DIFdelend \DIFaddbegin \DIFadd{_{u\le t}}\DIFaddend \;\sum_{s\in\F} w\DIFdelbegin \DIFdel{_{s,i}}\DIFdelend \DIFaddbegin \DIFadd{_{s,u}}\DIFaddend \,
\DIFdelbegin \DIFdel{\frac{E^{s}_{i}}{E^{s}_{i-1}}}\DIFdelend \DIFaddbegin \DIFadd{\frac{E^{s}_{u}}{E^{s}_{u-1}}}\DIFaddend ,
\end{equation}
which is an e-process because each factor is a predictable convex combination of
conditional-mean-one increments. When $R_t \ge 1/\alpha$ the verifier holds
anytime-valid evidence of residual memorisation and the certificate is withheld or
revoked; the weights meanwhile steer wealth toward whichever score discriminates best,
which is exactly what an alarm should do.

\subsection{The protocol end to end}\label{sec:protocol}

Algorithm~\ref{alg:vouch} assembles the pieces into the three-phase protocol, and
Figure~\ref{fig:flowchart} shows the same pipeline as a flowchart. Phase~0 runs at
design time: generate the cohort, flip the coins, publish the \DIFdelbegin \DIFdel{commitment}\DIFdelend \DIFaddbegin \DIFadd{Merkle root}\DIFaddend , fine-tune
with the in-twins planted. Phase~1 is the provider's unlearning step, in which the
in-twins ride along with the organic forget set. Phase~2 is the audit: \DIFdelbegin \DIFdel{reveal and check
the manifest, then }\DIFdelend stream the
pairs in \DIFaddbegin \DIFadd{the committed }\DIFaddend random order, \DIFaddbegin \DIFadd{opening and checking one leaf at a time, }\DIFaddend updating
the certificate processes, the confidence sequences, and the revocation mixture after
each pair, and \DIFdelbegin \DIFdel{act }\DIFdelend \DIFaddbegin \DIFadd{acting }\DIFaddend on whichever boundary is crossed first. R-VOUCH then re-runs the
same loop on probed variants of $M_u$---after relearning on public data, after 4-bit
quantisation, and under adversarial prompt wrappers---and the robust certificate
requires every probe to pass. \DIFaddbegin \DIFadd{Our empirical probe coverage is partial and we state it
here rather than leave it to be inferred: P1 and P3 run on every benchmark cell and
every seed but are anchored on NPO; P2 runs on the GPT-2 and TinyGPT tiers only,
because the shared-frozen-base multi-adapter design would have to materialise merged
weights to quantise a multi-billion-parameter model. }\DIFaddend Streaming deletion waves each draw a fresh cohort with
independent coins; Theorem~\ref{thm:streaming} dictates how their verdicts may be
combined.


\begin{algorithm}[t]
\caption{The VOUCH protocol}\label{alg:vouch}
\DIFdelbegin %DIFDELCMD < \begin{algorithmic}[1]
%DIFDELCMD < \Statex \textbf{Phase 0 (design time, before fine-tuning)}
%DIFDELCMD < \State generate twin pairs $\{(c_i^0, c_i^1)\}_{i=1}^m$; draw coins
%DIFDELCMD <        $b_i \sim \Bern(\tfrac12)$ and strata $r_i \in \{1,2,4,8\}$
%DIFDELCMD < \State $\textsc{In} \gets \{c_i^{b_i}\}$;\quad $\textsc{Ghost} \gets \{c_i^{1-b_i}\}$
%DIFDELCMD <        \Comment{ghosts are never shown to any model}
%DIFDELCMD < \State publish $\mathrm{commit} = H(\{(c_i^0, c_i^1, b_i, r_i)\})$
%DIFDELCMD <        \Comment{SHA-256, Eq.~\eqref{eq:commit}}
%DIFDELCMD < \State fine-tune $M_{\mathrm{ft}}$ on $D_{\text{keep}} \cup D_{\text{forget}} \cup
%DIFDELCMD <        (\textsc{In} \text{ with repetitions } r_i)$
%DIFDELCMD < \Statex \textbf{Phase 1 (unlearning)}
%DIFDELCMD < \State $M_u \gets \Ucal(M_{\mathrm{ft}};\ D_{\text{forget}} \cup \textsc{In})$
%DIFDELCMD <        \Comment{in-twins traverse the pipeline as organic forget data}
%DIFDELCMD < \Statex \textbf{Phase 2 (certification; black-box queries only)}
%DIFDELCMD < \State reveal manifest; \textbf{abort} unless $H(\text{manifest}) = \mathrm{commit}$
%DIFDELCMD < \State initialise $E^{s,+} = E^{s,-} = 1$ for all $s \in \F$; revocation mixture
%DIFDELCMD <        $R = 1$; CS grids $C^{(s)}$
%DIFDELCMD < \For{$i$ in a committed random order of $1,\dots,m$}
%DIFDELCMD <   \For{$s \in \F$}
%DIFDELCMD <      \State $D_i^{(s)} \gets s(M_u, c_i^{\text{in}}) - s(M_u, c_i^{\text{ghost}})$;
%DIFDELCMD <             \quad $Z_i^{(s)} \gets \mathbf{1}\{D_i^{(s)} > 0\}$ (ties: committed coin)
%DIFDELCMD <      \State update $E^{s,+}, E^{s,-}$ by Eq.~\eqref{eq:wealthwor} with mixture bets
%DIFDELCMD <             \eqref{eq:ons}--\eqref{eq:kt}; update $C^{(s)}$ by
%DIFDELCMD <             \eqref{eq:agrapa}--\eqref{eq:cs}
%DIFDELCMD <   \EndFor
%DIFDELCMD <   \State update the revocation mixture $R$ by Eq.~\eqref{eq:revmix}
%DIFDELCMD <   \If{$R \ge 1/\alpha$} \textbf{revoke}; \Return
%DIFDELCMD <         $\textsc{Fail}(\text{evidence} = R)$
%DIFDELCMD <   \EndIf
%DIFDELCMD <   \If{$\min_{s,\pm} E^{s,\pm} \ge 1/\alpha$} \textbf{break}
%DIFDELCMD <         \Comment{certificate earned early}
%DIFDELCMD <   \EndIf
%DIFDELCMD < \EndFor
%DIFDELCMD < \State run R-VOUCH probes P1 (relearn), P2 (quantise), P3 (jailbreak); repeat the
%DIFDELCMD <        loop above per probe
%DIFDELCMD < \If{$\min_{s,\pm} E^{s,\pm} \ge 1/\alpha$ \textbf{and} every probe passes}
%DIFDELCMD <    \State \textbf{issue} $\mathrm{cert} = (\eps, \alpha, t, C_t, \mathrm{commit},
%DIFDELCMD <           \F, \text{probe report})$
%DIFDELCMD < \Else
%DIFDELCMD <    \State \textbf{withhold} \Comment{undetermined: report $C_t$ and continue
%DIFDELCMD <           monitoring}
%DIFDELCMD < \EndIf
%DIFDELCMD < \Statex \textbf{Streaming (waves $k = 1, 2, \dots$)}: fresh cohort and coins per wave;
%DIFDELCMD <         global certificate $=$ all-pass; global alarm $=$ $\prod_k R^{(k)}$; per-wave
%DIFDELCMD <         alarms at $\alpha_k = \alpha 2^{-(k+1)}$ \Comment{Theorem~\ref{thm:streaming}}
%DIFDELCMD < \end{algorithmic}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \begin{algorithmic}[1]
\Statex \textbf{Phase 0 (design time, before fine-tuning)}
\State generate twin pairs $\{(c_i^0, c_i^1)\}_{i=1}^m$; draw coins
       $b_i \sim \Bern(\tfrac12)$ and strata $r_i \in \{1,2,4,8\}$
\State $\textsc{In} \gets \{c_i^{b_i}\}$;\quad $\textsc{Ghost} \gets \{c_i^{1-b_i}\}$
       \Comment{ghosts are never shown to any model}
\State publish the Merkle root over leaves
       $\ell_i = H(c_i^0, c_i^1, b_i, r_i, \rho_i)$
       \Comment{Eq.~\eqref{eq:commit}}
\State fine-tune $M_{\mathrm{ft}}$ on $D_{\text{keep}} \cup D_{\text{forget}} \cup
       (\textsc{In} \text{ with repetitions } r_i)$
\Statex \textbf{Phase 1 (unlearning)}
\State $M_u \gets \Ucal(M_{\mathrm{ft}};\ D_{\text{forget}} \cup \textsc{In})$
       \Comment{in-twins traverse the pipeline as organic forget data}
\Statex \textbf{Phase 2 (certification; black-box queries only)}
\State initialise $E^{s,+} = E^{s,-} = 1$ for all $s \in \F$; revocation mixture
       $R = 1$; CS grids $C^{(s)}$
\For{$t = 1,\dots,m$ in the committed random order $\pi$}
  \State open leaf $\ell_{\pi(t)}$; \textbf{abort} unless its Merkle path checks
         against the published root
  \For{$s \in \F$}
     \State $D^{(s)} \gets s(M_u, c^{\text{in}}_{\pi(t)}) -
            s(M_u, c^{\text{ghost}}_{\pi(t)})$;\quad
            $Z^{(s)} \gets \mathbf{1}\{D^{(s)} > 0\}$ (ties: committed coin)
     \State recentre the boundary $p_0(t)$ by Eq.~\eqref{eq:worboundary}; update
            $E^{s,+}, E^{s,-}$ by Eq.~\eqref{eq:wealthwor} with mixture bets
            \eqref{eq:ons}--\eqref{eq:kt}; update $C^{(s)}$ by
            \eqref{eq:agrapa}--\eqref{eq:cs}
  \EndFor
  \State update the revocation mixture $R$ by Eq.~\eqref{eq:revmix}
  \If{$R \ge 1/\alpha$ \textbf{and} not yet revoked}
     \State mark \textbf{revoked} at $t$; \textbf{continue} the loop
            \Comment{keep compounding $R$ for the streaming product alarm}
  \EndIf
  \If{not revoked \textbf{and} $\min_{s,\pm} E^{s,\pm} \ge 1/\alpha$}
     \State \textbf{break} \Comment{certificate earned early}
  \EndIf
\EndFor
\State run R-VOUCH probes P1 (relearn), P2 (quantise), P3 (jailbreak); repeat the
       loop above per probe
\If{$\min_{s,\pm} E^{s,\pm} \ge 1/\alpha$ \textbf{and} every probe passes}
   \State \textbf{issue} $\mathrm{cert} = (\eps, \alpha, t, C_t, \mathrm{root},
          \F, \text{probe report})$
\Else
   \State \textbf{withhold} \Comment{undetermined: report $C_t$ and continue
          monitoring}
\EndIf
\Statex \textbf{Streaming (waves $k = 1, 2, \dots$)}: fresh cohort and coins per wave;
        global certificate $=$ all-pass; global alarm $=$ $\prod_k R^{(k)}$; per-wave
        alarms at $\alpha_k = \alpha 2^{-(k+1)}$ \Comment{Theorem~\ref{thm:streaming}}
\end{algorithmic}
\DIFaddend \end{algorithm}

\begin{figure}[t]
\centering
\DIFdelbeginFL %DIFDELCMD < \resizebox{\textwidth}{!}{%
%DIFDELCMD < \begin{tikzpicture}[
%DIFDELCMD <   font=\footnotesize,
%DIFDELCMD <   node distance=4mm and 6mm,
%DIFDELCMD <   box/.style={draw, rounded corners=2pt, align=center, inner sep=3.5pt,
%DIFDELCMD <               minimum height=7mm},
%DIFDELCMD <   data/.style={box, fill=gray!12},
%DIFDELCMD <   proc/.style={box, fill=blue!9},
%DIFDELCMD <   eng/.style={box, fill=violet!10},
%DIFDELCMD <   good/.style={box, fill=green!18},
%DIFDELCMD <   bad/.style={box, fill=red!16},
%DIFDELCMD <   wait/.style={box, fill=orange!15},
%DIFDELCMD <   arr/.style={-{Latex[length=1.8mm]}, semithick},
%DIFDELCMD <   lab/.style={font=\scriptsize\itshape, text=black!60}
%DIFDELCMD < ]
%DIFDELCMD < % Phase 0 row
%DIFDELCMD < \node[proc] (gen) {twin pairs $(c_i^0,c_i^1)$\\ strata $r_i\in\{1,2,4,8\}$};
%DIFDELCMD < \node[proc, right=of gen] (coin) {fair coins\\ $b_i\sim\Bern(\frac{1}{2})$};
%DIFDELCMD < \node[data, right=of coin] (split) {$\textsc{In}=\{c_i^{b_i}\}$\\
%DIFDELCMD <   $\textsc{Ghost}=\{c_i^{1-b_i}\}$};
%DIFDELCMD < \node[data, right=of split] (commit) {publish\\ $H(\text{manifest})$};
%DIFDELCMD < \node[proc, below=6mm of split] (ft) {fine-tune $M_{\mathrm{ft}}$ on
%DIFDELCMD <   $D_{\text{keep}}\cup D_{\text{forget}}\cup \textsc{In}$};
%DIFDELCMD < \node[proc, below=of ft] (unlearn) {unlearn: $M_u \gets \Ucal(M_{\mathrm{ft}};\,
%DIFDELCMD <   D_{\text{forget}}\cup\textsc{In})$};
%DIFDELCMD < \node[proc, below=of unlearn] (score) {reveal manifest, check hash;\\
%DIFDELCMD <   stream pairs: $D_i^{(s)},\ Z_i^{(s)}$ by Eq.~\eqref{eq:sign}};
%DIFDELCMD < % three coupled processes
%DIFDELCMD < \node[eng, below=7mm of score] (cs) {confidence sequence\\
%DIFDELCMD <   $C_t$ on each $\Delta^{(s)}$, Eq.~\eqref{eq:cs}};
%DIFDELCMD < \node[eng, left=5mm of cs] (cert) {certificate arm\\
%DIFDELCMD <   $E_t^{s,\pm}$, Eq.~\eqref{eq:wealthwor}\\ (min over $s,\pm$)};
%DIFDELCMD < \node[eng, right=5mm of cs] (rev) {revocation arm\\
%DIFDELCMD <   mixture $R_t$, Eq.~\eqref{eq:revmix}};
%DIFDELCMD < % decisions
%DIFDELCMD < \node[good, below=7mm of cert, text width=30mm] (issue)
%DIFDELCMD <   {all $E^{s,\pm}\!\ge\!\frac{1}{\alpha}$, probes pass: \textbf{issue}
%DIFDELCMD <    $(\eps,\alpha)$-cert.};
%DIFDELCMD < \node[bad, below=7mm of rev, text width=24mm] (revoke) {$R_t \ge \frac{1}{\alpha}$:\\
%DIFDELCMD <   \textbf{revoke / withhold}};
%DIFDELCMD < \node[wait, below=7mm of cs, text width=24mm] (undet) {otherwise: report $C_t$,
%DIFDELCMD <   keep streaming};
%DIFDELCMD < \node[eng, below=4mm of undet] (probes) {R-VOUCH probes: relearn, quantise,
%DIFDELCMD <   jailbreak $\to$ rerun loop};
%DIFDELCMD < % arrows
%DIFDELCMD < \draw[arr] (gen) -- (coin);
%DIFDELCMD < \draw[arr] (coin) -- (split);
%DIFDELCMD < \draw[arr] (split) -- (commit);
%DIFDELCMD < \draw[arr] (split) -- (ft);
%DIFDELCMD < \draw[arr] (ft) -- (unlearn);
%DIFDELCMD < \draw[arr] (unlearn) -- (score);
%DIFDELCMD < \draw[arr] (commit.south) to[out=-90, in=20] (score.east);
%DIFDELCMD < \draw[arr] (score) -- (cert);
%DIFDELCMD < \draw[arr] (score) -- (cs);
%DIFDELCMD < \draw[arr] (score) -- (rev);
%DIFDELCMD < \draw[arr] (cert) -- (issue);
%DIFDELCMD < \draw[arr] (rev) -- (revoke);
%DIFDELCMD < \draw[arr] (cs) -- (undet);
%DIFDELCMD < \draw[arr] (undet) -- (probes);
%DIFDELCMD < \draw[arr] (probes.west) to[out=180, in=180, looseness=1.4] (cert.west);
%DIFDELCMD < % phase labels
%DIFDELCMD < \node[lab, left=2mm of gen.west, anchor=east] {Phase 0};
%DIFDELCMD < \node[lab, left=48mm of unlearn.west, anchor=east] {Phase 1};
%DIFDELCMD < \node[lab, left=41mm of score.west, anchor=east] {Phase 2};
%DIFDELCMD < \end{tikzpicture}}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  font=\footnotesize,
  node distance=4mm and 6mm,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=3.5pt,
              minimum height=7mm},
  data/.style={box, fill=gray!12},
  proc/.style={box, fill=blue!9},
  eng/.style={box, fill=violet!10},
  good/.style={box, fill=green!18},
  bad/.style={box, fill=red!16},
  wait/.style={box, fill=orange!15},
  arr/.style={-{Latex[length=1.8mm]}, semithick},
  lab/.style={font=\scriptsize\itshape, text=black!60}
]
% Phase 0 row
\node[proc] (gen) {twin pairs $(c_i^0,c_i^1)$\\ strata $r_i\in\{1,2,4,8\}$};
\node[proc, right=of gen] (coin) {fair coins\\ $b_i\sim\Bern(\frac{1}{2})$};
\node[data, right=of coin] (split) {$\textsc{In}=\{c_i^{b_i}\}$\\
  $\textsc{Ghost}=\{c_i^{1-b_i}\}$};
\node[data, right=of split] (commit) {publish\\ Merkle root};
\node[proc, below=6mm of split] (ft) {fine-tune $M_{\mathrm{ft}}$ on
  $D_{\text{keep}}\cup D_{\text{forget}}\cup \textsc{In}$};
\node[proc, below=of ft] (unlearn) {unlearn: $M_u \gets \Ucal(M_{\mathrm{ft}};\,
  D_{\text{forget}}\cup\textsc{In})$};
\node[proc, below=of unlearn] (score) {open leaf $\pi(t)$, check path;\\
  stream pairs: $D^{(s)},\ Z^{(s)}$ by Eq.~\eqref{eq:sign}};
% three coupled processes
\node[eng, below=7mm of score] (cs) {confidence sequence\\
  $C_t$ on each $\Delta^{(s)}$, Eq.~\eqref{eq:cs}};
\node[eng, left=5mm of cs] (cert) {certificate arm\\
  $E_t^{s,\pm}$, Eq.~\eqref{eq:wealthwor}\\ (min over $s,\pm$)};
\node[eng, right=5mm of cs] (rev) {revocation arm\\
  mixture $R_t$, Eq.~\eqref{eq:revmix}};
% decisions
\node[good, below=7mm of cert, text width=30mm] (issue)
  {all $E^{s,\pm}\!\ge\!\frac{1}{\alpha}$, probes pass: \textbf{issue}
   $(\eps,\alpha)$-cert.};
\node[bad, below=7mm of rev, text width=24mm] (revoke) {$R_t \ge \frac{1}{\alpha}$:\\
  \textbf{revoke / withhold}};
\node[wait, below=7mm of cs, text width=24mm] (undet) {otherwise: report $C_t$,
  keep streaming};
\node[eng, below=4mm of undet] (probes) {R-VOUCH probes: relearn, quantise,
  jailbreak $\to$ rerun loop};
% arrows
\draw[arr] (gen) -- (coin);
\draw[arr] (coin) -- (split);
\draw[arr] (split) -- (commit);
\draw[arr] (split) -- (ft);
\draw[arr] (ft) -- (unlearn);
\draw[arr] (unlearn) -- (score);
\draw[arr] (commit.south) to[out=-90, in=20] (score.east);
\draw[arr] (score) -- (cert);
\draw[arr] (score) -- (cs);
\draw[arr] (score) -- (rev);
\draw[arr] (cert) -- (issue);
\draw[arr] (rev) -- (revoke);
\draw[arr] (cs) -- (undet);
\draw[arr] (undet) -- (probes);
\draw[arr] (probes.west) to[out=180, in=180, looseness=1.4] (cert.west);
% phase labels
\node[lab, left=2mm of gen.west, anchor=east] {Phase 0};
\node[lab, left=48mm of unlearn.west, anchor=east] {Phase 1};
\node[lab, left=41mm of score.west, anchor=east] {Phase 2};
\end{tikzpicture}}
\DIFaddendFL \caption{The VOUCH pipeline. Phase 0 plants paired ghost canaries and commits to the
manifest \DIFaddbeginFL \DIFaddFL{as a Merkle tree}\DIFaddendFL ; Phase 1 unlearns with the in-twins riding along; Phase 2
\DIFaddbeginFL \DIFaddFL{opens one leaf at a time in a committed random order and }\DIFaddendFL streams the pairs through
three coupled anytime-valid processes---the per-score certificate minimum, the betting
confidence sequence, and the revocation mixture---whose boundary crossings trigger
issuance, continued monitoring, or revocation. R-VOUCH probes rerun the loop on
relearned, quantised, and jailbroken variants before a robust certificate is issued.}
\label{fig:flowchart}
\end{figure}

\section{Theory}\label{sec:theory}
\DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend Our guarantees rest on one structural fact---the null we plant is exact---and on the
supermartingale property of betting. The proofs are short, and their brevity is the
point: the design does the work, so we give every proof in full. Throughout,
\DIFdelbegin \DIFdel{$\F_{i-1}$ }\DIFdelend \DIFaddbegin \DIFadd{$\F_{t-1}$ }\DIFaddend denotes the verifier's filtration \DIFaddbegin \DIFadd{of Section~\ref{sec:filtration}}\DIFaddend ,
generated by everything \DIFdelbegin \DIFdel{revealed through pair $i-1$}\DIFdelend \DIFaddbegin \DIFadd{opened through step $t-1$}\DIFaddend ; all bets and mixture weights are
\DIFdelbegin \DIFdel{$\F_{i-1}$}\DIFdelend \DIFaddbegin \DIFadd{$\F_{t-1}$}\DIFaddend -measurable (predictable).

\DIFdelbegin %DIFDELCMD < \begin{theorem}[Exact, distribution-free null]%%%
\DIFdelend \DIFaddbegin \DIFadd{A word on what changed in this version, since it is the technical heart of the paper.
An earlier draft defined the certified quantity as the }\emph{\DIFadd{marginal}} \DIFadd{sign
probability $\Pr[Z_i^{(s)}=1]$ and proved coverage from a supermartingale argument
that in fact requires the stronger conditional statement
$\Efrak[Z_i^{(s)}\mid\F_{i-1}] \ge p_0$ at every step. The two coincide when signs
are independent and identically distributed across pairs, and they come apart when
they are not---which is exactly our situation, since every canary passes through one
jointly trained and unlearned model. Section~\ref{sec:exp-dependence} measures the
gap and finds it large. We therefore restate the certificate against the quantity the
protocol actually controls, and we obtain a guarantee that needs no independence,
no exchangeability across pairs, and no homogeneity across templates or repetition
strata.
}

\subsection{\DIFadd{The exact null}}

\begin{theorem}[Exact conditional null]\DIFaddend \label{thm:null}
Suppose the unlearned model $M_u$ is conditionally independent of the coin \DIFdelbegin \DIFdel{$b_i$ }\DIFdelend \DIFaddbegin \DIFadd{vector
$b=(b_1,\dots,b_m)$ }\DIFaddend given the pair \DIFdelbegin \DIFdel{$(c_i^0, c_i^1)$}\DIFdelend \DIFaddbegin \DIFadd{contents $C=\{(c_i^0,c_i^1)\}$}\DIFaddend ; exact unlearning
and retraining-from-scratch both satisfy this. Then \DIFdelbegin \DIFdel{within-pair exchangeability gives $D_i^{(s)} \overset{d}{=} -D_i^{(s)}$, so
$Z_i^{(s)} \sim \Bern(\tfrac12)$ exactly---for }\DIFdelend \DIFaddbegin \DIFadd{for every score $s\in\F$, every
step $t$, and the pair $j=\pi(t)$ opened at that step,
}\[
\DIFadd{Z_j^{(s)} \mid \F_{t-1} \;\sim\; \Bern(\tfrac12) \quad\text{exactly,}
}\]
\DIFadd{for }\DIFaddend any model, any score, and any set of query prompts. \DIFaddbegin \DIFadd{In particular the sign
sequence is a sequence of exact conditional fair coins }\emph{\DIFadd{whether or not the signs
are independent across pairs}}\DIFadd{.
}\DIFaddend \end{theorem}

\begin{proof}
\DIFdelbegin \DIFdel{Condition on the }\DIFdelend \DIFaddbegin \DIFadd{Enlarge $\F_{t-1}$ to $\G_{t-1} = \F_{t-1} \vee \sigma\bigl(\pi(t),\,
\{c_j^0, c_j^1\}\bigr)$, which reveals the position and the }\emph{\DIFadd{unordered}} \DIFadd{pair
opened at step $t$ but not its coin; this is legitimate because the manifest is
committed leaf-by-leaf (Section~\ref{sec:filtration}), so $b_j$ is opened only at
step $t$. Given $\G_{t-1}$ the two numbers $s(M_u, x)$ and $s(M_u, y)$ attached to
the }\DIFaddend unordered pair \DIFdelbegin \DIFdel{$\{c_i^0, c_i^1\}$ and the past $\F_{i-1}$. The twins
are exchangeable within the pair
by construction, and under the hypothesis $M_u$ is
conditionally independent of $b_i$: the pipeline either saw neither twin or saw only
the }\DIFdelend \DIFaddbegin \DIFadd{$\{x,y\}=\{c_j^0,c_j^1\}$ are determined. The coin $b_j$ is
independent of the other coins and of $C$ by construction, and independent of $M_u$
given $C$ by hypothesis; nothing opened before step $t$ involves $b_j$. Hence
$b_j \mid \G_{t-1} \sim \Bern(\tfrac12)$. Since $b_j$ decides which of the two
determined numbers is the }\DIFaddend in-twin\DIFdelbegin \DIFdel{, whose identity is determined by $b_i$, and after exact unlearning the
output law no longer depends on that identity. The joint law of $(c_i^{\text{in}}, c_i^{\text{ghost}}, M_u)$ is therefore invariant under swapping the two twins. Swapping negates
$D_i^{(s)} = s(M_u, c_i^{\text{in}}) - s(M_u, c_i^{\text{ghost}})$ while preserving its
distribution, so $D_i^{(s)} \overset{d}{=} -D_i^{(s)}$ conditionally on $\F_{i-1}$. Symmetry of $D_i^{(s)}$ gives $\Pr[D_i^{(s)} > 0 \mid \F_{i-1}] =
\Pr[D_i^{(s)} < 0 \mid \F_{i-1}]$}\DIFdelend \DIFaddbegin \DIFadd{'s,
}\[
\DIFadd{D_j^{(s)} \;=\; s(M_u, c_j^{\text{in}}) - s(M_u, c_j^{\text{ghost}})
\;=\; \pm\bigl|s(M_u,x) - s(M_u,y)\bigr|
}\]
\DIFadd{with the two signs equally likely, so $D_j^{(s)} \overset{d}{=} -D_j^{(s)}$ given
$\G_{t-1}$. Therefore $\Pr[D_j^{(s)}>0\mid\G_{t-1}] = \Pr[D_j^{(s)}<0\mid\G_{t-1}]$}\DIFaddend ,
and the committed tie-break coin \DIFdelbegin \DIFdel{distributes }\DIFdelend \DIFaddbegin \DIFadd{splits }\DIFaddend any atom at zero equally\DIFdelbegin \DIFdel{between the two signs, so
$Z_i^{(s)} \mid \F_{i-1} \sim \Bern(\tfrac12)$ exactly}\DIFdelend \DIFaddbegin \DIFadd{, giving
$Z_j^{(s)}\mid\G_{t-1}\sim\Bern(\tfrac12)$. Averaging over $\G_{t-1}\supseteq\F_{t-1}$
gives the claim}\DIFaddend . No property of the model, the score, \DIFdelbegin \DIFdel{or }\DIFdelend the query set\DIFaddbegin \DIFadd{, or the
dependence between pairs }\DIFaddend was used beyond measurability.
\end{proof}

Theorem~\ref{thm:null} is the reason VOUCH needs neither shadow models nor a
retrained reference: the null distribution is not estimated or assumed but generated
by our own coins, and it is exact at finite $m$. \DIFdelbegin \DIFdel{Every process below inherits its validity from it}\DIFdelend \DIFaddbegin \DIFadd{It is worth being explicit about why
the dependence objection does not bite here. The signs $Z_1^{(s)},\dots,Z_m^{(s)}$
are indeed dependent---they share one model---but the proof never asks for
independence. It asks only that the coin governing the pair opened at step $t$ still
be fair given everything opened before, and under exact unlearning it is, because the
model carries no information about it. The revocation arm therefore inherits an exact
null with no further conditions. The certificate arm, whose null is the composite
``some score still leaks,'' is where dependence has to be confronted, and that is the
next subsection.
}

\subsection{\DIFadd{The certified quantity and the certificate}}\label{sec:certquantity}

\DIFadd{Once $M_u$ and the manifest exist, the sign vector $z^{(s)} = (z_1^{(s)},\dots,
z_m^{(s)})$ of the committed cohort is fixed; the only randomness left to the audit
is the reveal order and the tie coins. We therefore certify the advantage the
declared score actually achieves against that cohort.
}

\begin{definition}[Realised cohort advantage]\label{def:delta}
\DIFadd{For $s\in\F$ the }\emph{\DIFadd{realised cohort advantage}} \DIFadd{is
}\begin{equation}\DIFadd{\label{eq:delta}
\Delta^{(s)} \;=\; \frac{2}{m}\sum_{i=1}^{m} z_i^{(s)} \;-\; 1
\;=\; 2\,\Pr\nolimits_{I}\bigl[z_I^{(s)}=1\bigr] - 1 ,
}\end{equation}
\DIFadd{the advantage of the rule ``guess that the twin scoring higher under $s$ is the
in-twin'' when the pair index $I$ is drawn uniformly from the committed cohort. Two
coarser quantities sit above it: the }\emph{\DIFadd{mechanism-level}} \DIFadd{advantage
$\Delta^{(s)}_C = \Efrak_b[\Delta^{(s)} \mid C, \xi]$, averaging over the inclusion
coins with the pair contents $C$ and the training randomness $\xi$ held fixed; and the
}\emph{\DIFadd{super-population}} \DIFadd{advantage $\bar\Delta^{(s)} = \Efrak[\Delta^{(s)}]$, averaging
over $C$ and $\xi$ as well.
}\end{definition}

\DIFadd{$\Delta^{(s)}$ is the quantity an auditor of }\emph{\DIFadd{this}} \DIFadd{model wants bounded, and it
is the one the protocol controls exactly; Definition~\ref{def:cert} is stated for it.
Proposition~\ref{prop:transfer} climbs one level, from $\Delta^{(s)}$ to
$\Delta^{(s)}_C$, at a stated cost in tolerance. We do not climb the second level to
$\bar\Delta^{(s)}$: that would need concentration over the draw of corpora, which we
neither prove nor assume. Where the experiments report a ``super-population'' verdict
they mean the fixed-boundary process of the earlier design, whose null is the
predictable conditional mean; we keep both columns throughout so that the reader can
see which claims survive at which tolerance.
}

\begin{definition}[$(\eps,\alpha)$-forgetting certificate]\label{def:cert}
\DIFadd{Fix a tolerance $\eps \in (0,1)$, an error budget $\alpha \in (0,1)$, and the boundary
$p_0 = \tfrac12 + \tfrac\eps2$. An $(\eps,\alpha)$-forgetting certificate for $M_u$
over the class $\F$ is a data-dependent stopping decision that asserts
$\sup_{s\in\F}|\Delta^{(s)}| < \eps$, issued by a procedure with the guarantee that,
whenever in truth $\sup_{s\in\F}|\Delta^{(s)}| \ge \eps$, the probability that a
certificate is }\emph{\DIFadd{ever}} \DIFadd{issued---at any stopping time, under any monitoring
rule---is at most $\alpha$}\DIFaddend .
\DIFaddbegin \end{definition}
\DIFaddend 

\DIFdelbegin %DIFDELCMD < \begin{theorem}[Certificate coverage and sound multi-score composition]%%%
\DIFdelend \DIFaddbegin \DIFadd{The two-sidedness in Definition~\ref{def:cert} is deliberate: $\Delta^{(s)} \le -\eps$,
the over-forgetting regime in which the in-twin scores conspicuously }\emph{\DIFadd{below}} \DIFadd{its
never-seen partner, is membership leakage exactly as much as $\Delta^{(s)} \ge \eps$
is, and Section~\ref{sec:exp} shows that gradient-ascent unlearning lands there in
practice.
}

\begin{theorem}[Certificate coverage under arbitrary dependence]\DIFaddend \label{thm:cert}
\DIFaddbegin \DIFadd{Reveal the cohort in a uniformly random order $\pi$ drawn by the verifier
}\emph{\DIFadd{independently of the realised sign vectors}} \DIFadd{$\{z^{(s)}\}_{s\in\F}$ and committed
before any query, and for each score $s$ and direction run the wealth process of
Eq.~\eqref{eq:wealthwor} with stakes confined to
$\lambda_t^{s,+} \in [0,\, c/(1-p_0(t)))$ and
$\lambda_t^{s,-} \in [0,\, c/\bar p_0(t))$ for some fixed $c < 1$, against the
predictable without-replacement boundary
}\begin{equation}\DIFadd{\label{eq:worboundary}
p_0(t) \;=\; \frac{m\,p_0 \;-\; \sum_{u<t} Z^{(s)}_{\pi(u)}}{m-t+1},
}\end{equation}
\DIFadd{declaring the null refuted outright if ever $p_0(t) > 1$. }\DIFaddend Issue the certificate only
when \emph{every} per-score, per-direction \DIFdelbegin \DIFdel{e-process }\DIFdelend \DIFaddbegin \DIFadd{process }\DIFaddend exceeds $1/\alpha$. Then for any
class $\F$ and any stopping rule,
\DIFdelbegin \DIFdel{$\Pr[\text{a false }(\eps,\alpha)\text{-certificate is ever issued}] \le \alpha$
whenever $\sup_s |\Delta^{(s)}| \ge \eps$}\DIFdelend \DIFaddbegin \[
\DIFadd{\Pr\bigl[\text{a false }(\eps,\alpha)\text{-certificate is ever issued}\bigr] \le \alpha
\qquad\text{whenever } \sup_{s\in\F}|\Delta^{(s)}| \ge \eps .
}\]
\DIFadd{The guarantee is exact and finite-sample, and it holds }\emph{\DIFadd{conditionally on the
cohort's sign vectors}}\DIFadd{: no independence across pairs, no exchangeability across
pairs, and no homogeneity across templates or repetition strata is assumed}\DIFaddend . No
multiplicity correction \DIFdelbegin \DIFdel{is required,
because issuance forces the truly-violating score's own supermartingale across
$1/\alpha$, an event of probability at most $\alpha$ by Ville's inequality}\DIFdelend \DIFaddbegin \DIFadd{over $\F$ is required. The one thing that }\emph{\DIFadd{is}} \DIFadd{assumed
is the independence of $\pi$ from the signs, and Remark~\ref{rem:order} explains why
it is not free}\DIFaddend .
\end{theorem}

\begin{proof}
Suppose the certification claim is false\DIFdelbegin \DIFdel{: }\DIFdelend \DIFaddbegin \DIFadd{, so }\DIFaddend some score $s^\ast$ and direction violate
the tolerance\DIFdelbegin \DIFdel{, say $p^{(s^\ast)} \ge p_0$ }\DIFdelend \DIFaddbegin \DIFadd{; say $\Delta^{(s^\ast)} \ge \eps$, i.e.\ the cohort mean
$\mu = \tfrac1m\sum_i z_i^{(s^\ast)}$ satisfies $\mu \ge p_0$ }\DIFaddend (the case
\DIFdelbegin \DIFdel{$p^{(s^\ast)} \le 1 - p_0$ is symmetric). Consider that score's own upper process $E^{s^\ast,+}$ from
Eq. ~\eqref{eq:wealthwor}. Each of its factors }\DIFdelend \DIFaddbegin \DIFadd{$\Delta^{(s^\ast)} \le -\eps$ is symmetric, run against $1-p_0$). Condition on the
entire sign vector $z^{(s^\ast)}$; the remaining randomness is the uniform
permutation $\pi$, and sequential reveal of a uniform permutation is precisely simple
random sampling without replacement. Hence the conditional mean of the next sign is
the mean of the unrevealed remainder,
}\[
\DIFadd{\Efrak\bigl[Z^{(s^\ast)}_{\pi(t)} \,\big|\, \F_{t-1}\bigr]
= \frac{m\mu - \sum_{u<t} Z^{(s^\ast)}_{\pi(u)}}{m-t+1}
\;\ge\; \frac{m p_0 - \sum_{u<t} Z^{(s^\ast)}_{\pi(u)}}{m-t+1}
\;=\; p_0(t),
}\]
\DIFadd{using $\mu \ge p_0$. Each wealth factor therefore }\DIFaddend has conditional mean
\[
\Efrak\bigl[\,1 + \lambda\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t\,}\DIFaddend (p_0\DIFaddbegin \DIFadd{(t) }\DIFaddend - Z\DIFdelbegin \DIFdel{_i}\DIFdelend ^{(s^\ast)}\DIFaddbegin \DIFadd{_{\pi(t)}}\DIFaddend ) \DIFdelbegin \DIFdel{\mid }\DIFdelend \DIFaddbegin \DIFadd{\,}\big|\DIFadd{\, }\DIFaddend \F\DIFdelbegin \DIFdel{_{i-1}}\DIFdelend \DIFaddbegin \DIFadd{_{t-1}}\DIFaddend \bigr]
= 1 + \lambda\DIFdelbegin \DIFdel{_i }\DIFdelend \DIFaddbegin \DIFadd{_t}\DIFaddend \bigl(p_0\DIFaddbegin \DIFadd{(t) }\DIFaddend - \DIFdelbegin \DIFdel{\Efrak[Z_i^{(s^\ast)} \mid \F_{i-1}]}\DIFdelend \DIFaddbegin \DIFadd{\Efrak[Z^{(s^\ast)}_{\pi(t)} \mid \F_{t-1}]}\DIFaddend \bigr) \;\le\; 1,
\]
since \DIFdelbegin \DIFdel{$\lambda_i \ge 0$ is predictableand the conditional mean of
the sign is at
least $p_0$.Hence $E^{s^\ast,+}$ }\DIFdelend \DIFaddbegin \DIFadd{$\lambda_t \ge 0$ is predictable. Nonnegativity is the other half and needs the
stake restriction: with $Z \in \{0,1\}$ the factor $1 + \lambda_t(p_0(t) - Z)$ is
minimised at $Z = 1$, where it equals $1 - \lambda_t(1 - p_0(t)) \ge 1 - c > 0$ by
$\lambda_t < c/(1-p_0(t))$; the mirror arm is identical with $\bar p_0(t)$ in place of
$1 - p_0(t)$. (This bound is why the implementation must derive the stake cap from the
}\emph{\DIFadd{same}} \DIFadd{clamped boundary the payoff is evaluated at; see
Appendix~\ref{app:repro}.) So the process }\DIFaddend is a nonnegative supermartingale \DIFdelbegin \DIFdel{with
$E^{s^\ast,+}_0 = 1$}\DIFdelend \DIFaddbegin \DIFadd{started at
one}\DIFaddend , and Ville's inequality gives
$\Pr[\exists t: E_t^{s^\ast,+} \ge 1/\alpha] \le \alpha$. \DIFaddbegin \DIFadd{The refutation branch is
never taken under the null, because $p_0(t) \le \Efrak[Z_{\pi(t)}\mid\F_{t-1}] \le 1$.
}\DIFaddend Issuing the certificate requires \emph{every} process to exceed $1/\alpha$---in
particular the violating score's own---so a false certificate is issued with
\DIFaddbegin \DIFadd{conditional }\DIFaddend probability at most $\alpha$, uniformly over stopping rules\DIFaddbegin \DIFadd{; averaging
over the sign vectors preserves the bound}\DIFaddend . No union bound over $\F$ is needed because
the argument runs entirely through the single process attached to the violating
score.
\end{proof}

\DIFaddbegin \DIFadd{Two consequences are worth naming because they shape how the certificate should be
read. First, the guarantee spends no assumption on the }\emph{\DIFadd{data}}\DIFadd{: whatever
dependence the shared model induces among the signs, it is absorbed by conditioning on
the realised sign vector, and the only randomness the proof spends is the verifier's
own permutation. Second, and less comfortably, a cohort audited to exhaustion resolves
}\emph{\DIFadd{deterministically}}\DIFadd{: at $t=m$ the realised mean is known, and no inference
remains. The betting machinery therefore buys three things---early stopping at a
fraction of the query budget, validity under continuous monitoring and across
deletion waves, and the alarm arm, whose null is the genuinely stochastic one of
Theorem~\ref{thm:null}---rather than buying the cohort statement itself. We think
this is the honest way to describe what a canary audit can do, and
Section~\ref{sec:exp-baselines} quantifies each of the three.
}

\begin{remark}[The reveal order must be the verifier's own randomness]\label{rem:order}
\DIFadd{Theorem~\ref{thm:cert} conditions on the sign vector and spends the permutation, so it
needs $\pi \perp \{z^{(s)}\}$. That is not a formality here. A provider
who can locate the cohort---and Section~\ref{sec:exp-detect} shows that is
cheap---could, if it also knew $\pi$, arrange which pairs are revealed early. The
released implementation therefore draws $\pi$ from one of three sources, and records
which on the certificate. A }\emph{\DIFadd{public randomness beacon}} \DIFadd{is the recommended one:
the verifier fetches a drand round published }\emph{\DIFadd{after}} \DIFadd{the manifest commitment and
seeds $\pi$ with $H(\text{root} \,\|\, \text{beacon randomness})$, so the order is
unpredictable to the provider before the round exists, independent of the signs, and
reconstructible afterwards by anyone holding the certificate---the audit's own
randomness becomes as checkable as its commitment. Failing network access, the
verifier draws from operating-system entropy, which satisfies the hypothesis but is
not third-party verifiable. The third source is the run seed, which is what produced
the results in this paper and which does }\emph{\DIFadd{not}} \DIFadd{satisfy the hypothesis, since that
seed also seeds cohort generation and training; nothing in the pipeline exploits the
coupling, but the condition is not met, and Appendix~\ref{app:repro} records exactly
where. Binding the beacon value to the commitment matters in both directions: drawing
the beacon first would let a provider grind the manifest against a known order.
}\end{remark}

\begin{proposition}[Transfer to the coin-averaged advantage]\label{prop:transfer}
\DIFadd{Fix the pair contents $C$ and the training randomness $\xi$, and write
$\Delta^{(s)}_C = \Efrak_b\bigl[\Delta^{(s)} \,\big|\, C, \xi\bigr]$ for the
advantage averaged over the inclusion coins alone---the advantage the unlearning
}\emph{\DIFadd{mechanism}} \DIFadd{yields on this corpus. Assume }\emph{\DIFadd{bounded coin influence}}\DIFadd{:
flipping a single coin $b_i$ changes at most $\kappa$ of the $m$ signs
$z_j^{(s)}$. Then for any $\alpha' \in (0,1)$, with probability at least
$1-\alpha'$,
}\begin{equation}\DIFadd{\label{eq:transfer}
\bigl|\Delta^{(s)} - \Delta^{(s)}_C\bigr| \;\le\;
\kappa\,\sqrt{\frac{2\log(2/\alpha')}{m}} .
}\end{equation}
\DIFadd{Consequently a cohort certificate at tolerance $\eps$ implies a mechanism-level
certificate at tolerance $\eps + \kappa\sqrt{2\log(2/\alpha')/m}$, at total error
level $\alpha + \alpha'$.
}\end{proposition}

\begin{proof}
\DIFadd{Write $f(b) = \tfrac2m\sum_j z_j^{(s)} - 1$ as a function of the coin vector, the
other randomness held fixed. Changing one coordinate changes $\sum_j z_j^{(s)}$ by at
most $\kappa$ by hypothesis, so $f$ has bounded differences $c_i = 2\kappa/m$.
McDiarmid's inequality gives
$\Pr[|f - \Efrak_b f| \ge u] \le 2\exp(-2u^2/\sum_i c_i^2) = 2\exp(-u^2 m/(2\kappa^2))$,
with the expectation over the coins and $(C,\xi)$ held fixed---which is exactly
$\Delta^{(s)}_C$; setting the right-hand side to $\alpha'$ yields
\eqref{eq:transfer}.
}\end{proof}

\DIFadd{Three qualifications travel with Proposition~\ref{prop:transfer} and we would rather
state them than let a reader over-read the bound. First, it bounds the distance to
$\Delta^{(s)}_C$, the }\emph{\DIFadd{coin-averaged}} \DIFadd{advantage on this corpus, not to
$\bar\Delta^{(s)} = \Efrak[\Delta^{(s)}]$ of Definition~\ref{def:delta}, which
additionally averages over the draw of $C$ and $\xi$; closing that second gap requires
a further concentration step over corpora that we do not have and do not claim.
Second, the bound is informative only when $\kappa = o(\sqrt m)$: at
$\kappa = \Theta(m)$ it is vacuous. Under exact unlearning $\kappa = 1$ holds
trivially, since a coin that leaves no trace in the model can only affect its own
pair's sign; away from it, an influential coin can move the model and hence many
signs, which is precisely the regime where the transfer is wanted. Third, the size of
the price is worth internalising: at $m=512$, $\alpha'=0.05$ and $\kappa=1$ the
inflation is $0.12$, which is why a mechanism-level claim at $\eps = 0.05$ needs a
cohort an order of magnitude larger than a cohort claim at the same tolerance---a gap
Section~\ref{sec:exp-bench} reports rather than elides.
}

\DIFaddend \begin{remark}[Why the intuitive mixture fails]\label{rem:mixture}
It is tempting to run a \emph{single} certificate process on the adaptively reweighted
sign \DIFdelbegin \DIFdel{$\bar Z_i = \sum_s w_s Z_i^{(s)}$}\DIFdelend \DIFaddbegin \DIFadd{$\bar Z_t = \sum_s w_s Z_t^{(s)}$}\DIFaddend , and indeed this is valid for the revocation arm,
whose null makes every component an exact \DIFaddbegin \DIFadd{conditional }\DIFaddend fair coin. It is \emph{not}
valid for the certificate, whose composite null is ``some score still leaks.'' The
failure is easy to exhibit: take \DIFdelbegin \DIFdel{$p^{(s_1)} = p_0$ }\DIFdelend \DIFaddbegin \DIFadd{$\Delta^{(s_1)} = \eps$ }\DIFaddend exactly at the boundary and
\DIFdelbegin \DIFdel{$p^{(s_2)} = \dots = \tfrac12$ }\DIFdelend \DIFaddbegin \DIFadd{$\Delta^{(s_2)} = \dots = 0$ }\DIFaddend clean. Then
\DIFdelbegin \DIFdel{$\Efrak[\bar Z_i \mid \F_{i-1}] = w_1 p_0 + (1 - w_1)/2 < p_0$ }\DIFdelend \DIFaddbegin \DIFadd{$\Efrak[\bar Z_t \mid \F_{t-1}] = w_1 p_0 + (1 - w_1)/2 < p_0$ }\DIFaddend whenever $w_1 < 1$, so
each factor of the mixture's certificate process has conditional mean strictly greater
than one: the process is a \emph{sub}martingale under the null and crosses $1/\alpha$
with probability approaching one. Worse, the violation is generic rather than
adversarial, because discrimination-driven reweighting moves weight \emph{away} from
the one leaking score under partial unlearning. \DIFdelbegin \DIFdel{We }\DIFdelend \DIFaddbegin \DIFadd{Against this construction---which is
our own earlier design, not a published method---we }\DIFaddend measured a false-certification
rate of $0.996$ \DIFdelbegin \DIFdel{for this construction }\DIFdelend against $0.007$ for the per-score minimum
(Table~\ref{tab:soundness}), which is why VOUCH requires consensus rather than an
average.
\end{remark}

\DIFdelbegin %DIFDELCMD < \begin{theorem}[Power]%%%
\DIFdelend \DIFaddbegin \subsection{\DIFadd{Power, confidence sequences, and composition}}

\begin{theorem}[Power, fixed-boundary process]\DIFaddend \label{thm:power}
\DIFaddbegin \DIFadd{Consider the }\emph{\DIFadd{super-population}} \DIFadd{arm, i.e.\ the process of
Eq.~\eqref{eq:wealthwor} run against the fixed boundary $p_0(t) \equiv p_0$ with signs
i.i.d.\ across pairs. }\DIFaddend If $|\Delta^{(s)}| < \eps$ for all $s$ and the bets have
sublinear regret (the mixture of Section~\ref{sec:engine} qualifies), then
$\tfrac1t \log E_t \to \KL(\Bern(p)\,\|\,\Bern(p_0))$ almost surely for each process,
and the expected certification time is
$\Efrak[\tau^*] = (1+o(1))\,\log(1/\alpha)/\min_{s,\pm}\KL$\DIFdelbegin \DIFdel{. For }\DIFdelend \DIFaddbegin \DIFadd{, the expectation being
taken under }\DIFaddend exact unlearning and \DIFaddbegin \DIFadd{the asymptotics in }\DIFaddend small $\eps$\DIFaddbegin \DIFadd{. Since
}\begin{equation}\DIFadd{\label{eq:klexp}
\KL\bigl(\Bern(\tfrac12)\,\big\|\,\Bern(\tfrac12 + \tfrac\eps2)\bigr)
\;=\; -\tfrac12\log\bigl(1-\eps^2\bigr)
\;=\; \tfrac{\eps^2}{2} + O(\eps^4),
}\end{equation}
\DIFaddend this is approximately \DIFdelbegin \DIFdel{$8\log(1/\alpha)/\eps^2$ }\DIFdelend \DIFaddbegin \DIFadd{$2\log(1/\alpha)/\eps^2$ }\DIFaddend pairs.
\end{theorem}

\DIFaddbegin \DIFadd{The restriction to the fixed boundary is not cosmetic. Under without-replacement reveal the log-wealth increments are
neither independent nor identically distributed, the boundary $p_0(t)$ moves, and the
admissible stake range moves with it---so ``the best constant bet'' is not well
defined, and neither the $O(\log t)$ ONS regret bound (which assumes a fixed convex
domain) nor the Krichevsky--Trofimov redundancy bound (which assumes a fixed $p_0$)
transfers as stated. Theorem~\ref{thm:power} should therefore be read as sizing the
cohort for a }\emph{\DIFadd{mechanism-level}} \DIFadd{claim. For the finite-cohort target we do not have
a matching analysis; its certification time is capped by $m$, since the claim resolves
deterministically once the cohort is exhausted, and we report its behaviour
empirically instead (Table~\ref{tab:power}, cohort column).
}

\DIFaddend \begin{proof}
Fix one process, say the upper process for a score with true sign probability
$p < p_0$. For a constant bet $\lambda$, the log-wealth increments
\DIFdelbegin \DIFdel{$\log(1 + \lambda(p_0 - Z_i))$ }\DIFdelend \DIFaddbegin \DIFadd{$\log(1 + \lambda(p_0 - Z_t))$ }\DIFaddend are i.i.d.\ with mean
$f(\lambda) = p \log(1 - \lambda(1 - p_0)) + (1-p)\log(1 + \lambda p_0)$, so by the
strong law $\tfrac1t \log E_t \to f(\lambda)$ almost surely. The Kelly-optimal
constant bet is $\lambda^\ast = (p_0 - p)/(p_0(1 - p_0))$: substituting it turns the
two possible wealth factors into the Bernoulli likelihood ratios
\DIFdelbegin \[
\DIFdel{1 - \lambda^\ast(1 - p_0) = \frac{p}{p_0}, \qquad
1 + \lambda^\ast p_0 = \frac{1 - p}{1 - p_0},
}\]%DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{$1 - \lambda^\ast(1 - p_0) = p/p_0$ and $1 + \lambda^\ast p_0 = (1-p)/(1-p_0)$,
}\DIFaddend so $f(\lambda^\ast) = p \log\tfrac{p}{p_0} + (1-p)\log\tfrac{1-p}{1-p_0}
= \KL(\Bern(p)\,\|\,\Bern(p_0))$, and no constant bet can do better because $f$ is
concave with this maximum. A betting strategy whose log-wealth regret against the best
constant bet is $o(t)$ therefore attains the same limit; the ONS expert has $O(\log t)$
regret, the KT plug-in \eqref{eq:kt} achieves $\KL - \tfrac{1}{2t}\log t$ directly by
the Krichevsky--Trofimov redundancy bound, and the uniform mixture over eight experts
trails its best expert by at most the constant $\log 8$, so all three satisfy the
hypothesis. For the crossing time, the process rejects when
$\log E_t \ge \log(1/\alpha)$; since $\tfrac1t\log E_t \to \KL$ almost surely, the
first-crossing time $\tau^\ast$ \DIFdelbegin \DIFdel{of the drifting random walk }\DIFdelend satisfies
$\Efrak[\tau^\ast] = (1 + o(1)) \log(1/\alpha)/\KL$ by the standard Wald argument, and
issuance waits for the \emph{slowest} process, whose drift is $\min_{s,\pm} \KL$.
Finally, under exact unlearning $p = \tfrac12$ for every score, and
\DIFdelbegin \DIFdel{a second-order
expansion gives $\KL(\Bern(\tfrac12) \| \Bern(\tfrac12 + \tfrac\eps2)) =
\tfrac{\eps^2}{8} + O(\eps^4)$, whence
$\Efrak[\tau^\ast] \approx 8\log(1/\alpha)/\eps^2$}\DIFdelend \DIFaddbegin \[
\DIFadd{\KL\bigl(\Bern(\tfrac12)\,\big\|\,\Bern(p_0)\bigr)
= \tfrac12\log\tfrac{1/2}{p_0} + \tfrac12\log\tfrac{1/2}{1-p_0}
= -\tfrac12\log\bigl(4p_0(1-p_0)\bigr)
= -\tfrac12\log(1-\eps^2),
}\]
\DIFadd{whose expansion is \eqref{eq:klexp}, giving
$\Efrak[\tau^\ast] \approx 2\log(1/\alpha)/\eps^2$}\DIFaddend .
\end{proof}

Theorem~\ref{thm:power} \DIFdelbegin \DIFdel{matters for deployment because it }\DIFdelend tells the provider how large a canary cohort to plant\DIFdelbegin \DIFdel{, and it corrects an earlier heuristic: an }\DIFdelend \DIFaddbegin \DIFadd{. An
}\DIFaddend $\eps = 0.1$ certificate at $\alpha = 0.05$ \DIFdelbegin \DIFdel{requires }\DIFdelend \DIFaddbegin \DIFadd{needs }\DIFaddend on the order of \DIFdelbegin \DIFdel{six hundred pairs, not the
one-hundred-ninety a naive calculation suggested, and }\DIFdelend \DIFaddbegin \DIFadd{$600$ pairs, an
$\eps = 0.05$ certificate roughly $2{,}400$, and an $\eps=0.02$ certificate roughly
$15{,}000$; }\DIFaddend because the drift equals the Kullback--Leibler divergence---the
information-theoretic optimum for testing $\Bern(\tfrac12)$ against $\Bern(p_0)$---no
valid procedure can do better by more than a constant factor. \DIFaddbegin \DIFadd{We note that an earlier
version of this theorem reported the expansion as $\eps^2/8$ and the sample size as
$8\log(1/\alpha)/\eps^2$; \eqref{eq:klexp} corrects it. The numerical columns of
Table~\ref{tab:power} were computed from the exact divergence and are unaffected.
}

\begin{theorem}[Finite-cohort certification time]\label{thm:cohortpower}
\DIFadd{Fix a score $s$ and run the upper certificate process of
Theorem~\ref{thm:cert}: the without-replacement boundary
\eqref{eq:worboundary} with the rule that the null is refuted outright once
$p_0(t) > 1$. Let the committed cohort of size $m$ carry exactly $j$ signs
$z_i^{(s)} = 1$, and let $k_0 = \lceil m p_0 \rceil$ be the least number of
such signs compatible with the null $\Delta^{(s)} \ge \eps$. If
$j \le k_0 - 2$---the realised cohort sits at least two pairs below the
boundary---then the process refutes, and hence certifies at }\emph{\DIFadd{any}}
\DIFadd{$\alpha$, by the stopping time
}\begin{equation}\DIFadd{\label{eq:cohortworst}
\tau^\ast \;\le\; m - (k_0 - j) + 2
\qquad\text{for \emph{every} reveal order } \pi,
}\end{equation}
\DIFadd{and, under the uniformly random $\pi$ that Theorem~\ref{thm:cert} requires,
}\begin{equation}\DIFadd{\label{eq:cohortmean}
\Efrak[\tau^\ast] \;\le\; \frac{(m - k_0 + 1)(m+1)}{m - j + 1} \;+\; 1 ,
}\end{equation}
\DIFadd{with equality when the decision is reached by refutation. The lower arm is
the mirror statement with $z$ replaced by $1-z$ and $p_0$ by $1-p_0$; the
two-sided certificate over a class $\F$ is issued by the largest of the
$2|\F|$ individual bounds. Neither bound involves $\alpha$, the betting
strategy, or $|\F|$.
}\end{theorem}

\begin{proof}
\DIFadd{Refutation fires at the first $t$ with $p_0(t) > 1$, i.e.\ with
$m p_0 - \sum_{u<t} Z_{\pi(u)} > m - t + 1$. Writing $S_{t-1}$ for the
revealed count of ones and $W_{t-1} = (t-1) - S_{t-1}$ for the revealed
count of zeros, and using $S_{t-1} + (m - t + 1) = m - W_{t-1}$, the
condition is $m - W_{t-1} < m p_0$, that is $W_{t-1} > m(1 - p_0)$. Both
sides being compared to an integer, this is exactly
$W_{t-1} \ge m - k_0 + 1 =: r$, whether or not $m p_0$ is an integer.
Refutation therefore occurs one step after the $r$-th zero is revealed:
$\tau^\ast = 1 + \pi\text{-position of the } r\text{-th zero}$. The cohort
contains $m - j$ zeros, and $r \le m - j - 1$ exactly when $j \le k_0 - 2$,
so under that hypothesis the $r$-th zero is revealed at some position
$\le m - 1$ and $\tau^\ast \le m$.
}

\DIFadd{For \eqref{eq:cohortworst}, the position of the $r$-th zero is largest when
every one precedes every zero, putting it at $j + r = j + m - k_0 + 1$;
adding the one step gives the bound, and the ordering that attains it shows
it is tight. For \eqref{eq:cohortmean}, let $M = m - j$ be the number of
zeros. In a uniformly random arrangement the $M$ zeros split the $m - M$
ones into $M+1$ exchangeable gaps, so the expected number of ones preceding
the $r$-th zero is $r(m - M)/(M + 1)$ and the expected position of that zero
is $r + r(m-M)/(M+1) = r(m+1)/(M+1)$. Adding one step gives
\eqref{eq:cohortmean}.
}

\DIFadd{Finally, the argument never inspects the stake $\lambda_t$: refutation is a
statement about which cohort compositions remain feasible, so it holds for
every predictable betting strategy, and the certificate's actual stopping
time is the minimum of this time and the time its wealth crosses $1/\alpha$
by ordinary growth.
}\end{proof}

\DIFadd{Theorem~\ref{thm:cohortpower} is the finite-population counterpart that
Theorem~\ref{thm:power} could not supply. Under without-replacement reveal
the log-wealth increments are neither independent nor identically
distributed and the regret bounds behind Theorem~\ref{thm:power} do not
transfer, so we bound the cohort target's stopping time by a different
route: not by how fast wealth grows, but by when the null's remaining
feasible compositions run out. The two mechanisms run in parallel and the
process stops at whichever fires first, which is what makes the cohort
column of Table~\ref{tab:power} legitimately faster than the
Kullback--Leibler column rather than mysteriously so. Their regimes are
also easy to tell apart: exponential growth dominates when the cohort is
large relative to $\log(1/\alpha)/\KL$, and the feasibility bound binds when
it is not, which is exactly the tight-tolerance regime of
Section~\ref{sec:exp-tight}. At $m = 384$, $\eps = 0.2$ and a cohort at
chance ($j = m/2$), \eqref{eq:cohortmean} gives $308$ pairs against a
measured median of $165$: the wealth process usually wins, and the
feasibility bound is the guarantee that holds when it does not.
}

\begin{remark}[Per-score versus simultaneous confidence sequences]\label{rem:cs}
\DIFadd{Each score's confidence sequence \eqref{eq:cs} covers that score's own advantage
$\Delta^{(s)}$ with uniform-in-time probability $1-\alpha$; the guarantee is
}\emph{\DIFadd{per score}}\DIFadd{. Two derived statements should be distinguished. The reported
$\max_{s\in\F} U_t^{(s)}$ }\emph{\DIFadd{is}} \DIFadd{a valid level-$\alpha$ anytime upper bound on
$\sup_{s\in\F}\Delta^{(s)}$ with no multiplicity correction, because the maximising
index of the population quantities is a fixed, non-random index once the cohort is
fixed, and that single score's own sequence is the only one the argument needs.
Simultaneous two-sided coverage of }\emph{\DIFadd{all}} \DIFadd{$|\F|$ advantages, on the other hand,
costs a union bound and holds at level $|\F|\alpha$; we report intervals at that
corrected level wherever we display the whole class.
}\end{remark}
\DIFaddend 

\begin{theorem}[Streaming composition, in both directions]\label{thm:streaming}
Let deletion waves $k = 1,\dots,K$ use cohorts with independent coins.
\emph{(a) Certificates compose by consensus.} Claiming that the whole history is clean
only when every wave has individually earned its certificate keeps the probability of a
false history-wide claim at or below $\alpha$ for any $K$; multiplying certificate
e-values instead tests the intersection null ``every wave is bad,'' whose rejection
certifies merely that \emph{some} wave is clean.
\emph{(b) Alarms compose by product.} Under the intersection null ``every wave was
exactly unlearned'' each per-wave revocation process is a supermartingale, so their
product is one too and crosses $1/\alpha$ with probability at most $\alpha$; the product
accumulates leakage spread so thinly across waves that no single wave reveals it.
\emph{(c)} Per-wave alarms with $\alpha$-spending $\alpha_k = \alpha 2^{-(k+1)}$ control
family-wise false revocation over an unbounded history.
\end{theorem}

\begin{proof}
\emph{(a)} Suppose the history-wide claim is false, so some wave $k^\ast$ truly has
$\sup_s |\Delta^{(s),(k^\ast)}| \ge \eps$. The all-pass rule claims only when every
wave has individually issued, so claiming requires wave $k^\ast$'s own per-score
minimum process to cross $1/\alpha$, an event of probability at most $\alpha$ by
Theorem~\ref{thm:cert}---independently of $K$ and of how the auditor schedules the
waves. For the negative half of the statement, note that a product
$\prod_k E^{(k)}$ of \emph{certificate} e-values is, by construction, an e-value for
the intersection null $\bigcap_k \{\text{wave } k \text{ bad}\}$; rejecting an
intersection establishes only the disjunction ``some wave is clean.'' In the
one-bad-wave scenario the clean waves' e-values grow without bound \DIFdelbegin \DIFdel{(Theorem~\ref{thm:power}) }\DIFdelend and drag the
product over $1/\alpha$ regardless of the bad wave\DIFdelbegin \DIFdel{; we measured this as a $0.96$ }\DIFdelend \DIFaddbegin \DIFadd{. We measure the damage rather than
assert it: over $500$ ten-wave histories containing one genuinely bad wave, the
product rule issues a }\DIFaddend false history-wide \DIFdelbegin \DIFdel{certification rate }\DIFdelend \DIFaddbegin \DIFadd{certificate in }\emph{\DIFadd{every}} \DIFadd{history whose bad
wave truly violates the tolerance, }\DIFaddend against $0.000$ for \DIFaddbegin \DIFadd{the }\DIFaddend all-pass \DIFaddbegin \DIFadd{rule
}\DIFaddend (Table~\DIFdelbegin \DIFdel{\ref{tab:streaming}}\DIFdelend \DIFaddbegin \DIFadd{\ref{tab:certprod}}\DIFaddend ).

\emph{(b)} Under the intersection null, Theorem~\ref{thm:null} applies wave by wave,
so each $R^{(k)}$ is a nonnegative supermartingale with initial value one with respect
to the combined filtration, and cohorts across waves are independent by construction.
Conditional on \DIFdelbegin \DIFdel{$\F_{i-1}$}\DIFdelend \DIFaddbegin \DIFadd{$\F_{t-1}$}\DIFaddend , the increment of the product
$\prod_k R^{(k)}$ at any arrival is the increment of the single wave whose pair is
revealed, which has conditional mean at most one; the product is therefore itself a
nonnegative supermartingale started at one, for any adapted within-wave sampling times,
and Ville's inequality bounds its crossing probability by $\alpha$. Its power against
distributed leakage is additive in the exponent: if every wave carries advantage
$\Delta_0 > 0$, each factor drifts upward at rate
$\KL(\Bern(\tfrac12 + \tfrac{\Delta_0}2) \,\|\, \Bern(\tfrac12))$ per pair, so the
product crosses after roughly $\log(1/\alpha)/(m \cdot \KL)$ waves even when no single
wave is individually detectable.

\emph{(c)} Give wave $k$ its own alarm at level $\alpha_k = \alpha 2^{-(k+1)}$. By
Ville applied per wave, the probability that a truly clean wave $k$ ever alarms is at
most $\alpha_k$, and the union bound over an unbounded horizon costs
$\sum_{k \ge 1} \alpha 2^{-(k+1)} \le \alpha/2 \le \alpha$.
\end{proof}

\begin{proposition}[A magnitude-aware alarm]\label{prop:magnitude}
Under the null of Theorem~\ref{thm:null}, \DIFdelbegin \DIFdel{$\mathrm{sign}(D_i)$ }\DIFdelend \DIFaddbegin \DIFadd{$\mathrm{sign}(D_t)$ }\DIFaddend is a fair coin even
conditional on \DIFdelbegin \DIFdel{$|D_i|$}\DIFdelend \DIFaddbegin \DIFadd{$|D_t|$}\DIFaddend , so wealth factors of the form
\DIFdelbegin \DIFdel{$1 + \lambda_i\,\mathrm{sign}(D_i)\,g_i$
}\DIFdelend \DIFaddbegin \DIFadd{$1 + \lambda_t\,\mathrm{sign}(D_t)\,g_t$ }\DIFaddend are an exact e-process for any predictable
stake \DIFdelbegin \DIFdel{$g_i \in [0,1]$ }\DIFdelend \DIFaddbegin \DIFadd{$g_t \in [0,1]$ }\DIFaddend measurable with respect to the past and \DIFdelbegin \DIFdel{$|D_i|$. Taking $g_i$ }\DIFdelend \DIFaddbegin \DIFadd{$|D_t|$. Taking $g_t$ }\DIFaddend to
be the predictable rank of \DIFdelbegin \DIFdel{$|D_i|$ }\DIFdelend \DIFaddbegin \DIFadd{$|D_t|$ }\DIFaddend concentrates bets on the large-effect pairs where
sparse memorisation lives, and halves detection time under $5\%$-sparse leakage
(Section~\DIFdelbegin \DIFdel{\ref{sec:exp}}\DIFdelend \DIFaddbegin \DIFadd{\ref{sec:exp-ablation}}\DIFaddend ). Because it bets aggressively we report it as an
optional power enhancement and keep the sign-based arm as the default.
\end{proposition}

\begin{proof}
Symmetry of the conditional law of \DIFdelbegin \DIFdel{$D_i$ }\DIFdelend \DIFaddbegin \DIFadd{$D_t$ }\DIFaddend (Theorem~\ref{thm:null}) means that
conditioning on the magnitude \DIFdelbegin \DIFdel{$|D_i|$ }\DIFdelend \DIFaddbegin \DIFadd{$|D_t|$ }\DIFaddend leaves the sign exactly fair:
\DIFdelbegin \DIFdel{$\Efrak[\mathrm{sign}(D_i) \mid |D_i|, \F_{i-1}] = 0$}\DIFdelend \DIFaddbegin \DIFadd{$\Efrak[\mathrm{sign}(D_t) \mid |D_t|, \F_{t-1}] = 0$}\DIFaddend , with ties randomised by the
committed coin. For any stake \DIFdelbegin \DIFdel{$g_i \in [0,1]$ }\DIFdelend \DIFaddbegin \DIFadd{$g_t \in [0,1]$ }\DIFaddend that is measurable with respect to
\DIFdelbegin \DIFdel{$\F_{i-1} \vee \sigma(|D_i|)$}\DIFdelend \DIFaddbegin \DIFadd{$\F_{t-1} \vee \sigma(|D_t|)$}\DIFaddend ---the predictable rank of \DIFdelbegin \DIFdel{$|D_i|$ }\DIFdelend \DIFaddbegin \DIFadd{$|D_t|$ }\DIFaddend among past magnitudes
qualifies---the tower property gives
\DIFdelbegin \DIFdel{$\Efrak[\mathrm{sign}(D_i)\, g_i \mid \F_{i-1}] = \Efrak[\,g_i\,
\Efrak[\mathrm{sign}(D_i) \mid |D_i|, \F_{i-1}] \mid \F_{i-1}] = 0$}\DIFdelend \DIFaddbegin \DIFadd{$\Efrak[\mathrm{sign}(D_t)\, g_t \mid \F_{t-1}] = \Efrak[\,g_t\,
\Efrak[\mathrm{sign}(D_t) \mid |D_t|, \F_{t-1}] \mid \F_{t-1}] = 0$}\DIFaddend , so each factor
\DIFdelbegin \DIFdel{$1 + \lambda_i\, \mathrm{sign}(D_i)\, g_i$ with predictable $\lambda_i \in [0,1)$ }\DIFdelend \DIFaddbegin \DIFadd{$1 + \lambda_t\, \mathrm{sign}(D_t)\, g_t$ with predictable $\lambda_t \in [0,1)$ }\DIFaddend has
conditional mean exactly one and stays positive. The product is a nonnegative
martingale with initial value one, hence an exact e-process, and Ville's inequality
applies as before. The power claim is empirical and quantified in
Section~\DIFdelbegin \DIFdel{\ref{sec:exp}}\DIFdelend \DIFaddbegin \DIFadd{\ref{sec:exp-ablation}}\DIFaddend .
\end{proof}

\DIFdelbegin %DIFDELCMD < \begin{proposition}[Bridge from certified unlearning]%%%
\DIFdelend \DIFaddbegin \begin{proposition}[One-directional bridge from certified unlearning]\DIFaddend \label{prop:bridge}
If $\Ucal$ is $(\eps_u,\delta_u)$-certified, then
\DIFdelbegin \DIFdel{$|\Delta^{(s)}| \le \tfrac{e^{\eps_u}-1}{e^{\eps_u}+1} + \delta_u$ }\DIFdelend \DIFaddbegin \DIFadd{$|\bar\Delta^{(s)}| \le \tfrac{e^{\eps_u}-1}{e^{\eps_u}+1} + \delta_u$ }\DIFaddend for every $s$, so for
$\eps$ above this value VOUCH issues with probability $1 - \alpha - o(1)$. A certified
algorithm therefore passes VOUCH, as consistency demands, while VOUCH remains meaningful
at scales where $(\eps_u,\delta_u)$ certificates are unattainable.
\end{proposition}

\begin{proof}
The sign $Z^{(s)}$ is a randomised post-processing of the pipeline output: it is
computed from $M_u$ and the pair alone. $(\eps_u,\delta_u)$-certified unlearning makes
the law of $M_u$, under either value of the coin,
$(\eps_u,\delta_u)$-indistinguishable from the law of the retrained reference, under
which $Z^{(s)}$ is an exact fair coin by Theorem~\ref{thm:null}. Now view the rule
``guess that the twin with the larger score is the in-twin'' as a hypothesis test for
the coin $b_i$; its advantage is exactly
\DIFdelbegin \DIFdel{$\Pr[\text{correct}] - \Pr[\text{wrong}] = 2p^{(s)} - 1 = \Delta^{(s)}$}\DIFdelend \DIFaddbegin \DIFadd{$\Pr[\text{correct}] - \Pr[\text{wrong}] = \bar\Delta^{(s)}$}\DIFaddend . The
trade-off region of \citet{kairouz2015composition} states that any test between two
$(\eps_u,\delta_u)$-indistinguishable distributions has type-I and type-II errors
satisfying $e^{\eps_u}\beta_1 + \beta_2 \ge 1 - \delta_u$ and
$\beta_1 + e^{\eps_u}\beta_2 \ge 1 - \delta_u$; adding the two constraints,
$\beta_1 + \beta_2 \ge 2(1 - \delta_u)/(1 + e^{\eps_u})$, so the advantage obeys
\[
|\DIFaddbegin \bar\DIFaddend \Delta^{(s)}| \;\le\; 1 - (\beta_1 + \beta_2)
\;\le\; 1 - \frac{2(1 - \delta_u)}{1 + e^{\eps_u}}
\;=\; \frac{e^{\eps_u} - 1}{e^{\eps_u} + 1} + \frac{2\delta_u}{1 + e^{\eps_u}}
\;\le\; \frac{e^{\eps_u} - 1}{e^{\eps_u} + 1} + \delta_u.
\]
When $\eps$ strictly exceeds this bound, every certificate process has strictly
positive drift by Theorem~\ref{thm:power} and crosses $1/\alpha$ with probability
tending to one as $m \to \infty$, while the revocation arm false-alarms with
probability at most $\alpha$; the issuance probability is therefore at least
$1 - \alpha - o(1)$.
\end{proof}

\DIFaddbegin \begin{remark}[The bridge runs one way only]\label{rem:onedirection}
\DIFadd{Proposition~\ref{prop:bridge} states that certified unlearning implies passing VOUCH.
The converse is false and we claim nothing like it: passing VOUCH does }\emph{\DIFadd{not}}
\DIFadd{imply any $(\eps_u,\delta_u)$ guarantee, does not bound the influence of any datum
outside the canary cohort, and does not constrain scores outside $\F$. The
proposition is a consistency check on the definition---an algorithm with a
parameter-space guarantee should not be flagged by a behavioural audit---and it
should not be read as a semantic equivalence in either direction.
}\end{remark}

\DIFaddend \paragraph{Scope, stated \DIFdelbegin \DIFdel{honestly}\DIFdelend \DIFaddbegin \DIFadd{precisely}\DIFaddend .} A VOUCH certificate concerns \DIFdelbegin \DIFdel{influence }\DIFdelend \DIFaddbegin \DIFadd{the advantage
}\DIFaddend extractable through the declared class $\F$\DIFdelbegin \DIFdel{on the canary law}\DIFdelend \DIFaddbegin \DIFadd{, on the committed canary cohort, against
the deployed model, under the honest-but-verifiable threat model of
Section~\ref{sec:setting}}\DIFaddend . It does not assert erasure in parameter space, which no
behavioural method can\DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{; it is a per-cohort statement, not a per-deletion-request one,
because per-request certificates would require per-user canaries; the transfer to the
super-population advantage costs the explicit inflation of
Proposition~\ref{prop:transfer}; }\DIFaddend and the extrapolation from \DIFaddbegin \DIFadd{planted }\DIFaddend canaries to
organic \DIFdelbegin \DIFdel{data }\DIFdelend \DIFaddbegin \DIFadd{records }\DIFaddend is \emph{calibrated} through the dose--response strata \DIFaddbegin \DIFadd{and the
head-to-head comparisons of Section~\ref{sec:exp-descriptive} }\DIFaddend rather than proven. We
regard stating these limits precisely as part of the contribution, since a certificate
whose scope is overclaimed is worse than none.


\section{Experiments}\label{sec:exp}
We organise the study into three tiers so that each claim is tested where it can be
tested most sharply. The \emph{simulation tier} draws pair signs directly from their
generating distribution---exactly the object \DIFdelbegin \DIFdel{Theorem~\ref{thm:null} controls---which
}\DIFdelend \DIFaddbegin \DIFadd{Theorems~\ref{thm:null} and
\ref{thm:cert} control---which }\DIFaddend lets us calibrate validity and power over thousands of
seeds without confounding them with the vagaries of a particular model. The
\emph{end-to-end tier} fine-tunes real models on corpora with injected canaries,
unlearns them with each subject method, and certifies the result, so that the abstract
null meets an actual training pipeline. The \emph{benchmark tier} repeats the
end-to-end protocol on the field's standard testbeds, TOFU and MUSE-News, across three
architectures.
\DIFaddbegin 

\DIFaddend Unless noted we use \DIFdelbegin \DIFdel{$\eps = 0.2$, }\DIFdelend $\alpha = 0.05$, two-sided certification, the sign-based
revocation arm, \DIFaddbegin \DIFadd{the finite-cohort certificate target, }\DIFaddend LoRA fine-tuning of rank \DIFdelbegin \DIFdel{sixteen}\DIFdelend \DIFaddbegin \DIFadd{$16$,
$Q = 2$ query wrappers}\DIFaddend , and between $384$ and $640$ canary pairs\DIFaddbegin \DIFadd{; the full
hyperparameter table is Appendix~\ref{app:repro}, and the headline tolerance is
$\eps = 0.2$ with the complete sweep over $\eps \in \{0.05, 0.1, 0.2\}$ reported in
Table~\ref{tab:epssweep}}\DIFaddend . The entire study ran on free, ephemeral compute---Colab and
Kaggle GPU sessions, Colab CPU sessions, \DIFaddbegin \DIFadd{an Apple-silicon laptop, }\DIFaddend and a four-core
container---coordinated by a released fault-tolerant scheduler; because every training
stage checkpoints and every verification is saved, a reclaimed session costs at most
\DIFdelbegin \DIFdel{a hundred }\DIFdelend \DIFaddbegin \DIFadd{$100$ }\DIFaddend optimiser steps.

\subsection{\DIFdelbegin \DIFdel{Validity under adversarial optional stopping}\DIFdelend \DIFaddbegin \DIFadd{Which null is actually controlled}\DIFaddend }\DIFdelbegin %DIFDELCMD < \label{sec:exp-validity}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \label{sec:exp-dependence}

\DIFadd{We begin with the question that reorganised this version of the paper. Signs from all
canaries pass through one jointly trained and unlearned model, so they are neither
independent nor identically distributed across templates and repetition strata. Does
that matter? Table~\ref{tab:dependence} answers it directly. We generate sign streams
whose }\emph{\DIFadd{marginal}} \DIFadd{sign probability is exactly $p_0 = 0.55$ under four regimes and
ask how often each certificate process issues.
}\DIFaddend 

The \DIFdelbegin \DIFdel{first thing to establish is that VOUCH does what it promises: it }\DIFdelend \DIFaddbegin \DIFadd{answer is that it matters, and in the direction least convenient for the earlier
analysis. When signs are independent, the fixed-boundary process of the
earlier draft holds its level at $0.034$, as its stated theorem claims. Heterogeneity
across strata alone does not break it either ($0.026$), because shuffling the reveal
order leaves the realised cohort mean tightly concentrated. But once the model itself
carries a shared random effect---so that a given training run either leaks a lot or
leaks little---the realised cohort advantage spreads out (its standard deviation rises
from $0.031$ to $0.50$) and the fixed-boundary process issues under the marginal null
in $38.9\%$ and $48.1\%$ of runs, eight- and ten-fold breaches of $\alpha = 0.05$. The
without-replacement process, by contrast, controls the realised-cohort error at or
below $0.020$ in every regime, exactly as Theorem~\ref{thm:cert} requires.
}

\DIFadd{Two readings of that table are available and we want to be clear which one we take.
One could say the marginal null is the target and the procedure fails; we say instead
that the marginal null is the wrong target, because it averages over training runs the
auditor is not auditing, and a certificate should concern the deployed model. The
column marked $\dagger$ makes the distinction concrete: those issuances are not errors
under the cohort claim, because on those runs the realised advantage genuinely sits
below the tolerance. What we do concede without qualification is that the earlier
draft claimed the marginal statement while proving the conditional one, and that the
gap is not academic.
}

\DIFadd{Table~\ref{tab:cohortnull} verifies the corrected theorem directly, on fixed sign
vectors sitting at the least favourable feasible point of the cohort null and carrying
deliberately adversarial internal structure---all successes contiguous, spread evenly,
or split into four strata with rates from $0.2$ to $0.9$. Under peeking after every
pair the realised error never exceeds $0.035$ against a nominal $0.05$, for every
structure, both tolerances and both cohort sizes. This is the guarantee that needs no
assumption at all.
}

\DIFadd{Is the dependence real on real models? Yes. Across the benchmark runs a chi-square test
of sign-rate homogeneity across repetition strata rejects at the $5\%$ level in $14\%$
of all runs---and in $24$ of the $32$ un-unlearned runs, where there is leakage to be
heterogeneous about. Heterogeneity across canary templates rejects in $8\%$. The
regime of Table~\ref{tab:dependence} is therefore the empirically relevant one.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Which null is controlled. Sign streams with }\emph{\DIFaddFL{marginal}} \DIFaddFL{mean
$p_0 = 0.55$ ($\eps = 0.1$), $1{,}024$ pairs, $2{,}000$ seeds, peeking after every
pair. ``marginal'' is the issuance rate under the marginal null; ``cohort'' restricts
to runs whose realised cohort advantage genuinely violates the tolerance, the event
Theorem~\ref{thm:cert} controls. Bold marks a breach of $\alpha = 0.05$.
$^{\dagger}$These issuances are not errors under the cohort claim.}}
\label{tab:dependence}
\input{tables/dependence}
\end{table}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Direct verification of Theorem~\ref{thm:cert}: realised type-I error on fixed
sign vectors at the least favourable feasible point of the cohort null, revealed in a
uniformly random order with peeking after every pair ($2{,}000$ seeds,
$\alpha = 0.05$). ``ours'' is the without-replacement process, ``fixed'' the
fixed-boundary one.}}
\label{tab:cohortnull}
\input{tables/cohortnull}
\end{table}

\subsection{\DIFadd{Validity under adversarial optional stopping}}\label{sec:exp-validity}

\DIFadd{With the target settled we can ask whether VOUCH }\DIFaddend holds its error level \DIFdelbegin \DIFdel{even }\DIFdelend when an
adversary peeks after every observation and stops at the first crossing. We simulate
exactly this attack. Table~\ref{tab:validity} reports the realised type-I error over
\DIFdelbegin \DIFdel{two thousand seeds, and the picture is unambiguous---VOUCH
}\DIFdelend \DIFaddbegin \DIFadd{$2{,}000$ seeds: VOUCH }\DIFaddend stays below the nominal level for both the certificate and the
revocation arm at both $\alpha=0.05$ and $\alpha=0.01$, whereas a fixed-sample binomial
test used in the same peeking fashion inflates its error to $0.35$, \DIFdelbegin \DIFdel{seven }\DIFdelend \DIFaddbegin \DIFadd{$7$ }\DIFaddend times the
nominal rate. The calibration curve in Figure~\ref{fig:validity} confirms that the
conservatism holds across the whole range of $\alpha$. When we run the complete
protocol---three correlated scores, the confidence sequence, and both arms
together---the false revocation rate is $0.025$ and the per-score confidence sequence
covers the truth with uniform-in-time probability $0.971$ against the nominal $0.95$;
and when there is genuine memorisation to find ($\Delta \approx 0.36$), the revocation
arm fires in every run at a median of \DIFdelbegin \DIFdel{forty-four }\DIFdelend \DIFaddbegin \DIFadd{$44$ }\DIFaddend pairs, with no false certificate ever
issued.
\DIFdelbegin \DIFdel{Validity,
in short, is not merely asymptotic but exact and finite-sample, exactly as
Theorem~\ref{thm:null} predicts}\DIFdelend \DIFaddbegin 

\DIFadd{One caveat on reading these numbers, which a reviewer rightly pressed. A realised error
of $0.031$ against a nominal $0.05$ is what a supermartingale bound is }\emph{\DIFadd{supposed}}
\DIFadd{to produce; it is evidence of validity, not of tightness, and the same conservatism
costs power, which is the price Table~\ref{tab:baselines} quantifies. We do not claim
the bound is tight and the gap is not a defect}\DIFaddend .

\begin{table}[t]
\centering\small
\caption{Type-I error over \DIFdelbeginFL \DIFdelFL{2}%DIFDELCMD < {%%%
\DIFdelFL{,}%DIFDELCMD < }%%%
\DIFdelFL{000 }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$2{,}000$ }\DIFaddendFL seeds with peeking after every pair (\DIFdelbeginFL \DIFdelFL{1}%DIFDELCMD < {%%%
\DIFdelFL{,}%DIFDELCMD < }%%%
\DIFdelFL{024
}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$1{,}024$
}\DIFaddendFL pairs). Bold marks a procedure whose realised error exceeds its nominal level.}
\label{tab:validity}
\input{tables/validity}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig1_validity.pdf}
\caption{\textbf{Anytime validity.} Left: the realised false-certification rate lies
below the nominal level at every $\alpha$ under adversarial optional stopping. Right:
a fixed-$n$ binomial test used with peeking inflates its error sevenfold; VOUCH does
not.}
\label{fig:validity}
\end{figure}

\subsection{Sound composition across scores}\label{sec:exp-soundness}

We next confirm the composition result of Theorem~\ref{thm:cert} and the failure it
warns against. Constructing a composite null in which one of three scores sits exactly
at the boundary while the other two are clean, we compare the per-score-minimum
certificate that VOUCH uses against the adaptive-mixture certificate that seems
natural. The difference is \DIFdelbegin \DIFdel{stark and is }\DIFdelend reported in Table~\ref{tab:soundness}: the mixture arm
certifies the leaking model in $99.6\%$ of runs---it is not merely loose but
anytime-invalid, as Remark~\ref{rem:mixture} explains---while the per-score minimum
holds at $0.7\%$, comfortably below $\alpha$. \DIFdelbegin \DIFdel{This is the empirical reason VOUCH
requires every score's process to concur rather than betting on their average}\DIFdelend \DIFaddbegin \DIFadd{We want to be precise about what this
number is and is not: the mixture arm is }\emph{\DIFadd{our own}} \DIFadd{earlier design, evaluated
against an adversarially constructed composite null. It is not a published method and
the figure is not a refutation of anyone else's work}\DIFaddend .

\begin{table}[t]
\centering\small
\caption{False certification under a composite null (score $A$ at the boundary
$p_0=0.55$, scores $B$ and $C$ clean; \DIFdelbeginFL \DIFdelFL{2}%DIFDELCMD < {%%%
\DIFdelFL{,}%DIFDELCMD < }%%%
\DIFdelFL{048 }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$2{,}048$ }\DIFaddendFL pairs, $\alpha=0.05$). \DIFaddbeginFL \DIFaddFL{The mixture arm
is our own earlier design, not a published method.}\DIFaddendFL }
\label{tab:soundness}
\input{tables/soundness}
\end{table}

\subsection{\DIFdelbegin \DIFdel{Power and tightness}\DIFdelend \DIFaddbegin \DIFadd{Comparison against valid sequential procedures}\DIFaddend }\DIFdelbegin %DIFDELCMD < \label{sec:exp-power}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \label{sec:exp-baselines}
\DIFaddend 

\DIFdelbegin \DIFdel{Having established validity, we ask what it costs in sample size}\DIFdelend \DIFaddbegin \DIFadd{A fixed-sample binomial test inspected after every pair is a useful illustration of
why naive peeking is invalid, but it is not a serious competitor. The right question is
what anytime validity costs relative to procedures that are }\emph{\DIFadd{also}} \DIFadd{valid, so we
add four: exact group-sequential $\alpha$-spending with Pocock and O'Brien--Fleming
boundaries, calibrated by dynamic programming over the exact binomial lattice rather
than by a normal approximation; a truncated Beta$(\tfrac12,\tfrac12)$-mixture
(Jeffreys) e-process; a Robbins normal-mixture e-process; and a fixed-$n$ binomial test
at a pre-committed sample size. All see the same sign stream and the same query
budget. Table~\ref{tab:baselines} reports the result.
}

\DIFadd{Read the two type-I columns together, because they measure different things and the
distinction is the same one Section~\ref{sec:exp-dependence} draws. Against the
}\emph{\DIFadd{marginal}} \DIFadd{null every super-population procedure holds its level to within Monte
Carlo error at $2{,}000$ seeds ($0.030$ to $0.053$, standard error $\approx 0.005$),
and VOUCH's cohort arm does not---it issues in half of runs, because half of those
cohorts have a realised advantage genuinely below the tolerance. Against the
}\emph{\DIFadd{cohort}} \DIFadd{null every procedure holds its level, and the two group-sequential
boundaries and the fixed-$n$ test are markedly conservative there ($0.004$}\DIFaddend , \DIFdelbegin \DIFdel{and the answer is :
close to
nothing beyond the information-theoretic minimum.
}\DIFdelend \DIFaddbegin \DIFadd{$0.000$,
$0.000$) precisely because they were built for the other target. Neither column alone
is a fair scoreboard, which is why we print both. On power and cost the ordering is
informative. Against exact unlearning at $\eps = 0.1$ and a budget of $1{,}536$ pairs,
VOUCH on the cohort target certifies every run using a mean of $523$ queries; Pocock
reaches $0.971$ using $645$; O'Brien--Fleming $0.985$ using $844$; the two mixture
e-processes trail at $0.77$--$0.78$; and the fixed-$n$ test attains $0.989$ but spends
the entire budget of $1{,}536$ queries on every run, because a fixed-$n$ test cannot
stop early by construction. The honest summary is that anytime validity is close to
free here: against the strongest valid sequential comparator VOUCH gives up nothing in
power and saves about a fifth of the queries, and it matches the fixed-$n$ test's power
to within a rounding of one point ($0.9995$ against $0.9885$) at a threefold reduction
in verifier cost. The comparison also
isolates how much of VOUCH's advantage comes from retargeting: on the
super-population target, at the same budget, power falls from $1.000$ to $0.882$ and
mean queries rise from $523$ to $747$. That gap is the price of the stronger claim, and
it is the same gap Proposition~\ref{prop:transfer} charges in tolerance.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Valid sequential comparators on the same sign stream and query budget
($2{,}000$ seeds, $\alpha = 0.05$). ``type I'' is measured at the least favourable
null; for the cohort target it is restricted to runs whose realised cohort advantage
violates the tolerance. ``power'' and ``queries'' are measured under exact unlearning.}}
\label{tab:baselines}
\input{tables/baselines}
\end{table}

\subsection{\DIFadd{Power, tightness, and the cost of a horizon}}\label{sec:exp-power}

\DIFaddend Table~\ref{tab:power} \DIFdelbegin \DIFdel{shows
that the median certification time tracks }\DIFdelend \DIFaddbegin \DIFadd{reports certification time against }\DIFaddend the Kullback--Leibler \DIFdelbegin \DIFdel{bound
$\log(1/\alpha)/\KL$ within fifteen percent across the whole $\eps$ grid, and
Figure~\ref{fig:power} plots the same relationship together with the
revocation
detection time.
}\DIFdelend \DIFaddbegin \DIFadd{limit.
Two corrections to the earlier version are folded in. First, the theoretical column now
follows the corrected expansion \eqref{eq:klexp}; the numbers themselves are unchanged
because they were always computed from the exact divergence, but the accompanying
sample-size rule of thumb is $2\log(1/\alpha)/\eps^2$, not $8\log(1/\alpha)/\eps^2$.
Second, and more consequentially, the earlier table's starred entries were medians
taken over the runs that happened to issue within a $20{,}000$-pair horizon, which is
a selection-biased statistic: at $\eps = 0.02$ fully $45\%$ of runs are censored, and
conditioning on the fast half produced a median of $11{,}189$ that sat }\emph{\DIFadd{below}} \DIFadd{the
information-theoretic limit of $14{,}976$, an impossibility that should have been a
red flag. Reporting the censoring rate and the Kaplan--Meier median removes the
artefact: the corrected median is $18{,}377$, comfortably above the limit, and at
$\alpha = 0.01$ censoring reaches $72\%$ and the survival curve never reaches one half,
so no median exists and we report none.
}

\DIFadd{The finite-cohort column is faster than the super-population one at every tolerance,
and the reason is structural rather than statistical: the cohort claim resolves
deterministically once the cohort is exhausted, so its sample complexity is capped by
$m$ and it may legitimately sit below a bound that governs the super-population
parameter. Readers should not read the cohort column as beating an information limit;
it is answering an easier question, and Proposition~\ref{prop:transfer} prices the
difference.
}

\DIFaddend Two-sided certification, which we adopt because of the over-forgetting phenomenon
documented below, costs roughly a factor of $1.7$ in pairs---$283$ against $166$ at
$\eps=0.2$---a price we consider well worth paying for a certificate that cannot be
defeated by driving scores below chance. On the detection side the revocation arm finds
an advantage of $\Delta=0.4$ in a median of \DIFdelbegin \DIFdel{forty-eight }\DIFdelend \DIFaddbegin \DIFadd{$48$ }\DIFaddend pairs and $\Delta=0.1$ in \DIFdelbegin \DIFdel{six hundred and ninety}\DIFdelend \DIFaddbegin \DIFadd{$690$}\DIFaddend . The
confidence sequence tightens at the expected $O(\sqrt{\log\log t / t})$ rate
(Figure~\ref{fig:tightness}): under exact unlearning its upper bound on $\Delta$ falls
from $0.26$ at \DIFdelbegin \DIFdel{a hundred and twenty-eight
}\DIFdelend \DIFaddbegin \DIFadd{$128$ }\DIFaddend pairs to $0.10$ at \DIFdelbegin \DIFdel{a thousand. It is worth being candid about the one place where a
classical test wins: at a single }\emph{\DIFdel{pre-committed}} %DIFAUXCMD
\DIFdel{sample size a permutation or
binomial test is somewhat more powerful, and on one benchmark seed such a test rejects
where VOUCH remains undetermined. This is the unavoidable price of validity at
}\emph{\DIFdel{every}} %DIFAUXCMD
\DIFdel{sample size at once, and Theorem~\ref{thm:power} guarantees the gap to the
best possible valid procedure is only a constant factor}\DIFdelend \DIFaddbegin \DIFadd{$1{,}000$}\DIFaddend .

\begin{table}[t]
\centering\small
\caption{Median pairs to certification under exact unlearning\DIFdelbeginFL \DIFdelFL{against }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{, $\alpha = 0.05$,
horizon $20{,}000$ pairs, $500$ seeds per cell. For }\DIFaddendFL the \DIFdelbeginFL \DIFdelFL{KL }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{super-population target the
median over issued runs is selection-biased where censoring is heavy, so the
Kaplan--Meier median and the censoring rate are given. The $\mathrm{KL}$ column is the
information limit for the }\emph{\DIFaddFL{super-population}} \DIFaddFL{target only: the cohort claim
resolves once the cohort is exhausted, so its time is capped by $m$ and may legitimately
fall below that }\DIFaddendFL limit. \DIFdelbeginFL \DIFdelFL{Starred entries are medians over }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{At $\alpha = 0.01$ (not shown) censoring at $\eps = 0.02$ reaches
$72\%$ and }\DIFaddendFL the \DIFdelbeginFL \DIFdelFL{runs issued within a 20}%DIFDELCMD < {%%%
\DIFdelendFL \DIFaddbeginFL \DIFaddFL{survival curve never reaches one half}\DIFaddendFL , \DIFdelbeginFL %DIFDELCMD < }%%%
\DIFdelFL{000-pair horizon}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{so no median exists}\DIFaddendFL .}
\label{tab:power}
\input{tables/power}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig3_power.pdf}
\caption{\textbf{Power.} Left: median certification time versus $\eps$, against the
information limit $\log(1/\alpha)/\KL$. Right: revocation detection time versus the
true residual advantage.}
\label{fig:power}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=.5\linewidth]{fig4_tightness.pdf}
\caption{\textbf{Tightness.} The anytime confidence-sequence upper bound on $\Delta$
shrinks with the number of pairs observed, under $\Delta=0$ and $\Delta=0.2$.}
\label{fig:tightness}
\end{figure}

\subsection{Streaming deletion waves}\label{sec:exp-streaming}

Because deletion requests arrive continually, we test the composition rules of
Theorem~\ref{thm:streaming} on histories of \DIFdelbegin \DIFdel{ten }\DIFdelend \DIFaddbegin \DIFadd{$10$ }\DIFaddend waves, and Table~\ref{tab:streaming}
bears out both halves of the theorem. When every wave is exactly unlearned, the
$\alpha$-spending per-wave alarm never once fires falsely and the global product alarm
raises a false alarm in \DIFdelbegin \DIFdel{only two runs per thousand}\DIFdelend \DIFaddbegin \DIFadd{$2$ of $1{,}000$ histories}\DIFaddend . When a single wave is genuinely bad
($\Delta = 0.25$), its own alarm catches it in $98\%$ of histories and---crucially---the
consensus certificate is \emph{never} falsely issued for the \DIFdelbegin \DIFdel{history}\DIFdelend \DIFaddbegin \DIFadd{history---whereas
multiplying certificate e-values falsely certifies that history in }\emph{\DIFadd{every}} \DIFadd{run
(Table~\ref{tab:certprod})}\DIFaddend , which is \DIFdelbegin \DIFdel{exactly
the failure that multiplying certificate e-values would produce}\DIFdelend \DIFaddbegin \DIFadd{the failure Theorem~\ref{thm:streaming}(a)
predicts. One cost of consensus deserves stating, since it is a headline rule and the number is
not flattering: requiring }\emph{\DIFadd{every}} \DIFadd{wave to issue means a history of ten
}\emph{\DIFadd{perfectly clean}} \DIFadd{waves earns a history-wide certificate only $48\%$ of the time
at $512$ pairs per wave, because each wave independently risks landing in the
undetermined band. Consensus buys soundness across waves and pays for it in exactly
this way, and a provider auditing a long deletion history should size per-wave cohorts
with $K$ in mind rather than sizing one wave and multiplying}\DIFaddend . The most telling case
is the one the product alarm was designed for: \DIFdelbegin \DIFdel{ten }\DIFdelend \DIFaddbegin \DIFadd{$10$ }\DIFaddend waves each leaking only weakly
($\Delta = 0.06$), so weakly that a per-wave test detects them just $14\%$ of the time,
are nonetheless caught by the accumulated product alarm in $62\%$ of histories, at a
median firing wave of three. Distributed leakage that hides beneath every individual
wave's threshold is thus surfaced by composition---a capability no fixed-sample audit
possesses.

\begin{table}[t]
\centering\small
\DIFaddbeginFL \caption{\DIFaddFL{The negative half of Theorem~\ref{thm:streaming}(a), measured: histories of
$K{=}10$ waves ($512$ pairs each, $500$ histories) containing one genuinely bad wave
($\Delta = 0.25 > \eps = 0.2$), restricted to the histories whose bad wave's
}\emph{\DIFaddFL{realised}} \DIFaddFL{cohort advantage does violate the tolerance. Multiplying certificate
e-values---the instinctive move---falsely certifies the whole history every time.}}
\label{tab:certprod}
\input{tables/certprod}
\end{table}

\begin{table}[t]
\centering\small
\DIFaddendFL \caption{Streaming composition over $K{=}10$ waves ($512$ pairs each, $500$ histories).
The dagger excludes the genuinely bad wave. Bold marks the two payoffs: no false
history-wide certificate, and distributed weak leakage caught only by the product
alarm.}
\label{tab:streaming}
\input{tables/streaming}
\end{table}

\subsection{End-to-end certification on real models}\label{sec:exp-e2e}

We now let the abstract null meet a genuine training pipeline. Table~\ref{tab:gpt2v2}
reports GPT-2 fine-tuned on a corpus with $640$ injected canary pairs and then
subjected to each unlearning method. The pattern is \DIFdelbegin \DIFdel{exactly }\DIFdelend what a working certificate should
produce: the un-unlearned model is revoked in every seed within about \DIFdelbegin \DIFdel{eleven
}\DIFdelend \DIFaddbegin \DIFadd{$11$ }\DIFaddend pairs, its
canary loss gap averaging $2.2$ nats; \DIFdelbegin \DIFdel{retraining-from-scratch }\DIFdelend \DIFaddbegin \DIFadd{retraining from a fresh adapter }\DIFaddend and all three
genuine unlearning methods earn certificates in every seed; and the deliberately
weakened NPO together with the post-relearning probe \DIFdelbegin \DIFdel{land in the
honest undetermined band, their leakage signal elevated but not yet conclusive.
}\DIFdelend \DIFaddbegin \DIFadd{sit at the boundary of the
undetermined band.
}

\DIFaddend One episode during development \DIFdelbegin \DIFdel{is worth recounting because it }\DIFdelend illustrates what statistical certification buys beyond a
pass/fail label. An early version of our NPO implementation carried a sign error that
caused its loss to optimise \emph{toward} memorisation rather than away from it; the
revocation arm flagged every such run within roughly \DIFdelbegin \DIFdel{eleven }\DIFdelend \DIFaddbegin \DIFadd{$11$ }\DIFaddend pairs, before we had noticed
the bug ourselves. A certificate that can catch a defect in the unlearning \emph{code},
not merely in its outcome, is doing more than a descriptive metric \DIFdelbegin \DIFdel{ever could}\DIFdelend \DIFaddbegin \DIFadd{can}\DIFaddend . A
CPU-affordable tier built around a small transformer, where \DIFdelbegin \DIFdel{retraining-from-scratch }\DIFdelend \DIFaddbegin \DIFadd{retraining }\DIFaddend is cheap enough
to serve as ground truth, reproduces every one of these qualitative outcomes.

\begin{table}[t]
\centering\small
\caption{GPT-2 end-to-end over \DIFdelbeginFL \DIFdelFL{3 }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$2$ }\DIFaddendFL seeds ($640$ pairs, two-sided $\eps=0.2$\DIFaddbeginFL \DIFaddFL{, cohort
target}\DIFaddendFL ): per-seed verdicts (\vI\ issued, \vR\ revoked, \vU\ undetermined), the mean
confidence-sequence upper bound on $\Delta$, and the mean canary \DIFdelbeginFL \DIFdelFL{loss }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{score }\DIFaddendFL gap $\bar D$
\DIFaddbeginFL \DIFaddFL{with its across-seed standard deviation. Seeds $1$ and $2$ are shown; the seed-$0$
run of this tier predates per-pair logging and cannot be replayed under the cohort
target, so it is omitted rather than mixed in from the earlier analysis}\DIFaddendFL .}
\label{tab:gpt2v2}
\input{tables/gpt2v2}
\end{table}

\subsection{The standard benchmarks: TOFU and MUSE-News}\label{sec:exp-bench}

The central empirical test is whether VOUCH behaves correctly on the benchmarks the
community actually uses. We inject canaries in each benchmark's native format---
question--answer pairs for TOFU, record-style sentences for MUSE-News---into its
fine-tuning mixture, and we track held-out utility for every subject.
Table~\ref{tab:bench} and Figures~\ref{fig:tofu} and \ref{fig:muse} report verdicts
across two benchmarks, three architectures spanning 124M to 1.4B parameters, and
\DIFdelbegin \DIFdel{three seeds each, and five findings emerge
that together make the case for the framework}\DIFdelend \DIFaddbegin \DIFadd{$3$ seeds each. Table~\ref{tab:counts} gives every verdict count with a
Clopper--Pearson interval, because at $n = 18$ these counts carry wide uncertainty that a bare
fraction does not convey}\DIFaddend .

\DIFdelbegin \DIFdel{First, }\DIFdelend \emph{\DIFdelbegin \DIFdel{detection }\DIFdelend \DIFaddbegin \DIFadd{Detection }\DIFaddend works\DIFaddbegin \DIFadd{, at the stated tolerance.}\DIFaddend } \DIFdelbegin \DIFdel{: the }\DIFdelend \DIFaddbegin \DIFadd{The }\DIFaddend un-unlearned model is revoked in
\DIFdelbegin \DIFdel{sixteen of eighteen }\DIFdelend \DIFaddbegin \DIFadd{$16$ of $18$ }\DIFaddend benchmark runs under the default sign \DIFdelbegin \DIFdel{arm---all }\DIFdelend \DIFaddbegin \DIFadd{arm---$95\%$ CI $[0.65, 0.99]$---all
}\DIFaddend six Phi-1.5 runs \DIFdelbegin \DIFdel{emphatically so, }\DIFdelend at log-wealth between \DIFdelbegin \DIFdel{$166$ and $182$---with the remaining two honest
undetermineds rather than false certificates, and the magnitude-aware arm of
Proposition~\ref{prop:magnitude} lifts detection to eighteen of eighteen at the cost
of the occasional transient crossing.
The same arm also catches a genuinely }\emph{\DIFdel{failed}} %DIFAUXCMD
\DIFdel{unlearning run in the wild: on one MUSE/Phi-1.5 seed the
sign arm
revokes GradDiff outright, exposing residual memorisation in a method that a forget-metric evaluation would have passed. Second, }\emph{\DIFdel{exact unlearning
certifies}}%DIFAUXCMD
\DIFdel{: retraining-from-scratch is issued on seventeen of eighteen runs, and the single miss is a 384-pair cohort that lands at a log-wealth of $2.98$ against the $3.00$ threshold and issues cleanly once we raise the
cohort to $512$ pairs---the
cohort-size guidance of }\DIFdelend \DIFaddbegin \DIFadd{$149$ and $186$. The two
remaining runs are the two weakest-leaking GPT-2 cells, and here we must report
something the earlier version did not surface: under the corrected, more powerful
cohort target those two runs are not merely undetermined, they are }\emph{\DIFadd{certified}}\DIFadd{.
Their realised advantages are $0.062$ and $0.078$, genuinely below the
$\eps = 0.2$ tolerance, so a certificate is the correct verdict for the claim being
made. This is the clearest possible illustration of why the tolerance is not a free
parameter: at $\eps = 0.2$ a model that was never unlearned at all can pass, because
$\eps = 0.2$ permits an adversary in the declared class to win $60\%$ of the time.
We return to how $\eps$ should be chosen at the end of this subsection.
}

\emph{\DIFadd{Exact unlearning certifies.}} \DIFadd{Retraining from a fresh adapter is issued on all
$18$ runs at $\eps = 0.2$ and on all $18$ at $\eps = 0.1$; at $\eps = 0.05$ it is
issued on $12$ of $18$ (Table~\ref{tab:epssweep}). This is the end-to-end result at a
tight tolerance that was missing from the previous version, and it comes with a
caveat that must travel with it: it is a }\emph{\DIFadd{cohort}} \DIFadd{certificate. On the
super-population target, no run certifies at $\eps = 0.05$ at these cohort sizes, and
}\DIFaddend Theorem~\ref{thm:power} \DIFdelbegin \DIFdel{, confirmed on real data. Third, and most useful in practice, the certificate/utility pairing exposes
}\DIFdelend \DIFaddbegin \DIFadd{says none should---$\eps=0.05$ needs roughly $2{,}400$ pairs
and we plant at most $640$. Both columns are in Table~\ref{tab:epssweep} and we
recommend reading them together.
}

\DIFaddend \emph{\DIFdelbegin \DIFdel{forgetting-by-lobotomy}\DIFdelend \DIFaddbegin \DIFadd{The certificate/utility pairing exposes destructive unlearning.}\DIFaddend } \DIFdelbegin \DIFdel{: gradient }\DIFdelend \DIFaddbegin \DIFadd{Gradient }\DIFaddend ascent
earns forgetting certificates while its held-out loss balloons to between $58$ and
$286$ nats \DIFdelbegin \DIFdel{against }\DIFdelend \DIFaddbegin \DIFadd{per seed---the cell means in Table~\ref{tab:bench} run from $60$ to
$179$---against }\DIFaddend retraining's $1.5$ to $3.4$\DIFdelbegin \DIFdel{, a catastrophe }\DIFdelend \DIFaddbegin \DIFadd{: a collapse }\DIFaddend that no forget-only metric
would reveal \DIFdelbegin \DIFdel{but that our utility column makes impossible to miss.
Fourth, }\emph{\DIFdel{two-sided certification matters}}%DIFAUXCMD
\DIFdel{: on }\DIFdelend \DIFaddbegin \DIFadd{and that the paired utility column exposes directly.
}

\DIFadd{A reviewer observed, correctly, that held-out NLL identifies severe collapse but does
not establish general utility, so we added the benchmark's own forget and retain
readings and a general-knowledge probe (Table~\ref{tab:metrics}). They sharpen rather
than soften the picture. Gradient ascent reaches a forget NLL of $94.3$ and a retain
NLL of $93.7$ with ROUGE-L recall of exactly $0.00$ on both splits: it has not
forgotten the forget set, it has stopped producing language. GradDiff is subtler and
more interesting---its retain NLL is $6.8$ but its capability NLL is $19.4$, so the
damage is nearly $3$ times larger off the retain distribution than on it, which a
retain-only utility reading would have missed entirely. SimNPO \mbox{%DIFAUXCMD
\citep{fan2025simnpo}}\hskip0pt%DIFAUXCMD
,
added at revision, achieves the widest forget/retain separation of any subject that
remains fluent ($6.20$ against $2.62$, versus NPO's $5.50$ against $2.60$) at a
capability NLL of $4.17$ against retraining's $3.23$, and is the best-behaved of the
approximate methods on this axis.
}

\DIFadd{One cell of that table is deliberately blank. For TOFU the P1 relearn corpus
}\emph{\DIFadd{is}} \DIFadd{the capability probe set (}\texttt{\DIFadd{world\_facts}}\DIFadd{), so a capability reading
after P1 measures training on the probe rather than retained ability; it reads $1.46$,
better than the retrained reference, which is an artefact and not a finding. We report
it as unavailable rather than quietly printing a flattering number, and note that
disentangling the two would require a probe set disjoint from the relearn corpus.
}

\emph{\DIFadd{Two-sided certification matters, though not on these runs.}} \DIFadd{On }\DIFaddend several TOFU
seeds gradient ascent, GradDiff \DIFdelbegin \DIFdel{,
}\DIFdelend and NPO push the canaries \emph{below} chance, with
mean \DIFdelbegin \DIFdel{loss }\DIFdelend \DIFaddbegin \DIFadd{score }\DIFaddend gaps as negative as \DIFdelbegin \DIFdel{$-0.42$; the
}\DIFdelend \DIFaddbegin \DIFadd{$-0.54$ (GradDiff on TOFU/Pythia) and $-0.51$ (NPO on
TOFU/Phi-1.5). We should be exact about what follows from that, because an earlier
draft was not: at $\eps = 0.2$ these gaps are still inside the tolerance, so the
two-sided arm issues here too and nothing is withheld on any TOFU cell. What the
two-sided arm buys is that the }\emph{\DIFadd{same}} \DIFadd{runs would be certified by a }\DIFaddend one-sided test
\DIFdelbegin \DIFdel{of our original design would certify these over-forgotten
models, and only }\DIFdelend \DIFaddbegin \DIFadd{at }\emph{\DIFadd{any}} \DIFadd{tolerance, however tight, because a one-sided test cannot see below
chance at all; }\DIFaddend the two-sided arm \DIFdelbegin \DIFdel{correctly withholds. Fifth, }\emph{\DIFdel{recoverability is
measured rather than argued}}%DIFAUXCMD
\DIFdel{: on TOFU with GPT-2 and Pythia the relearn probe drives
NPO from issued to undetermined and preferentially resurfaces the most-repeated
canaries; on TOFU with the
larger Phi-1.5 and on MUSE with }\DIFdelend \DIFaddbegin \DIFadd{prices them correctly, and at $\eps = 0.05$ $5$ of
the $6$ retrain runs that fail to issue are held back by the }\emph{\DIFadd{lower}} \DIFadd{arm, not the
upper one---that is, by over-forgetting rather than by residual memorisation. The behavioural
signature is the one \mbox{%DIFAUXCMD
\citet{yuan2025closer} }\hskip0pt%DIFAUXCMD
and \mbox{%DIFAUXCMD
\citet{li2026beliefs} }\hskip0pt%DIFAUXCMD
describe from
the optimisation side, showing up in an audit.
}

\emph{\DIFadd{Recoverability is measured rather than argued.}} \DIFadd{At $\eps = 0.2$ on the cohort
target the relearn probe leaves NPO's certificate intact on all three TOFU
architectures and on MUSE/Pythia; it }\emph{\DIFadd{revokes}} \DIFadd{on one MUSE/}\DIFaddend GPT-2 \DIFdelbegin \DIFdel{and Pythia the same probe leaves the certificate intact ; }\DIFdelend \DIFaddbegin \DIFadd{seed }\DIFaddend and on one
MUSE/Phi-1.5 seed\DIFaddbegin \DIFadd{. So on this benchmark tier the probe changes the verdict in exactly
$2$ of $18$ runs, both on MUSE-News---the more open-domain corpus, and the one where
the public relearn set sits closest in distribution to }\DIFaddend the \DIFdelbegin \DIFdel{probe goes further
and }\emph{\DIFdel{revokes}}%DIFAUXCMD
\DIFdel{---relearning on public data resurfaces the supposedly forgotten
canaries strongly enough to trigger the alarm}\DIFdelend \DIFaddbegin \DIFadd{forget set}\DIFaddend . Recoverability
is benchmark- and model-dependent, \DIFaddbegin \DIFadd{exactly as \mbox{%DIFAUXCMD
\citet{hu2025jogging} }\hskip0pt%DIFAUXCMD
report, }\DIFaddend and VOUCH
quantifies the dependence rather than assuming it away. \DIFaddbegin \DIFadd{But we should be plain that at
$\eps = 0.2$ the probe is close to inert on our runs: $16$ of $18$ certificates
survive it, and the two that do not are revocations rather than downgrades. A relearn
probe becomes informative as the tolerance tightens, which is another reason not to
read $\eps = 0.2$ as a recommendation.
}

\DIFaddend Underlying all of this is a clean dose--response signal\DIFdelbegin \DIFdel{: memorisation rises }\DIFdelend \DIFaddbegin \DIFadd{, measured on the
}\emph{\DIFadd{un-unlearned}} \DIFadd{models, where there is memorisation to dose. Mean score gaps rise
}\DIFaddend monotonically with training-time \DIFdelbegin \DIFdel{repetition (mean loss gaps
of $1.2$, $1.5$, $2.7$, and $4.1$ }\DIFdelend \DIFaddbegin \DIFadd{repetition---$+0.46$, $+0.78$, $+1.54$ and $+2.35$
}\DIFaddend nats at repetition factors \DIFdelbegin \DIFdel{one through eight), which }\DIFdelend \DIFaddbegin \DIFadd{$1$, $2$, $4$ and $8$, averaged over the six
benchmark/model cells---and so do the realised advantages, $+0.22$, $+0.35$, $+0.59$
and $+0.73$. This }\DIFaddend is the curve that calibrates
the extrapolation from canaries to organic data\DIFaddbegin \DIFadd{, and its shape is also the honest
limit of that extrapolation: the $r = 1$ stratum, the one closest to how an organic
record appears, is where the signal is weakest}\DIFaddend . Figure~\ref{fig:example} makes \DIFdelbegin \DIFdel{all of this }\DIFdelend \DIFaddbegin \DIFadd{the
whole mechanism }\DIFaddend concrete on a single real canary pair\DIFdelbegin \DIFdel{from the TOFU/Phi-1.5 pipeline}\DIFdelend \DIFaddbegin \DIFadd{.
}

\emph{\DIFadd{A certificate and its confidence sequence can disagree, and often do.}} \DIFadd{A reader
comparing Tables~\ref{tab:bench} and \ref{tab:descriptive} will notice issued
certificates sitting beside confidence-sequence upper bounds that exceed the very
tolerance they certify: across the benchmark tier this happens in $46$ of $107$ issued
runs, with $U_t$ reaching $0.232$ against $\eps = 0.2$. This is not an inconsistency in
the sense of one object being wrong, and it is worth explaining because it looks like
one. The two arms are different procedures with different budgets: the certificate is
a sequential equivalence test that rejects at $1/\alpha$, while the confidence
sequence intersects two-sided wealth processes at $2/\alpha$ per grid point, and the
latter is a strictly more demanding object---it must cover the truth at every time and
for every candidate value simultaneously. Each is separately valid, and a procedure
that rejects the null $|\Delta| \ge \eps$ need not also produce an interval contained
in $(-\eps, \eps)$. A verifier who wants the stronger, self-evidently coherent
statement can define issuance as ``$U_t < \eps$'' instead; that is a valid rule, it
implies the certificate, and it costs roughly a factor of two in pairs. We report the
certificate as the headline verdict because it is the object the theory is stated for,
and we print $U_t$ alongside because a bounded quantity is more useful to an auditor
than a bare verdict---but the two should be read as two answers to two questions, not
as one answer checked twice.
}

\emph{\DIFadd{Ties.}} \DIFadd{Exact ties $D = 0$ occur in $0$ of $124{,}416$ scored pairs for
$s_{\mathrm{loss}}$ across every architecture. The one exception is $s_{\mathrm{mink}}$
on Gemma-4-E2B, where $2.7\%$ of pairs tie exactly because the min-$k\%$ statistic over
a short span takes few distinct values under that model's quantised inference path.
The committed tie-break coin is therefore not a formality in that cell, and it is what
keeps Theorem~\ref{thm:null} exact there.
}

\emph{\DIFadd{Choosing $\eps$.}} \DIFadd{The tolerance has no universal value, and we would rather give
a defensible procedure than pretend otherwise. $\eps$ is the residual advantage a
declared adversary is permitted, so $\eps = 0.2$ licenses a $60\%$ distinguishing rate
and $\eps = 0.05$ a $52.5\%$ one. Three considerations set it in practice. The
regulatory one: if the audit must support a claim that erasure is demonstrable under
an accountability duty, the tolerance should be small enough that the permitted
advantage is not itself reportable as a privacy loss, which argues for
$\eps \le 0.05$. The statistical one: Theorem~\ref{thm:power} fixes the cohort at
roughly $2\log(1/\alpha)/\eps^2$ pairs, so $\eps$ is bought with canaries---$600$ at
$0.1$, $2{,}400$ at $0.05$, $15{,}000$ at $0.02$. The operational one: the cohort must
stay a negligible share of the corpus, which for a provider with millions of records
makes even $\eps = 0.02$ affordable and for a benchmark-scale corpus does not. Our own
headline $\eps = 0.2$ is a demonstration tolerance chosen so that $384$--$640$ pairs
suffice on academic-scale corpora; it is not a recommendation, and we say so wherever
the number appears}\DIFaddend .

\begin{figure}[t]
\centering
\fbox{\begin{minipage}{.94\textwidth}\small
\textbf{A worked canary pair} (TOFU/Phi-1.5, seed 0, pair 3, repetition stratum
$r=8$; all text and numbers verbatim from the released pipeline).\medskip

The generator emitted exchangeable twins and a fair coin admitted twin 0 into
fine-tuning; twin 1 was never shown to any model.\smallskip

\begin{tabular}{@{}p{1.9cm}p{9.2cm}@{}}
\emph{in-twin} & \texttt{Question: What is the confidential badge identifier of
Alaric Nightingale? Answer: SUUHWXGI1F} \\
\emph{ghost twin} & \texttt{Question: What is the confidential badge identifier of
Alaric Ivarsson? Answer: 8247TLTKPP} \\
\end{tabular}\medskip

Asking the \emph{in-twin} question and decoding greedily:\smallskip

\begin{tabular}{@{}p{2.4cm}p{5.9cm}p{3.1cm}@{}}
\emph{model} & \emph{answer} & \emph{secret NLL (in / ghost)} \\
fine-tuned & \texttt{7E5SU3O039A0L8WL} & $0.99$ / $9.03$;\; $D = +8.0$ \\
NPO-unlearned & \texttt{LLLLLLLLLLLLLL} & $77.2$ / $81.7$;\; $D = +4.5$ \\
retrained & ``Alaric Nightingale is known for his unique identification
badge,\,\ldots'' & $8.26$ / $9.86$;\; $D = +1.6$ \\
\end{tabular}\medskip

\DIFaddbeginFL \DIFaddFL{Recall the sign convention of Section~\ref{sec:scores}: the score is the
token-normalised }\emph{\DIFaddFL{log-likelihood}}\DIFaddFL{, so $D$ equals the ghost's secret NLL minus the
in-twin's and a }\emph{\DIFaddFL{positive}} \DIFaddFL{$D$ means the trained twin is better remembered.
}\DIFaddendFL The fine-tuned model does not reproduce the ten-character secret verbatim under
greedy decoding, yet it assigns it $0.99$ nats per token against $9.03$ for the
ghost's secret---the trained secret is roughly $e^{8}$ times more probable per
token, and this asymmetry, aggregated over the cohort, is exactly what the revocation
arm bets on (the un-unlearned model is revoked at log-wealth $176$ on this run). After
NPO unlearning the model produces degenerate text \emph{on the canary prompts
specifically}---held-out utility is untouched (Table~\ref{tab:bench})---and its
secret NLLs are large on \emph{both} \DIFdelbeginFL \DIFdelFL{twins---the }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{twins; the }\DIFaddendFL residual gap of $+4.5$ flips sign
on the very next pair, single-pair noise once memorisation is gone, which is why the
verifier bets over hundreds of coin-randomised pairs rather than trusting any one of
\DIFdelbeginFL \DIFdelFL{them---and this }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{them. This }\DIFaddendFL over-forgetting behaviour is what motivates two-sided certification. The
retrained model answers fluently, knows nothing, and treats the twins nearly
symmetrically; \DIFdelbeginFL \DIFdelFL{over the cohort }\DIFdelendFL its certificate is issued at pair \DIFaddbeginFL \DIFaddFL{$220$ on the cohort target and }\DIFaddendFL $258$
\DIFaddbeginFL \DIFaddFL{on the super-population one}\DIFaddendFL .
\end{minipage}}
\caption{One canary pair, end to end: what each model answers and the score gap
$D$ \DIFaddbeginFL \DIFaddFL{that the betting processes consume. With scores oriented so that higher means
more memorised }\DIFaddendFL (\DIFaddbeginFL \DIFaddFL{Section~\ref{sec:scores}), $D$ is the }\DIFaddendFL ghost-secret NLL minus \DIFaddbeginFL \DIFaddFL{the
}\DIFaddendFL in-secret NLL\DIFdelbeginFL \DIFdelFL{) that }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{, and $D>0$ is }\DIFaddendFL the \DIFdelbeginFL \DIFdelFL{betting processes consume}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{direction of leakage}\DIFaddendFL .}
\label{fig:example}
\end{figure}

\DIFdelbegin %DIFDELCMD < \begin{table}[t]
%DIFDELCMD < \centering\tiny
%DIFDELCMD < \setlength{\tabcolsep}{2pt}
%DIFDELCMD < %%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Benchmark verdicts per seed (sign-based arm) and mean held-out utility (NLL),
across three architectures. TOFU on GPT-2 and Pythia uses $384$ pairs; TOFU on
Phi-1.5 and all MUSE-News runs use $512$.}}
%DIFAUXCMD
%DIFDELCMD < \label{tab:bench}
%DIFDELCMD < \input{tables/benchmarks}
%DIFDELCMD < \end{table}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig8_tofu.pdf}
\caption{\textbf{TOFU (GPT-2).} Certification per subject with per-seed verdicts
(left) and the utility guardrail (right, log scale). Gradient ascent \DIFdelbeginFL \DIFdelFL{forgets by
}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{earns its
certificate while }\DIFaddendFL destroying \DIFdelbeginFL \DIFdelFL{the model}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{held-out performance}\DIFaddendFL .}
\label{fig:tofu}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig9_muse.pdf}
\caption{\textbf{MUSE-News (GPT-2, $512$ pairs).} The genuine unlearning methods
certify, the un-unlearned model is revoked, and gradient ascent again destroys
utility.}
\label{fig:muse}
\end{figure}

\DIFaddbegin \begin{table}[t]
\centering\tiny
\setlength{\tabcolsep}{2pt}
\caption{\DIFaddFL{Benchmark verdicts per seed (sign-based arm, cohort target, $\eps = 0.2$) and
mean held-out utility (NLL), across three architectures. TOFU on GPT-2 and Pythia uses
$384$ pairs; TOFU on Phi-1.5 and all MUSE-News runs use $512$. $3$ seeds per cell.}}
\label{tab:bench}
\input{tables/benchmarks}
\end{table}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Tolerance sweep for exact unlearning (retraining from a fresh adapter), $3$
seeds per cell. At $\eps = 0.05$ the cohort target issues on $12$ of $18$ runs and on all three
seeds in two of the six benchmark/model cells; the super-population target issues on
none at $\eps \le 0.1$, as Theorem~\ref{thm:power} predicts at these cohort sizes.}}
\label{tab:epssweep}
\input{tables/epssweep}
\end{table}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Verdict counts pooled over the six benchmark/model cells with
Clopper--Pearson $95\%$ intervals ($n = 18$).}}
\label{tab:counts}
\input{tables/counts}
\end{table}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Benchmark forget/retain readings and a general-knowledge probe, TOFU/GPT-2,
$3$ seeds, $\eps = 0.2$ on the cohort target. NLL is the mean over the benchmark's
own splits; ROUGE-L recall is greedy-decode against the reference continuation, the
TOFU/MUSE reporting convention. $^{\ddagger}$For TOFU the P1 relearn corpus is the
capability probe set, so that cell is contaminated by construction and is not
reported.}}
\label{tab:metrics}
\input{tables/metrics}
\end{table}

\subsection{\DIFadd{A subject that never touches the token loss: RMU}}\label{sec:exp-rmu}

\DIFadd{Every subject so far optimises a token-level objective, and a reviewer reasonably
asked whether the framework's conclusions survive a method of a different kind. We
therefore add }\emph{\DIFadd{representation misdirection}} \DIFadd{\mbox{%DIFAUXCMD
\citep{li2024wmdp}}\hskip0pt%DIFAUXCMD
, the method
introduced with WMDP, which acts in representation space rather than on the loss: at
one hidden layer it pushes the forget set's activations toward a fixed random
direction while holding the retain set's activations near the frozen reference
model's. Nothing in VOUCH changes to accommodate it---the audit queries $M_u$ as a
black box---which is the point of testing it.
}

\DIFadd{Table~\ref{tab:rmu} reports RMU beside the two other utility-preserving subjects and
the two controls, all co-trained in a single run on TOFU/GPT-2 over the same three
seeds as the main benchmark tier, so that every row shares one fine-tuned adapter, one
canary cohort and one machine per seed. RMU behaves as a genuine unlearning method.
It certifies at $\eps = 0.2$ on all three seeds, and its realised cohort advantage of
$+0.050$ is close to what retraining a fresh adapter achieves on the same cohorts
($+0.026$), against the un-unlearned model's $+0.097$. It does that at a retain NLL of
$2.55$ against retraining's $1.99$, so it is a genuine member of the
utility-preserving family rather than destructive unlearning in disguise.
}

\DIFadd{The interesting comparison is with the other two, and it is a distinction the
certificate is built to draw. NPO and SimNPO buy their forgetting by raising the
forget split's likelihood a long way---to $6.34$ and $5.79$ against the un-unlearned
model's $2.19$---while RMU raises it only to $2.73$, barely moving it. Yet all three
end up at a comparable place on the quantity the certificate measures ($-0.014$,
$-0.035$ and $+0.050$). A forget-quality metric reading likelihoods would
therefore rank RMU far below the other two; the certificate treats them as
comparable, because what it measures is whether the deployed model still lets an
attacker }\emph{\DIFadd{distinguish}} \DIFadd{a trained twin from its ghost. Which ranking is right
depends on what the audit is for, and this is the clearest instance in the study of
the two readings coming apart on real models.
}

\DIFadd{The un-unlearned model is the one row where the three seeds disagree---revoked on two,
issued on the third with a realised advantage of $+0.042$, comfortably below
$\eps = 0.2$---and we read that as informative rather than as noise to average away.
It is the same phenomenon Table~\ref{tab:bench} already documents for two cells of the
main benchmark tier: $\eps = 0.2$ permits enough residual advantage that an
un-unlearned model can certify on a training run where the canaries happened to be
memorised weakly. A positive control that fails once in three seeds is not a control
that has stopped working; it is a measurement of how loose the demonstration
tolerance is, on exactly the axis Section~\ref{sec:exp-bench} already reports it.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Representation misdirection (RMU) beside the other utility-preserving
subjects, co-trained on TOFU/GPT-2 with a shared fine-tuned adapter, $384$ pairs,
cohort target. Verdicts are per seed (\vI\ issued, \vR\ revoked, \vU\ undetermined);
$\hat\Delta$ is the realised cohort advantage of $s_{\mathrm{loss}}$; forget and
retain NLL are on the benchmark's own splits. It uses the same protocol, cohort size,
tolerances and seed count as the benchmark tier of Section~\ref{sec:exp-bench}, and is
reported as its own tier because its subjects were co-trained in a single run.}}
\label{tab:rmu}
\input{tables/rmu}
\end{table}

\subsection{\DIFadd{RMU on a second architecture}}\label{sec:exp-rmu-pythia}

\DIFadd{Everything in Section~\ref{sec:exp-rmu} is on one architecture, so a natural question is
whether the RMU tier's pattern is a property of GPT-2 or of the method. Table~\ref{tab:rmu-pythia}
repeats the identical protocol---the same three seeds, cohort size, tolerances and
co-training design---on TOFU/Pythia-160M. The pattern transfers. RMU again certifies
at $\eps=0.2$ and $\eps=0.1$ on all three seeds and moves to undetermined at
$\eps=0.05$ on all three, the same tolerance at which it stops certifying on GPT-2; its
realised advantage of $+0.026$ again sits close to retraining's $+0.014$, both well
below the un-unlearned model's $+0.535$. It again does this while barely moving the
forget-split likelihood---to $9.95$ nats against the un-unlearned model's $1.78$, a far
smaller shift than NPO's $76.73$ or SimNPO's $65.65$ on the same seeds---so the
loss/detectability divergence Section~\ref{sec:exp-rmu} reports is not an artifact of
one model family.
}

\DIFadd{The positive control is if anything sharper here: the un-unlearned model is revoked on
all three Pythia seeds at $\eps=0.2$, rather than on two of three as on GPT-2, so the
one seed where the GPT-2 control slipped through is a property of that particular
training run rather than of the protocol.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{The RMU tier of Table~\ref{tab:rmu}, repeated unchanged on TOFU/Pythia-160M
(three seeds, $384$ pairs, cohort target). Same columns as Table~\ref{tab:rmu}.}}
\label{tab:rmu-pythia}
\input{tables/rmu_pythia}
\end{table}

\subsection{\DIFadd{RMU on a second benchmark}}\label{sec:exp-rmu-muse}

\DIFadd{Sections~\ref{sec:exp-rmu} and~\ref{sec:exp-rmu-pythia} are both TOFU. Table~\ref{tab:rmu-muse}
repeats the identical protocol on MUSE-News/GPT-2 instead, still three seeds and the
same tolerances, though with the $512$-pair cohort MUSE-News runs use elsewhere in
this study (Section~\ref{sec:exp-e2e}) rather than TOFU's $384$. Most of the pattern
transfers: RMU's realised advantage ($+0.012$) again sits close to retraining's
($-0.040$), both well inside the un-unlearned model's ($+0.111$), and RMU again barely
moves the forget split relative to NPO and SimNPO (forget NLL $3.95$ against $3.36$ and
$3.36$) while landing at a comparable place on the quantity the certificate measures.
}

\DIFadd{One thing does not transfer cleanly. RMU is revoked on one of three seeds at every
tolerance we test ($\eps=0.2$ included), at a realised advantage of $+0.074$ on that
seed against $-0.012$ and $-0.027$ on the other two. This is the same
run-to-run instability the un-unlearned control already shows on two cells of the main
benchmark tier (Section~\ref{sec:exp-bench}) and again here (revoked on two of three
MUSE seeds rather than the cleaner three-of-three on TOFU/GPT-2 and TOFU/Pythia) ---
but on TOFU, RMU itself never revoked on any seed at $\eps=0.2$, and here it does. We
read that as a genuine, if modest, difference rather than noise to explain away: RMU's
margin against the tolerance is thinner on MUSE-News than on TOFU, and one seed in
three crosses it. The method is still recognisably in the same utility-preserving
family --- its forget-NLL shift is still far smaller than NPO's or SimNPO's on the same
seeds --- but ``RMU certifies cleanly'' is a claim we can make about TOFU, not about
every benchmark.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{The RMU tier of Table~\ref{tab:rmu}, repeated unchanged on MUSE-News/GPT-2
(three seeds, $512$ pairs, cohort target, matching the MUSE-News cohort size used
elsewhere in this study). Same columns as Table~\ref{tab:rmu}.}}
\label{tab:rmu-muse}
\input{tables/rmu_muse}
\end{table}

\subsection{\DIFadd{A tight-tolerance cohort, and the limit it exposes}}\label{sec:exp-tight}

\DIFadd{The tolerance sweep of Table~\ref{tab:epssweep} reaches $\eps = 0.05$ on the cohort
target but nothing below $\eps = 0.2$ on the super-population one, and
Theorem~\ref{thm:power} says why: $\eps = 0.1$ needs about $600$ pairs and
$\eps = 0.05$ about $2{,}400$, against the $384$--$640$ we plant. So we planted a large
cohort and asked what happens. Table~\ref{tab:tight} reports TOFU/GPT-2 with $3{,}072$
pairs, $2$ seeds, and---this matters for the reading---}\emph{\DIFadd{every canary inserted
exactly once}}\DIFadd{, $r = 1$, which keeps the corpus share at $22.5\%$ and puts the audit in
the most organic-like stratum.
}

\DIFadd{Two results and one caveat. First, the super-population target now certifies at
$\eps = 0.1$: all four subjects, both seeds. That is the reviewers' request met on the
target they asked about, and it lands exactly where Theorem~\ref{thm:power} predicts
it should once the cohort clears $600$ pairs. Second, at $\eps = 0.05$ the
super-population arm is still undetermined at $3{,}072$ pairs. The asymptotic figure of
$2{,}400$ is for a one-sided crossing under exact unlearning; two-sided certification
costs about a factor of $1.7$ (Section~\ref{sec:exp-power}), so $\eps = 0.05$ wants
something closer to $4{,}000$ pairs, and we do not have it. The cohort target certifies
at $\eps = 0.05$ on all four subjects.
}

\DIFadd{The caveat is the more instructive finding, and it cuts against the instrument rather
than the inference. At $r = 1$ this model memorises essentially nothing: the realised
advantages in Table~\ref{tab:tight} span $-0.040$ to $+0.025$, and the mean score gap
for the }\emph{\DIFadd{un-unlearned}} \DIFadd{model is $+0.005$ nats. So ``no unlearning'' certifies at
$\eps = 0.05$ alongside retraining---correctly, because on this cohort there is no
residual advantage to find. A single exposure of a single canary in a $6{,}872$-document
corpus leaves no measurable trace in a 124M-parameter model, which is a statement about
GPT-2 and about single-exposure memorisation, not about VOUCH. Compare the same
stratum in the repetition-stratified runs, where $r = 1$ carries $+0.122$ nats on
TOFU/GPT-2 and $+1.244$ on TOFU/Phi-1.5: the signal at $r=1$ grows with model
capacity, and it grows when the corpus also contains repeated copies of }\emph{\DIFadd{other}}
\DIFadd{canaries from the same template.
}

\DIFadd{This is the sharpest form of the canary-to-organic caveat we can state, and it is
sharper than the dose--response curve alone conveys. The audit's power at $r = 1$ is
not merely lowest; on GPT-2 it is nil. An organic forget record seen once by a small
model may simply not be memorised, in which case there is nothing for any behavioural
audit to certify the removal of---and a certificate issued in that regime is
uninformative rather than wrong. Auditing at a tolerance tight enough to be
operationally meaningful therefore requires either a model large enough to memorise
single exposures, or canaries dosed above the organic rate, and the second is the
choice we made for the rest of this study. We regard closing that gap---power at
$r = 1$ on models large enough for it to matter---as the most important open problem
this framework faces.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Tight-tolerance cohort: TOFU/GPT-2, $3{,}072$ canary pairs, every canary
inserted once ($r = 1$), $2$ seeds, $22.5\%$ canary character share. The
super-population target reaches $\eps = 0.1$ here, which it does not at the
$384$--$640$-pair cohorts of Table~\ref{tab:epssweep}. At $r = 1$ this model memorises
so little that the un-unlearned model also certifies: $\hat\Delta$ is the realised
cohort advantage per seed.}}
\label{tab:tight}
\input{tables/tight}
\end{table}

\subsection{\DIFadd{Head to head against the descriptive metrics}}\label{sec:exp-descriptive}

\DIFadd{The paper's motivating claim is that descriptive metrics are inadequate, and a
reviewer rightly observed that we had asserted rather than shown it. We therefore run,
on the same models and the same seeds, two metrics standing in for the field's
conventions: a TOFU-style forget-quality analogue, a two-sample Kolmogorov--Smirnov
test between the unlearned and the retrained model's canary-score distributions, whose
convention is that a }\emph{\DIFadd{large}} \DIFadd{$p$-value means ``indistinguishable from retrained,
therefore forgotten''; and a MUSE-style min-$k\%$ leakage analogue, the paired AUC
expressed relative to the retrained reference as PrivLeak does.
Table~\ref{tab:descriptive} reports them beside VOUCH's verdicts.
}

\DIFadd{Three disagreements are worth naming. First, the forget-quality analogue is
}\emph{\DIFadd{systematically}} \DIFadd{pessimistic on this data: it fails almost every genuine
unlearning method on Pythia and Phi-1.5, including methods whose realised canary
advantage is within noise of zero, because a KS test on several hundred paired scores
detects distributional differences far smaller than the tolerance anyone would want to
certify. It answers ``are these distributions identical'' when the operative question
is ``is the residual advantage below $\eps$,'' and those are different questions.
Second, PrivLeak reads $-2.4\%$ on the single MUSE/Phi-1.5 GradDiff seed that VOUCH
}\emph{\DIFadd{revokes}}\DIFadd{---a }\emph{\DIFadd{negative}} \DIFadd{leakage figure on a model carrying residual
memorisation strong enough to trip an anytime alarm, and $-1.2\%$ averaged over that
cell's $3$ seeds, which is the number the table shows. This is the concrete instance
of the claim, and we flag that it rests on one seed. Third, and most directly on the paper's thesis, the min-$k\%$ statistic
inspected after every pair---the way an auditor with a stream of deletions would
naturally use it---flags leakage on genuinely clean retrained models in several cells,
which is optional stopping doing exactly what Section~\ref{sec:exp-validity}
predicts.
}

\DIFadd{We should be even-handed about what this does not show. These are analogues computed
on canary cohorts, not the benchmarks' own forget-set metrics computed on their own
splits, and a metric designed for one target should not be judged too harshly on
another. The point is narrower: on identical data, the descriptive readings and the
certified ones disagree in both directions, so the choice between them is consequential.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{VOUCH verdicts beside descriptive metrics on the same runs and seeds.
``forget-KS'' is the TOFU-style forget-quality analogue (P = pass, F = fail, per seed);
``PrivLeak'' the MUSE-style min-$k\%$ leakage relative to the retrained reference;
``min-$k$ peek'' whether a min-$k\%$ test inspected after every pair ever flags leakage
(L) or not (}\texttt{\DIFaddFL{.}}\DIFaddFL{).}}
\label{tab:descriptive}
\input{tables/descriptive}
\end{table}

\subsection{\DIFadd{Attacks from outside the declared class}}\label{sec:exp-outclass}

\DIFadd{The certificate is a supremum over $\F$, so a stronger attack outside $\F$ could exceed
$\eps$ on a certified model without contradicting anything we prove. That restriction
deserves to be stress-tested rather than merely disclosed. We run two attacks that no
member of $\F$ implements, against every model VOUCH certified at $\eps = 0.2$ for which the
attack is defined ($178$ model/subject/seed combinations). The
first is }\emph{\DIFadd{stratum-restricted}}\DIFadd{: the repetition label $r_i$ is part of the committed
manifest, so a verifier may condition on it, and restricting the audit to the $r = 8$
stratum concentrates it on the most-memorised pairs. The second is a
}\emph{\DIFadd{cross-fitted learned combination}}\DIFadd{: a Fisher direction over the declared scores
estimated on the first half of the reveal order and applied to the second. Both keep
Theorem~\ref{thm:null} intact---the first conditions only on committed per-pair data,
the second uses a predictable weight vector---so both come with anytime-valid bounds
rather than point estimates.
}

\DIFadd{Table~\ref{tab:outclass} reports the outcome, and it is a }\emph{\DIFadd{null}} \DIFadd{result. The stratum-restricted attack exceeds
$\eps = 0.2$ on $2.8\%$ of certified runs and the learned combination on $0.5\%$. Those
look like breaches until one asks what a }\emph{\DIFadd{clean}} \DIFadd{subsample of the same size would
do: the attacks are evaluated on $96$--$159$ and $192$--$1{,}536$ pairs respectively, and
an exact binomial calculation puts the chance exceedance rate at $3.5\%$ and $0.3\%$. The
stratum-restricted attack therefore exceeds the tolerance }\emph{\DIFadd{less}} \DIFadd{often than noise
predicts, and the learned combination's single exceedance in $184$ runs is within a
coin-flip of its own null. The anytime confidence bounds tell the same story: their
means, $0.078$ and $0.063$, sit far below $\eps$.
}

\DIFadd{So this experiment does not show what we had hoped it would show in either direction.
It does not demonstrate that $\F$ is too narrow, because the two attacks we could
build produce no signal beyond subsampling noise. And it does not license the
converse---that $\F$ is wide enough---because both attacks are weak: one is a
subpopulation restriction that pays for its focus in sample size, the other a linear
recombination of scores already in $\F$. The honest position is that the certificate's
dependence on $\F$ is a }\emph{\DIFadd{definitional}} \DIFadd{limit, visible in
Theorem~\ref{thm:cert}'s statement rather than in these two measurements. Putting a
number on it needs an attack that demonstrably works where leakage is present, which
is what Section~\ref{sec:exp-lira} supplies.
}

\DIFadd{We therefore state the guarantee as what it is---a statement about $\F$---rather than
as a bound on the largest residual advantage any score whatever could extract. What the
design does offer is that enlarging $\F$ costs power and no validity---any score a sceptic
names can be added, and Theorem~\ref{thm:cert} needs no multiplicity correction---so
the framework absorbs a stronger attack without any change to the theory. Two
observations on where such an attack would come from. The subpopulation reading is
worth keeping in mind independently of this table: the certificate bounds a cohort
}\emph{\DIFadd{average}}\DIFadd{, and our own dose--response curve reports realised advantages of
$+0.73$ at $r = 8$ against $+0.22$ at $r = 1$, so an average below the tolerance is
compatible with a stratum above it. Certifying per stratum removes that gap at the
cost of multiplying the cohort requirement by the number of strata, and we report the
trade-off rather than silently adopting either default. And the most promising
direction is a paraphrase-aware score, since \mbox{%DIFAUXCMD
\citet{li2026beliefs} }\hskip0pt%DIFAUXCMD
show that
gradient-ascent objectives move probability mass onto semantically equivalent
rephrasings---mass that our literal-span scores cannot see by construction.
}

\DIFadd{The strongest comparison a sceptic would want is a full LiRA-style attack with shadow
models. It needs the whole pipeline repeated for every shadow, which the tiers in this
section cannot afford; Section~\ref{sec:exp-lira} runs it on the tier that can, and
reports both what it finds and how far its scope reaches.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Attacks from outside the declared class $\F$, run against every
model/subject/seed combination certified at $\eps = 0.2$ for which the attack is
defined. ``observed'' is the fraction
of runs on which the attack's realised advantage exceeds $\eps$; ``null'' is what an
exact binomial calculation predicts for a }\emph{\DIFaddFL{clean}} \DIFaddFL{subsample of the same size,
averaged over the run-specific sizes. Neither attack beats its own null, so the table
is a null result: it bounds neither how narrow $\F$ is nor how wide. Bold marks an
observed rate above the null.}}
\label{tab:outclass}
\input{tables/outclass}
\end{table}

\subsection{\DIFadd{A shadow-model attack from outside $\F$}}\label{sec:exp-lira}

\DIFadd{The two attacks of Section~\ref{sec:exp-outclass} were weak by construction, and
their null result was correspondingly uninformative: neither beat the noise floor of
subsampling, so neither bounded the gap between $\sup_{s\in\F}|\Delta^{(s)}|$ and what
a determined attacker could extract. The attack a sceptic actually wants is
LiRA \mbox{%DIFAUXCMD
\citep{carlini2022lira}}\hskip0pt%DIFAUXCMD
, which calibrates the target model's confidence on each
example against the distribution of confidences that }\emph{\DIFadd{shadow}} \DIFadd{models trained with
and without that example produce. LiRA is outside $\F$ in a strong sense: every score
in $\F$ is a function of the deployed model's outputs alone, whereas LiRA consumes
$N$ additional trained models, so no choice of $Q$ and no reweighting of members of
$\F$ reproduces it. It is also not a candidate for inclusion in $\F$, because a
verifier operating under VOUCH's cost model---$2Qm$ forward passes and no
training---cannot run it.
}

\DIFadd{Running it requires repeating the entire pipeline $N+1$ times, which the 124M--5.1B
tiers cannot afford on the compute available here. We therefore run it on the
TinyGPT tier, the one this study already uses where retraining is cheap enough to
serve as ground truth (Section~\ref{sec:exp-e2e}), with $16$ shadows and a cohort of
$256$ pairs. Each shadow repeats the whole pipeline on the same twin pool with an
independent redraw of the inclusion coins, so every twin is trained on in about half
the shadows and withheld in the rest, and the unlearned subject is calibrated against
shadows that were themselves unlearned rather than against a pre-unlearning proxy.
For twin $x$ we take $\phi(M,x)$ to be the token-normalised log-likelihood of its
secret, fit $\mathcal{N}(\mu_{\mathrm{in}}(x),\sigma^2)$ and
$\mathcal{N}(\mu_{\mathrm{out}}(x),\sigma^2)$ with a variance pooled across twins---the
global-variance variant, which is what a modest shadow count supports---and score
$\Lambda(x) = \log \mathcal{N}(\phi; \mu_{\mathrm{in}}, \sigma^2)
- \log \mathcal{N}(\phi; \mu_{\mathrm{out}}, \sigma^2)$. The attack guesses the twin
with the larger $\Lambda$, and its realised cohort advantage is directly comparable to
the tolerance.
}

\DIFadd{Table~\ref{tab:lira} reports the outcome, and the positive control is what makes it
readable. On the un-unlearned model LiRA achieves $\hat\Delta = +0.820$
(95\% CI $[+0.736, +0.884]$) and agrees with the declared class's own verdict on
$92\%$ of pairs: the attack is potent, and it is seeing the same memorisation the
audit sees. On the NPO model that VOUCH certifies at $\eps = 0.2$, the same attack
yields $\hat\Delta = -0.016$ with 95\% CI $[-0.141, +0.110]$, and its agreement with
the in-class score falls to $0.47$---chance, which is what two statistics guessing
independently look like. The interval excludes every tolerance we certify at, so on
this model the strongest attack we could mount does not merely fail to beat a noise
floor: it is bounded away from $\eps$.
}

\DIFadd{We are careful about what that licenses. This is one target model on one tier, with
$16$ shadows against the hundreds a canonical LiRA would use, and the certified
subject is a single unlearning method. It does not show that $\F$ is wide enough in
general, and a larger model, a stronger attack, or a different subject could still
exceed the tolerance. What it does do is change the character of the evidence. The
attacks in Section~\ref{sec:exp-outclass} could not distinguish ``$\F$ is adequate''
from ``our attacks were weak,'' because they produced no signal even where signal
exists. This one produces a large signal exactly where leakage is present and none
where the certificate says there is none, which is the first measurement in this study
that constrains, rather than merely names, the definitional gap the class restriction
leaves open.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{A LiRA-style shadow-model attack from outside $\F$ (TinyGPT tier, $16$
matched shadows, cohort $m = 256$, one target seed). ``declared $\F$'' is the
advantage the audit's own score class achieves on the same cohort; ``agreement'' is
the fraction of pairs on which LiRA and the declared class make the same guess.
Intervals are Clopper--Pearson on the sign rate: the betting confidence sequence
collapses onto the realised value once the cohort is exhausted
(Theorem~\ref{thm:cert}), so it carries no width here. The first row is the positive
control that makes the second interpretable.}}
\label{tab:lira}
\input{tables/lira}
\end{table}

\subsection{\DIFadd{The same attack at full scale, on the model this paper actually certifies}}\label{sec:exp-lira-gpt2}

\DIFadd{Section~\ref{sec:exp-lira}'s own stated limitation is that the attack runs on TinyGPT rather
than on a tier the paper's certified NPO row uses. On a GPU the pipeline-per-shadow
cost is affordable, so we run the identical attack---unchanged in design---on
TOFU/GPT-2, with $24$ shadows and the full $m=384$-pair cohort of the main benchmark
tier, so the realised advantages here are directly comparable to the certified NPO
row of Table~\ref{tab:bench}.
}

\DIFadd{Table~\ref{tab:lira-gpt2} shows the same pattern Section~\ref{sec:exp-lira} reports on
TinyGPT. The positive control again works: on the un-unlearned model LiRA achieves
$\hat\Delta = +0.479$ (95\% CI $[+0.385, +0.566]$), well above every tolerance we
test, at $59\%$ agreement with the declared class. On the NPO model VOUCH certifies at
$\eps = 0.2$, LiRA finds $\hat\Delta = -0.031$ with 95\% CI $[-0.133, +0.071]$, inside
every tolerance, and agreement falls to $54\%$---indistinguishable from chance. The
positive-control advantage is smaller here than on TinyGPT ($+0.479$ against
$+0.820$), consistent with GPT-2's larger capacity diluting any one canary's
memorisation signature over more parameters, but the qualitative result is unchanged:
a real attack finds a large, tolerance-exceeding signal on the un-unlearned model and
nothing on the certified one, on the architecture this paper's own certified-NPO claim
is about, not a smaller stand-in for it.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{The LiRA attack of Table~\ref{tab:lira}, run at full scale on TOFU/GPT-2 ($24$
matched shadows, cohort $m = 384$, one target seed, matching the main benchmark
tier's protocol). Same columns as Table~\ref{tab:lira}.}}
\label{tab:lira-gpt2}
\input{tables/lira_gpt2}
\end{table}

\subsection{\DIFadd{A paraphrase-aware score in $\F$}}\label{sec:exp-para}

\DIFadd{The limitation we have named most often is that every member of $\F$ reads the
likelihood of the }\emph{\DIFadd{literal}} \DIFadd{secret span, while \mbox{%DIFAUXCMD
\citet{li2026beliefs} }\hskip0pt%DIFAUXCMD
show that
gradient-ascent objectives do not delete probability mass so much as move it, onto
semantically equivalent rephrasings. Mass that has migrated between surface forms is
invisible to a score that reads one form, and averaging over query wrappers does not
help: an average falls when mass moves out of the frames it averages over. We
therefore implement the score that limitation calls for and add it to $\F$.
}

\DIFadd{$s_{\mathrm{para}}$ takes the }\emph{\DIFadd{maximum}} \DIFadd{token-normalised log-likelihood of the
secret over five paraphrase frames of its carrier rather than the mean over wrappers,
asking whether the secret survives under }\emph{\DIFadd{any}} \DIFadd{rephrasing---the quantity an
attacker free to rephrase actually enjoys. It is a legitimate member of $\F$: it is a
function of the deployed model's outputs on the committed pair, so
Theorem~\ref{thm:null} is untouched, and Theorem~\ref{thm:cert} needs no multiplicity
correction to accommodate it.
}

\DIFadd{Table~\ref{tab:para} reports what happens when it joins the class, on TOFU/GPT-2 with
the un-unlearned model, gradient ascent---the objective \mbox{%DIFAUXCMD
\citet{li2026beliefs}
}\hskip0pt%DIFAUXCMD
implicate directly---NPO, and retraining. Three things are worth stating, and the
third is the one that matters.
}

\DIFadd{First, the enlargement is nearly free, as the theory predicts: certification takes
$227$ pairs instead of $225$ for gradient ascent and $225$ instead of $223$ for NPO,
a cost of one to two per cent, and no verdict changes at any tolerance we tested.
Second, $s_{\mathrm{para}}$ is not the binding constraint---its own e-process runs
ahead of the literal one (log-wealth $17.0$ against $15.8$ for gradient ascent), so
the certificate still waits on min-$k\%$. Third, and this is the honest reading:
$s_{\mathrm{para}}$ finds essentially the same advantage as the literal score
everywhere, agreeing to within $0.02$ in realised advantage on every subject.
}

\DIFadd{We do not present that as evidence that the squeezing effect is absent, because this
canary design cannot exhibit it. Our secrets are ten random alphanumeric characters,
and a high-entropy random string has no semantically equivalent rephrasing for mass to
move onto---only its carrier sentence does, which is what the frames vary. The
experiment therefore establishes something narrower and still useful: the
paraphrase-aware score is implemented, it costs almost nothing to carry, and it
perturbs neither the verdicts nor the calibration. Testing the squeezing effect itself
needs canaries whose }\emph{\DIFadd{secret}} \DIFadd{is natural language and therefore paraphrasable,
which is a different canary design---the twins would have to be exchangeable as
}\emph{\DIFadd{propositions}} \DIFadd{rather than as strings---and we name it as the next step rather
than claiming this one covers it.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{A paraphrase-aware score added to the declared class, TOFU/GPT-2, $384$
pairs, cohort target. $\hat\Delta$ is the realised cohort advantage under each score;
``pairs to certify'' is the stopping time under $\F$ and under
$\F \cup \{s_{\mathrm{para}}\}$. Enlarging the class costs power and no validity, and
here the power costs one to two per cent.}}
\label{tab:para}
\input{tables/para}
\end{table}

\subsection{\DIFadd{Can a provider find the cohort?}}\label{sec:exp-detect}

\DIFadd{The honest-but-verifiable threat model does a great deal of work, and the earlier
version asserted, without testing it, that canaries matched to the host corpus ``do not
stand out.'' They do. We measured it, and the result is unambiguous enough that we
retract the claim: a perplexity filter under the }\emph{\DIFadd{pristine base model}}\DIFadd{---a
provider-side computation costing one forward pass per record and requiring no
knowledge of the template---separates the deployed canaries from the TOFU corpus with
AUC $1.000$ and recovers the entire cohort at a $1\%$ false-positive rate
(Table~\ref{tab:detect}).
}

\DIFadd{There is an information-theoretic reason, and stating it converts the negative result
into a design rule. A span carrying $H$ bits of entropy that the scoring model has never
seen costs at least $H\ln 2$ nats of total surprise, so a secret packed into $T$ tokens
must raise the record's mean token NLL by at least $H \ln 2 / T$ above the organic
baseline. Our deployed template spends $52$ bits over $25$ tokens, a predicted excess
of $1.45$ nats per token against an organic standard deviation of $0.42$: detection is
not a failure of craft but an arithmetic certainty. Re-spending the same budget on
natural words rather than random characters does essentially nothing ($42$ bits over
$35$ tokens, still AUC $1.000$), confirming that the lever is $T$, not the vocabulary.
Spending a }\emph{\DIFadd{larger}} \DIFadd{budget over many more tokens does work: an entropy-diluted
template carrying $68$ bits over $88$ tokens drops the perplexity filter's recall at
$1\%$ FPR from $1.00$ to $0.21$.
}

\DIFadd{Two caveats keep this from being a clean fix. A }\emph{\DIFadd{trained}} \DIFadd{detector---a character
n-gram classifier fitted on labelled canaries---still recovers every cohort, diluted or
not, because the template's fixed phrasing is a signature; but a provider who can fit
such a classifier already knows the template, which is a far stronger position than the
perplexity filter requires and one the threat model already excludes. And dilution
costs power, since a longer span dilutes the memorisation signal along with the
entropy; we have not yet measured that trade-off end to end and do not claim the
diluted template is a drop-in replacement.
}

\DIFadd{What detection does }\emph{\DIFadd{not}} \DIFadd{do is defeat the audit, and the reason is worth stating
plainly because it is the crux of the threat model. Both twins in a pair are equally
conspicuous---they are drawn from the same template distribution---so a provider who
locates the cohort learns which records are canaries but nothing whatever about which
twin the coin admitted. The null of Theorem~\ref{thm:null} survives complete
disclosure of the manifest's }\emph{\DIFadd{contents}}\DIFadd{; what it does not survive is a provider
who, having located the cohort, treats it differently from organic data. That is
special-casing, it is excluded by the honest-but-verifiable assumption, and it is
exactly the gap cryptographic attestation \mbox{%DIFAUXCMD
\citep{eisenhofer2025verifiable} }\hskip0pt%DIFAUXCMD
exists to
close. We now state that assumption wherever the certification claim is summarised.
}

\begin{table}[t]
\centering\small
\caption{\DIFaddFL{Canary detectability on TOFU under GPT-2 as the pristine base model
($3{,}000$ organic records, $384$ canaries, one seed --- this is a single
configuration, not a sweep). ``recall'' is at a $1\%$ false-positive
rate on organic data. The predicted excess is $H\ln 2/T$; the observed excess exceeds
it because the template's own phrasing is also off-distribution. Bold marks the one
configuration a zero-knowledge filter substantially fails to recover.}}
\label{tab:detect}
\input{tables/detect}
\end{table}

\DIFaddend \subsection{A model axis from 2019 to 2026}\label{sec:exp-zoo}

To show that nothing about the framework is tied to a particular model generation, we
run the pipeline across parameter scales and release years, from GPT-2 to the
2025--2026 cohort (Qwen-3 at 0.6B and 4B, Phi-4-mini, and the \DIFdelbegin \DIFdel{five-billion-parameter
}\DIFdelend \DIFaddbegin \DIFadd{5.1B-parameter
}\DIFaddend Gemma-4-E2B), using a shared-base multi-adapter design that keeps a single frozen base
in memory and represents every training stage as a LoRA adapter---a construction that
fits \DIFdelbegin \DIFdel{five-billion-parameter }\DIFdelend \DIFaddbegin \DIFadd{5B-parameter }\DIFaddend models on a \DIFdelbegin \DIFdel{sixteen-gigabyte }\DIFdelend \DIFaddbegin \DIFadd{16\,GB }\DIFaddend GPU and, incidentally, lets
NPO's frozen reference be the fine-tuned adapter on the same base rather than a second
model.
\DIFaddbegin 

\DIFaddend Table~\ref{tab:zoo} collects the verdict patterns, \DIFdelbegin \DIFdel{and they are stable across the axis }\DIFdelend \DIFaddbegin \DIFadd{now with the number of seeds stated
per row, because the earlier version did not distinguish the three-seed rows from the
single-seed ones and described the axis as ``stable'' on that basis. The corrected
description is narrower. Detection is stable}\DIFaddend : every un-unlearned model is revoked \DIFdelbegin \DIFdel{, exact unlearning certifies whenever the
cohort size suffices, and NPO sits in honest undetermined with positive drift at the 384-pair }\DIFdelend \DIFaddbegin \DIFadd{at
every scale, on every seed run. Certification is not uniformly stable: at the $384$-pair
}\DIFaddend cohorts we use for the largest \DIFdelbegin \DIFdel{models---the calibrated behaviour that
}\DIFdelend \DIFaddbegin \DIFadd{models, the super-population target returns
}\emph{\DIFadd{undetermined}} \DIFadd{for retraining and NPO on Qwen3-0.6B and Qwen3-4B, and the
single-seed rows cannot distinguish a systematic effect from noise. Under the cohort
target those cells issue. We think the fair summary is that the framework's
}\emph{\DIFadd{detection}} \DIFadd{behaviour is scale-stable on the evidence we have, that its
}\emph{\DIFadd{certification}} \DIFadd{behaviour at the largest scales is limited by cohort size exactly
as }\DIFaddend Theorem~\ref{thm:power} predicts\DIFaddbegin \DIFadd{, and that one seed per cell is too thin to say more}\DIFaddend .

\begin{table}[t]
\centering\small
\caption{Model axis, verdicts per seed for the core subjects \DIFaddbeginFL \DIFaddFL{at $\eps = 0.2$ on the
cohort target}\DIFaddendFL , spanning 0.9M to 5.1B parameters and release years 2019 to 2026. \DIFaddbeginFL \DIFaddFL{The
seeds column states how many runs each cell aggregates; the $4$ largest models have
$1$ seed each.}\DIFaddendFL }
\label{tab:zoo}
\input{tables/zoo}
\end{table}

\subsection{Ablations, guardrails, and cost}\label{sec:exp-ablation}

We close with the studies that probe the design choices and the practicality of the
whole apparatus (Figure~\ref{fig:ablation}). On \emph{betting strategy}, the
online-Newton-step learner, the mixture, aGRAPA, and the truncated
Krichevsky--Trofimov plug-in all certify within \DIFdelbegin \DIFdel{twenty thousand }\DIFdelend \DIFaddbegin \DIFadd{$20{,}000$ }\DIFaddend pairs, whereas a fixed
aggressive bet goes bankrupt and issues only \DIFdelbegin \DIFdel{ten percent }\DIFdelend \DIFaddbegin \DIFadd{$10\%$ }\DIFaddend of the time---adaptivity is not a
refinement but a requirement. On the \emph{magnitude-aware arm}, sparse leakage in
which only \DIFdelbegin \DIFdel{five percent }\DIFdelend \DIFaddbegin \DIFadd{$5\%$ }\DIFaddend of pairs are strongly memorised is detected in a median of $941$ pairs
against $2{,}196$ for sign-only betting, \DIFdelbegin \DIFdel{the }\DIFdelend \DIFaddbegin \DIFadd{a }\DIFaddend $2.3\times$ speed-up\DIFdelbegin \DIFdel{promised
by }\DIFdelend \DIFaddbegin \DIFadd{.
}\DIFaddend Proposition~\ref{prop:magnitude} \DIFaddbegin \DIFadd{says only that the magnitude arm is exact and more
powerful under sparse leakage; the factor is measured, not predicted}\DIFaddend . A \emph{guardrail on the protocol itself} confirms
that planting canaries does not damage the model being audited: even at an extreme
$38\%$ canary share, held-out loss shifts by only $0.005$ nats per character\DIFdelbegin \DIFdel{, and the
deployment target is }\DIFdelend \DIFaddbegin \DIFadd{. We should
be explicit that our own experiments sit far above a deployment share: the canary
character share is $11.9\%$ on TOFU and $22.5\%$ in the tight-tolerance cohort of
Section~\ref{sec:exp-tight}, against a target }\DIFaddend below $0.05\%$ \DIFdelbegin \DIFdel{. Finally, on }\DIFdelend \DIFaddbegin \DIFadd{for a provider with
millions of records. Academic corpora are simply too small to plant a
statistically useful cohort at a deployment share, which is why the guardrail study
exists; but it also means the detectability and dose--response numbers are measured at
a share no deployment would use, and both would be harder to reproduce at
$0.05\%$---detection because the flagged set's precision collapses, dose--response
because the absolute cohort would have to be far larger.
}

\DIFadd{On }\DIFaddend \emph{cost}, \DIFdelbegin \DIFdel{a full certification
takes seventy-six seconds on a single mid-range GPU---about ten thousand }\DIFdelend \DIFaddbegin \DIFadd{the earlier version reported a single figure of $76$ seconds
without saying which configuration produced it, and compared it to alternatives without
stating the hardware. Both are corrected here. Verification cost is
$2 \cdot Q \cdot m$ }\DIFaddend short forward passes and no training at \DIFdelbegin \DIFdel{all---against one full fine-tune for }\DIFdelend \DIFaddbegin \DIFadd{all: $1{,}536$ passes per
subject at the benchmark tier's $Q = 2$ and $m = 384$, and $5{,}120$ at the GPT-2
synthetic tier's $Q = 4$ and $m = 640$. Measured
end to end on the hardware each tier actually ran on, the median scoring time per
certificate is $13$ seconds for the small transformer, $63$ seconds for GPT-2 at $640$
pairs, $112$--$148$ seconds for the 124M--1.4B benchmark tier, and $397$--$499$ seconds
for the 3.8B--5.1B models. The comparison to descriptive alternatives should be read as
a ratio of }\emph{\DIFadd{workloads}}\DIFadd{, not of wall-clock times on unmatched hardware: }\DIFaddend a
retrain-based reference \DIFdelbegin \DIFdel{and at least sixteen for }\DIFdelend \DIFaddbegin \DIFadd{costs one full fine-tune of the same model on the same
hardware, and }\DIFaddend a shadow-model evaluator \DIFdelbegin \DIFdel{, which makes VOUCHroughly
sixty times cheaper than the strongest descriptive alternative while providing the error control that alternative lacks}\DIFdelend \DIFaddbegin \DIFadd{at least $16$, against VOUCH's zero. On our
TOFU/GPT-2 configuration one fine-tune is $600$ optimiser steps at batch $8$, roughly
$4{,}800$ sequence-forward-and-backward passes, so a $16$-model shadow panel is
about $77{,}000$ training passes against VOUCH's $1{,}536$ inference passes, a ratio of
about $50$ to one---and the same ratio on any hardware, because both scale with the
same model. A training pass is also several times more expensive than an inference
pass, so the wall-clock gap is larger than $50\times$; we give the pass ratio because
it is the part we can state without assuming a hardware model. We give a ratio rather than a speed-up factor because the two workloads are not
interchangeable}\DIFaddend .

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig5_ablation.pdf}
\caption{\textbf{Ablations.} Left: betting strategies; a fixed aggressive bet goes
bankrupt. Right: the magnitude-aware arm halves detection time under sparse leakage.}
\label{fig:ablation}
\end{figure}


\section{Limitations and conclusion}\label{sec:conclusion}

We set out to turn unlearning verification from a descriptive exercise into
certification, and the ingredients that made it possible were an exact null created by
the verifier's own randomness, a betting engine that survives continuous monitoring and
streaming deletions, and a pair of composition rules that we proved must run in
opposite directions. The empirical study shows the framework is not merely sound but
practical: it calibrates correctly under adversarial stopping, \DIFdelbegin \DIFdel{certifies genuine }\DIFdelend \DIFaddbegin \DIFadd{matches or beats four
valid sequential procedures at a fraction of their query cost, certifies exact
}\DIFaddend unlearning on standard benchmarks across architectures \DIFaddbegin \DIFadd{at tolerances down to
$\eps = 0.05$}\DIFaddend , measures recoverability instead of arguing about it, and does so at a
verifier cost \DIFdelbegin \DIFdel{two orders of magnitude below shadow-model
evaluation}\DIFdelend \DIFaddbegin \DIFadd{of $2Qm$ forward passes and no training}\DIFaddend .

The claim a certificate makes is precise, and we regard stating its boundaries just as
precisely as part of the contribution. VOUCH certifies \DIFdelbegin \DIFdel{extractable influence through a declared score class on the canary law: parameter-space }\DIFdelend \DIFaddbegin \DIFadd{the residual advantage
extractable through a }\emph{\DIFadd{declared score class}} \DIFadd{$\F$, on a }\emph{\DIFadd{committed canary
cohort}}\DIFadd{, against the }\emph{\DIFadd{deployed model}}\DIFadd{, under an }\emph{\DIFadd{honest-but-verifiable}}
\DIFadd{provider. Each of those four qualifications has a measured cost. Parameter-space
}\DIFaddend erasure is behaviourally uncertifiable, so we do not claim it\DIFdelbegin \DIFdel{; the transfer from canaries to organic data is calibrated by the dose--response strata,
not proven; and }\DIFdelend \DIFaddbegin \DIFadd{. The class restriction
remains a definitional limit, but this version measures rather than merely names it.
Two weak out-of-class attacks settle nothing, failing to beat the noise floor of
subsampling. A LiRA-style shadow-model attack does better: with matched shadows it
recovers an advantage of $0.82$ on an un-unlearned model, and on a certified one
returns $-0.016$ with a $95\%$ interval that excludes every tolerance we certify at
(Section~\ref{sec:exp-lira}), a pattern that reproduces on the architecture this
paper's own certified-NPO claim is about---$+0.48$ on the un-unlearned model,
$-0.03$ on the certified one, again excluding every tolerance
(Section~\ref{sec:exp-lira-gpt2}). Both runs are $16$--$24$ shadows against the
hundreds a canonical LiRA study would use, so together they bound the gap rather than
closing it. Enlarging $\F$ costs power and no validity, and
we now exercise that too: a paraphrase-aware score of the kind
\mbox{%DIFAUXCMD
\citet{li2026beliefs} }\hskip0pt%DIFAUXCMD
motivate is implemented and added to $\F$
(Section~\ref{sec:exp-para}). The cohort statement transfers to
the mechanism-level advantage only at the explicit inflation of
Proposition~\ref{prop:transfer}, which is why our $\eps = 0.05$ results are cohort
certificates; }\DIFaddend the \DIFaddbegin \DIFadd{super-population arm reaches $\eps = 0.1$ once the cohort clears
$3{,}000$ pairs and no further, exactly as Theorem~\ref{thm:power} predicts. That
experiment also exposed the sharpest limit we know of: at a single insertion per
canary---the most organic-like dose---a 124M-parameter model memorises so little that
the un-unlearned model certifies too, so the audit has no power in precisely the
regime the canary-to-organic extrapolation needs it. The }\DIFaddend guarantee is
per-cohort rather than per-user, since per-request certificates would require per-user
canaries. The provider is assumed honest-but-verifiable, and \DIFaddbegin \DIFadd{we now know that
assumption is easier to violate than we had supposed: a perplexity filter costing one
forward pass per record locates the entire planted cohort, so }\DIFaddend a provider willing to
special-case \DIFdelbegin \DIFdel{the committed canaries }\DIFdelend \DIFaddbegin \DIFadd{it can find it, and closing that gap }\DIFaddend needs cryptographic attestation that
VOUCH does not itself supply. \DIFaddbegin \DIFadd{The audit's own randomness is now as checkable as its
commitment---the reveal order can be drawn from a public randomness beacon bound to the
manifest root, which is what Theorem~\ref{thm:cert}'s hypothesis actually
requires---though the results reported here predate that and were computed with a
seed-derived order. }\DIFaddend Canaries must be planted before fine-tuning, so legacy
models cannot be retro-certified. \DIFdelbegin \DIFdel{Finally, both }\DIFdelend \DIFaddbegin \DIFadd{Both }\DIFaddend two-sided certification and consensus-based
streaming composition cost power, which we quantify (Tables~\ref{tab:power} and
\ref{tab:streaming}) so that cohorts can be sized accordingly\DIFdelbegin \DIFdel{rather than discovering
the cost after the fact}\DIFdelend \DIFaddbegin \DIFadd{. And the extrapolation
from planted canaries to organic records remains calibrated by the dose--response
strata rather than proven---with the $r = 1$ stratum, the one most like an organic
record, carrying the weakest signal}\DIFaddend .

Within those boundaries, the framework delivers what a deletion audit actually needs:
an error-controlled, continuously monitorable, black-box certificate of bounded
residual influence \DIFaddbegin \DIFadd{on a committed cohort}\DIFaddend . The released harness reproduces the entire
study on free, ephemeral compute, which we hope lowers the barrier to auditing
unlearning wherever it is deployed.

\backmatter
\DIFaddbegin 

\begin{appendices}
\section{\DIFadd{Reproducibility}}\label{app:repro}
\DIFadd{Everything below is fixed by the released code and its committed seeds; this appendix
collects the settings a reader would otherwise have to recover from source.
}

\paragraph{\DIFadd{What ``retraining from scratch'' means here.}} \DIFadd{All fine-tuning and all
unlearning in this paper operate on LoRA adapters of rank $16$
($\alpha_{\mathrm{LoRA}} = 32$, dropout $0$, target modules }\texttt{\DIFadd{all-linear}}\DIFadd{)
over a }\emph{\DIFadd{frozen}} \DIFadd{pretrained base model. ``Retrain'' therefore means: initialise a
}\emph{\DIFadd{fresh}} \DIFadd{adapter on the same frozen base and train it on
$D_{\text{keep}}$ only---the keep split with neither the organic forget data nor any
canary---using the identical optimiser, schedule and step count as the original
fine-tune. It is exact unlearning }\emph{\DIFadd{within the adapter paradigm}}\DIFadd{: the adapter that
saw the canaries is discarded and never contributes to the retrained model, and the
frozen base never saw a canary at any point. It is not retraining of the base model's
pretraining, which we never perform and do not claim. We now call this subject
``retrain (fresh adapter)'' in the tables to remove the ambiguity.
}

\DIFadd{Whether adapter-level unlearning is representative of a deployment setting deserves a
direct answer, since the certificate is meant to cover the latter. The audit itself is
indifferent: it queries $M_u$ as a black box and never inspects parameters, so nothing
in Theorem~\ref{thm:null} or Theorem~\ref{thm:cert} changes if the provider unlearns by
full-parameter gradient steps instead. What the adapter setting does change is the
}\emph{\DIFadd{difficulty}} \DIFadd{of the unlearning problem the subjects face. A rank-$16$ adapter has
far fewer degrees of freedom in which to hide memorised content than a full
fine-tune, so our subjects are, if anything, being certified in a regime that favours
them; a provider doing full-parameter unlearning should expect verdicts at least as
demanding. We flag this as a limitation of the empirical study rather than of the
framework, and full-parameter runs are the natural next scaling step.
}

\paragraph{\DIFadd{Models.}} \texttt{\DIFadd{gpt2}} \DIFadd{(124M), }\texttt{\DIFadd{EleutherAI/pythia-160m}} \DIFadd{(160M),
}\texttt{\DIFadd{microsoft/phi-1\_5}} \DIFadd{(1.4B), }\texttt{\DIFadd{Qwen/Qwen3-0.6B}}\DIFadd{,
}\texttt{\DIFadd{Qwen/Qwen3-4B-2507}}\DIFadd{, }\texttt{\DIFadd{microsoft/Phi-4-mini-instruct}}\DIFadd{,
}\texttt{\DIFadd{google/gemma-4-E2B-it}}\DIFadd{, and an in-repo 0.9M-parameter TinyGPT for the
CPU-affordable tier. Phi-1.5 LoRA fine-tuning diverges to NaN in fp16, so all Phi-1.5
runs are fp32; the remaining large models run fp16 with a shared frozen base and one
adapter per training stage.
}

\paragraph{\DIFadd{Data.}} \DIFadd{TOFU \mbox{%DIFAUXCMD
\citep{maini2024tofu}}\hskip0pt%DIFAUXCMD
: keep $=$ }\texttt{\DIFadd{retain90}} \DIFadd{minus a
200-record held-out slice, forget $=$ }\texttt{\DIFadd{forget10}} \DIFadd{(400 QA pairs), public probe
set for P1 and the capability reading $=$ }\texttt{\DIFadd{world\_facts}}\DIFadd{, utility $=$ the
held-out retain slice, all formatted }\texttt{\DIFadd{Question: \{q\}\textbackslash nAnswer:
\{a\}}}\DIFadd{. MUSE-News \mbox{%DIFAUXCMD
\citep{shi2024muse} }\hskip0pt%DIFAUXCMD
raw split: keep $=$ 3}{\DIFadd{,}}\DIFadd{000 500-character chunks
of }\texttt{\DIFadd{retain1}}\DIFadd{, forget $=$ 500 chunks of }\texttt{\DIFadd{forget}}\DIFadd{, public/holdout $=$ 600
chunks of }\texttt{\DIFadd{holdout}} \DIFadd{split 400/200 into the relearn corpus and the utility set.
}

\paragraph{\DIFadd{Canaries.}} \DIFadd{Templated generator, deterministic in the run seed; domains
}\texttt{\DIFadd{qa}} \DIFadd{for TOFU and \{}\texttt{\DIFadd{pii}}\DIFadd{, }\texttt{\DIFadd{fact}}\DIFadd{\} for MUSE-News; repetition
strata $r_i \in \{1,2,4,8\}$ assigned round-robin; secrets of $10$ alphanumeric
characters ($51.7$ bits). Cohort sizes: $384$ pairs (TOFU/GPT-2, TOFU/Pythia, all
model-axis runs), $512$ (TOFU/Phi-1.5, all MUSE-News runs, TinyGPT), $640$ (the
GPT-2 synthetic tier). Canary character share of the fine-tuning corpus is $11.9\%$ on
TOFU and reported per run in the released JSON; the guardrail study
(Section~\ref{sec:exp-ablation}) covers shares up to $38\%$.
}

\paragraph{\DIFadd{Training and unlearning.}} \DIFadd{Fine-tune and retrain: $600$ steps, batch $8$,
AdamW at learning rate $2\times10^{-4}$, block length $160$, gradient-norm clip $1.0$.
Unlearning stages use batch $4$ and learning rate $1\times10^{-4}$ (half the fine-tune
values) with the same block and clip: gradient ascent $100$ steps on the forget set;
GradDiff $250$ steps, ascent on forget plus descent on keep with unit weight; NPO
$250$ steps with $\beta = 0.1$ and the fine-tuned adapter as the frozen reference;
SimNPO $250$ steps with $\beta = 0.1$ and margin $\gamma = 0$, reference-free on the
length-normalised forget NLL; RMU $250$ steps at the layer half-way through the
network's depth ($\mathrm{layer\_frac} = 0.5$), pushing forget activations toward a
fixed random unit direction scaled by $c = 20$ while holding retain activations at
unit weight against the frozen fine-tuned adapter's; ``NPO (25\% budget)'' truncates
NPO to $62$ steps.
Probes: P1 relearn $=$ $100$ steps on the public corpus, which never contains a
canary; P2 $=$ symmetric per-channel round-to-nearest 4-bit quantisation of all linear
and embedding weights; P3 $=$ three adversarial prompt wrappers applied at scoring
time only. Probe budgets are fixed across all subjects and models.
}

\paragraph{\DIFadd{Scoring.}} \DIFadd{$Q = 2$ query wrappers for every benchmark and model-axis run
(identity and ``According to the archives, \ldots''); $Q = 4$ for the GPT-2 synthetic
tier, which additionally reports the base-calibrated }\texttt{\DIFadd{ratio}} \DIFadd{score. Score class
$\F = \{s_{\mathrm{loss}}, s_{\mathrm{mink}}\}$ for the benchmark and model-axis runs
and $\F = \{s_{\mathrm{loss}}, s_{\mathrm{mink}}, s_{\mathrm{ratio}}\}$ for the
synthetic tier, with $k = 20\%$ for min-$k$. Scores are computed from temperature-0
forward passes and aggregated over wrappers by the mean. Verification cost is exactly
$2Qm$ forward passes per subject.
}

\paragraph{\DIFadd{Two implementation notes that affect validity.}} \DIFadd{First, the stake cap and the
wealth factor must be computed from the }\emph{\DIFadd{same}} \DIFadd{clamped value of the recentred
boundary $p_0(t)$. The admissible range is $\lambda_t < c/(1-p_0(t))$, so deriving the
cap from one clamped boundary and the payoff from another lets the worst-case factor
$1 + \lambda_t(p_0(t)-1)$ fall below zero. The case is reachable exactly when $m\,p_0$ is an integer---which holds for
$m = 640$ at every tolerance we report, in both directions---so it is not a corner a
reimplementation can ignore. The released code derives both from one clamp and carries
a regression test for it.
}

\DIFadd{Second, Theorem~\ref{thm:cert} requires the reveal order $\pi$ to be independent of
the realised sign vectors. Every result table in this paper was produced with
}\texttt{\DIFadd{reveal\_source = "seeded"}}\DIFadd{, in which $\pi$ is seeded from the run seed---the
same seed that drives cohort generation and training---so for those runs the
independence does not strictly hold, even though nothing in the pipeline exploits the
coupling. We report them as they were computed rather than restating them under a
different reveal rule. The released code now also implements the two sources that do
satisfy the hypothesis (}\texttt{\DIFadd{vouch/verify/beacon.py}}\DIFadd{): }\texttt{\DIFadd{"beacon"}} \DIFadd{seeds $\pi$
with $H(\text{manifest root} \,\|\, r)$ for the randomness $r$ of a drand round drawn
after the commitment is published, recording the round number and value on the
certificate so a third party can recompute the order; and }\texttt{\DIFadd{"local"}} \DIFadd{draws from
operating-system entropy. A deployed audit should use the beacon, and the certificate
object now carries a }\texttt{\DIFadd{reveal}} \DIFadd{block naming the source, whether it is
third-party verifiable, and whether it meets Theorem~\ref{thm:cert}'s hypothesis.
}

\paragraph{\DIFadd{Verification.}} \DIFadd{$\alpha = 0.05$; two-sided certification; sign-based
revocation for all headline tables, with the magnitude-aware arm reported separately;
finite-cohort certificate target by default and the super-population target reported
alongside wherever tolerance matters. Confidence-sequence grid $1001$ points on
$[0,1]$. Betting mixture: fixed fractions $\{0.02, 0.05, 0.1, 0.2, 0.4, 0.7\}$ of
$\lambda_{\max}$, an ONS expert with $\eta = \tfrac12$ and $A_0 = 1$, and a truncated
KT plug-in, combined with uniform prior weights. Reveal order: a permutation seeded by
the run seed, committed before any query. Tie-breaking: a committed PRNG seeded at
$20260702$, shared across every run so that tie decisions are reproducible.
Early stopping is enabled for the certificate arm; the revocation arm continues to
accumulate evidence after a crossing so that the streaming product alarm is
well defined.
}

\paragraph{\DIFadd{Simulation tiers.}} \DIFadd{Validity, soundness, dependence and baselines: $2{,}000$
seeds. Power and censoring: $500$ seeds per cell with a $20{,}000$-pair horizon.
Tightness and streaming: $500$ seeds. Group-sequential boundaries use $K = 10$ equally
spaced looks with Pocock and O'Brien--Fleming spending functions, calibrated by exact
binomial dynamic programming over the partial-sum lattice at the least favourable
null.
}

\paragraph{\DIFadd{Hardware and cost accounting.}} \DIFadd{The study ran on heterogeneous free and local
compute: Colab and Kaggle T4/P100 GPU sessions for the 1.4B--5.1B models, an
Apple-silicon laptop (MPS backend) for the GPT-2 and Pythia benchmark runs and the
revision-round SimNPO runs, and Colab CPU sessions plus a four-core container for the
TinyGPT tier and all simulations. Because the hardware is not matched across tiers, we
report verifier cost as a }\emph{\DIFadd{workload}} \DIFadd{in forward passes and give measured
wall-clock times only alongside the tier that produced them
(Section~\ref{sec:exp-ablation}). The comparison against retrain-based and
shadow-model evaluators is likewise stated as a ratio of workloads on the same model
and hardware---$1{,}536$ inference passes against roughly $4{,}800$ training passes per
fine-tune and $16$ fine-tunes for a shadow panel---rather than as a wall-clock
speed-up factor.
}

\end{appendices}
\DIFaddend 

\section*{Data availability statement}

The complete source code of the VOUCH framework, every experimental result reported
in this paper (including all per-seed JSON records, generated tables, and figures),
and the fault-tolerant execution harness that produced them are publicly available
\ifanon
in an open repository; the repository reference is withheld here to preserve
double-blind review and is provided to the editorial office in the cover letter.
\else
at \url{https://github.com/vinhqdang/VOUCH-Verifiable-Online-Unlearning-Certification-via-Hypothesis-betting}.
\fi
The TOFU and MUSE-News benchmark datasets used in the experiments are publicly
available from their original releases \citep{maini2024tofu,shi2024muse}; all
canary data are generated deterministically from committed seeds by the released
code.

\bibliography{refs}

\end{document}
